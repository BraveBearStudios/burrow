---
phase: 05-stop-start-controls-drawer-polish
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - ui/tests/e2e/stop-start.spec.ts
autonomous: true
requirements: [UI-07, UI-08, UI-09, UI-10, UI-11]

must_haves:
  truths:
    - "Over the real built UI (Fake provider + stub ttyd), a create->stop->start journey passes: Stop hits POST /stop, the placeholder appears after the poll, Start hits POST /start, the terminal remounts"
    - "On a 375px viewport the activity drawer measures full-width (boundingBox width == 375)"
    - "Keyboard-focusing a control paints the --accent-line ring and a scroll surface renders the custom scrollbar (live-browser proof)"
  artifacts:
    - path: "ui/tests/e2e/stop-start.spec.ts"
      provides: "The phase e2e gate — stop/start round-trip + 375px drawer width + focus-ring/scrollbar live assertions"
      contains: "waitForResponse"
  key_links:
    - from: "ui/tests/e2e/stop-start.spec.ts"
      to: "ui/dist (built SPA) + Fake provider + stub ttyd"
      via: "Playwright webServer over BURROW_COMPUTE=fake"
      pattern: "workspaces/[^/]+/(stop|start)"
---

<objective>
Close the phase with the Playwright e2e gate over the real built UI: complete the Plan-01 `stop-start.spec.ts` scaffold so the live-browser proofs for UI-07..UI-11 pass (the stop→start round-trip, the 375px full-width drawer, and the focus-ring/scrollbar render that jsdom cannot assert). This is the integration tier that proves the whole slice end-to-end over `BURROW_COMPUTE=fake` + the protocol-accurate stub ttyd.

Purpose: vitest proves the seams; only real Chromium proves the media-query width, the `:focus-visible` paint, the scrollbar pseudo-element, and the real ~3s poll driving the Stop↔Start swap. The e2e harness (Fake + stub ttyd + vite preview) already ships from Plan 02-06 — no harness change.
Output: the finished `stop-start.spec.ts` and a green full suite (unit + lint + typecheck + build + e2e).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-VALIDATION.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-UI-SPEC.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-PATTERNS.md

<interfaces>
<!-- The e2e handles + harness. Extracted from source. -->

From ui/tests/e2e/terminal.spec.ts (analog):
- `createWorkspace(page, name)` helper (lines 32-45): opens New workspace, fills #ws-name/#ws-repo/#ws-branch, clicks Create, waits for the dialog hidden + the name visible
- `waitForResponse` POST-assertion pattern (the DELETE example at lines ~131): `page.waitForResponse(res => /regex/.test(new URL(res.url()).pathname) && res.request().method() === "POST")`
- `test.describe.configure({ mode: "serial" })` + a `Date.now()`-stamped unique name per run (lines 20-25)

From ui/tests/e2e/activity-drawer.spec.ts (analog):
- the open-drawer flow: `page.getByRole("button", { name: "Activity log" }).first().click()` then `page.getByRole("dialog")`

From ui/playwright.config.ts (harness — no change):
- the `webServer` runs the stub ttyd + uvicorn-on-Fake + `vite preview` over `ui/dist`; `BURROW_E2E_TTYD_HOST` retargets the bridge dial at the local stub
- Chromium `Desktop Chrome`; `ui/dist` must be freshly built (`npm run build`) before `npm run e2e`

From the Plan-02 / Plan-03 deliverables this gate exercises:
- `aria-label="Stop workspace"` / `"Start workspace"` buttons (Plan 02); the `Workspace stopped` placeholder + `Start workspace` CTA (Plan 02)
- `--w-drawer` token + 375px override; the global `:focus-visible` ring; the custom scrollbar (Plan 03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the stop->start round-trip e2e + the 375px drawer-width + focus-ring/scrollbar live assertions</name>
  <files>ui/tests/e2e/stop-start.spec.ts</files>
  <read_first>
    - ui/tests/e2e/stop-start.spec.ts (the file being modified — the Plan-01 scaffold)
    - ui/tests/e2e/terminal.spec.ts (analog — `createWorkspace`, `waitForResponse`, serial mode, the unique-name stamp)
    - ui/tests/e2e/activity-drawer.spec.ts (analog — the open-drawer flow + the dialog handle)
    - 05-PATTERNS.md "ui/tests/e2e/*.spec.ts — stop -> placeholder -> start journey" (the journey shape, the POST regex adaptation, the 375px viewport assertion) lines 494-535
    - 05-RESEARCH.md "Playwright: drawer full-width at 375px" + "Playwright: stop->start round-trip" code examples
    - 05-UI-SPEC.md "Acceptance-Checkable Visual Criteria" 1, 5, 6, 8, 9, 10 (the live proofs)
  </read_first>
  <action>
    Finish `ui/tests/e2e/stop-start.spec.ts` (the Plan-01 scaffold) so it runs against the real built UI. (1) Round-trip: `createWorkspace(page, uniqueName)` → click `getByRole("button", { name: "Stop workspace" }).first()` inside a `Promise.all` with `page.waitForResponse` asserting `POST …/workspaces/[^/]+/stop$` (`stopRes.ok() === true`) → assert `getByText("Workspace stopped")` visible (poll-driven, generous timeout) and the `[data-testid^="term-"]` body gone → click `getByRole("button", { name: "Start workspace" })` (placeholder CTA or header) inside a `Promise.all` with `waitForResponse` for `…/start$` → assert the placeholder hidden and `[data-testid^="term-"]` visible again. (2) Responsive (UI-09): in a `test.use({ viewport: { width: 375, height: 800 } })` block, create a workspace, open the Activity drawer (`Activity log` button), assert `(await drawer.boundingBox())?.width === 375`; optionally a sibling assertion at a >375px viewport that width is ~360px. (3) Focus ring (UI-10): Tab to (or `.focus()`) an interactive control and assert the painted ring via `toHaveCSS("outline-style", "solid")` (or `outline-width` `2px`) — `:focus-visible` is UA-heuristic, so this is the only place it is real. (4) Scrollbar (UI-11): on a scroll surface (drawer list / terminal body), assert the styled scrollbar renders (a `toHaveCSS("scrollbar-width", "thin")` Firefox assert and/or a Chromium screenshot/visual check per the harness; pick the assertion the Chromium runner supports). Keep `test.describe.configure({ mode: "serial" })` and the unique-name-per-run stamp. SPDX header intact.
  </action>
  <verify>
    <automated>cd ui && npm run build && npm run e2e 2>&1 | grep -Ev '^#' | grep -Eq 'stop-start|passed|✓'</automated>
    <human-check>End-of-phase visual sign-off (human_verify_mode=end-of-phase): in the running app, switch through all four data-themes (dark / dark-soft / medium / light) and confirm on each — (a) a running panel shows the outline-square Stop button (neutral, never red/gold), a stopped panel shows the outline play-triangle Start button + the calm `Workspace stopped` placeholder (not an error scrim); (b) keyboard-Tab paints the `--accent-line` focus ring on the Stop/Start buttons, the placeholder CTA, the drawer ×, and sidebar rows; (c) the drawer list / terminal body / sidebar list show the custom Burrow scrollbar (thumb `--border-mid`), not the native one; (d) at a ≤375px window the drawer fills the width as a sheet. Confirm zero gold on the new controls and no CDN/font request in the network panel.</human-check>
  </verify>
  <acceptance_criteria>
    - `ui/tests/e2e/stop-start.spec.ts` asserts: a `POST …/workspaces/<id>/stop` fires and `stopRes.ok()`; the `Workspace stopped` placeholder appears after the poll; a `POST …/start` fires; the terminal `[data-testid^="term-"]` remounts.
    - The 375px viewport test asserts `drawer.boundingBox().width === 375`.
    - A live focus-ring assertion (`toHaveCSS("outline-style","solid")` / `outline-width 2px`) and a scrollbar render assertion exist (the jsdom-untestable halves of UI-10/UI-11).
    - `cd ui && npm run build && npm run e2e` exits 0 (the `stop-start` spec passes over the Fake + stub ttyd).
    - The end-of-phase `<human-check>` four-theme visual sign-off is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The phase e2e gate passes over the real built UI: stop→start round-trip, the 375px full-width drawer, and the live focus-ring/scrollbar proofs are all green; the four-theme visual sign-off is captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → built SPA → Fake provider + stub ttyd | the e2e exercises the real UI over `BURROW_COMPUTE=fake`; `BURROW_E2E_TTYD_HOST` retargets the bridge dial at the local stub (operator env, never client input — SSRF posture unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | the built `ui/dist` consumed by the e2e | mitigate | the e2e runs the real build; the four-theme `<human-check>` + the `googleapis\|gstatic\|jsdelivr` source assert confirm no CDN/font request sneaks into `dist` |
| T-05-10 | Elevation (of action) | the live stop/start round-trip | mitigate | the e2e proves the UI only offers the applicable control and the backend (Fake, lifecycle-accurate) drives the real status transition — no illegal action is reachable in the journey |
| T-05-04 | Spoofing (of true state) | the real ~3s poll | mitigate | the e2e asserts the placeholder appears only AFTER the poll re-lists `stopped` (server-truth), proving no optimistic flip masks a backend state |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package install (the Playwright/Chromium toolchain already ships; `npx playwright install chromium` is a CI step, not a package add); legitimacy gate n/a |

**Block-on-HIGH:** no HIGH threats — LAN-only, no-auth v1, Fake provider, no real Proxmox, no secret. No auth/session/crypto control added (out of scope, CLAUDE.md). The real-Proxmox stop/start is the deferred v1.0 acceptance debt (ACC-01), explicitly NOT a v1.1 gate.
</threat_model>

<verification>
- `cd ui && npm test && npm run lint && npm run typecheck && npm run build && npm run e2e` — the FULL suite green (the phase gate): all UI-07..UI-11 unit + integration tests plus the `stop-start.spec.ts` e2e leg.
- `ui/dist` is freshly rebuilt before `npm run e2e` (the `vite preview` serves the built SPA, not live source).
- The end-of-phase four-theme `<human-check>` sign-off is recorded (no `checkpoint:human-verify` task — human_verify_mode is end-of-phase).
</verification>

<success_criteria>
- The stop→start round-trip e2e passes over the Fake + stub ttyd: Stop→`POST /stop`→placeholder-after-poll→Start→`POST /start`→terminal remounts.
- The drawer measures full-width (375px) on a phone viewport; the focus ring and custom scrollbar render in live Chromium.
- The full suite (unit + lint + typecheck + build + e2e) is green; the four-theme visual sign-off confirms tokens-only, zero gold, no CDN.
</success_criteria>

<output>
Create `.planning/phases/05-stop-start-controls-drawer-polish/05-04-SUMMARY.md` when done
</output>
