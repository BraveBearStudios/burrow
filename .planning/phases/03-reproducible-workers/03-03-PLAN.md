---
phase: 03-reproducible-workers
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [WORK-02, WORK-05]
must_haves:
  truths:
    - "The plugin-cadence decision (boot-time-latest, reproducibility via manifest ref-pinning) is recorded in ADR-0009 with Status/Context/Decision/Consequences/Revisit-trigger sections"
    - "CI lints burrow-boot.sh with shellcheck and runs the boot-harness + manifest-schema pytest tiers so manifest drift and boot-script regressions fail the build"
  artifacts:
    - path: "docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md"
      provides: "The B4 plugin-cadence decision record (SC-4)"
      contains: "boot-time-latest"
    - path: ".github/workflows/ci.yml"
      provides: "shellcheck on the boot script + a pytest job running the boot + manifest tiers"
      contains: "shellcheck"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "cc-worker-config/lxc/worker-template/burrow-boot.sh"
      via: "shellcheck step"
      pattern: "shellcheck"
    - from: ".github/workflows/ci.yml"
      to: "api/tests/boot + api/tests/integration/test_manifest_schema.py"
      via: "uv run pytest step"
      pattern: "uv run pytest"
---

## Phase Goal

**As an** operator, **I want to** see the plugin-cadence decision recorded and the boot script + manifest gated in CI, **so that** reproducibility semantics are explicit and drift/regressions fail the build, not the homelab.

<objective>
Close the phase: record the B4 plugin-cadence decision as ADR-0009 (boot-time-latest; reproducibility via manifest ref-pinning, not config-repo snapshotting) so reproducibility semantics are explicit (SC-4), and wire CI to actually run the new gates — a `shellcheck` step on `burrow-boot.sh` (the repo noted shellcheck was unavailable on the Windows dev host; CI is where it runs) and a `pytest` job running the boot-harness + manifest-schema tiers so manifest drift (Pitfall 5) and boot-script regressions fail the build.

This plan touches only `docs/` and `.github/` — no overlap with the boot script or manifest (Plans 01/02), so it runs in Wave 2 in parallel with Plan 02.

Purpose: deliver SC-4 (the recorded cadence decision) and make the WORK-02/WORK-05 gates CI-enforced.
Output: ADR-0009 + a CI shellcheck step + a CI pytest job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reproducible-workers/03-CONTEXT.md
@.planning/phases/03-reproducible-workers/03-PATTERNS.md
@docs/adr/ADR-0002-boot-config-pull-at-boot.md
@.github/workflows/ci.yml

<interfaces>
<!-- ADR template (ADR-0002 headings to mirror exactly):
     front-matter SPDX comment (lines 1-4) → # ADR-0009: ... → ## Status (Accepted)
     → ## Context → ## Decision → ## Consequences → ## Revisit trigger -->

CI structure (ci.yml): the `static-gates` job already SHA-pins third-party actions
(checkout @34e1148... v4.3.1, setup-uv @d0cc045... v6.8.0, setup-node @4993... v4.4.0)
and runs `uv sync --frozen` + ruff/mypy/lock-check (working-directory: api) + `uvx reuse lint`.
A new pytest step belongs after the api install steps (working-directory: api), and a
shellcheck step is a repo-root step (no working-directory). New third-party actions MUST be
SHA-pinned with a trailing version comment (the repo convention).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0009 (plugin cadence: boot-time-latest)</name>
  <files>docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md</files>
  <read_first>
    - docs/adr/ADR-0002-boot-config-pull-at-boot.md (copy the section skeleton EXACTLY: SPDX front-matter, # ADR-NNNN title, ## Status / Context / Decision / Consequences / Revisit trigger)
    - .planning/phases/03-reproducible-workers/03-CONTEXT.md §"Plugin Cadence & Reproducibility (B4)" (the decision content)
    - 03-PATTERNS.md §ADR-0009 (numbering: 0001-0008 exist, 0009 is next free; the exact content bullets)
  </read_first>
  <action>
    Create `docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md` mirroring the ADR-0002 structure. Status: Accepted. Decision: cadence = boot-time-latest — the boot pulls the `cc-worker-config` branch HEAD each boot (tech-spec §988); reproducibility comes from MANIFEST REF-PINNING (each `claude-plugin` entry pins an immutable git tag/commit SHA), NOT from snapshotting the config repo per workspace. Context: record the plugin-type split (`binary`/`npm-global` baked at provision time; only `claude-plugin` pulled fresh at boot) and why boot-time-latest was chosen over snapshot-at-create. Consequences: two boots of the same manifest → identical plugin tree (SC-2); a `ref: "main"` for a claude-plugin is intentionally non-reproducible "latest" (Pitfall 1). Revisit trigger: per-workspace plugin version pinning / snapshot-at-create, deferred until the manifest stabilizes (CONTEXT.md Deferred Ideas). SPDX two-line header in the markdown comment front-matter (lines 1-4, like ADR-0002). Match the existing ADR house style (`##` headings only).
  </action>
  <verify>
    <automated>test -f docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md && grep -c "boot-time-latest" docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/ADR-0009-plugin-cadence-boot-time-latest.md` exists with the SPDX front-matter comment (lines 1-4).
    - It has the five sections `## Status`, `## Context`, `## Decision`, `## Consequences`, `## Revisit trigger` (grep each).
    - Decision states boot-time-latest + reproducibility via manifest ref-pinning; Revisit trigger names snapshot-at-create / per-workspace pinning.
    - `uvx reuse lint` is green (the ADR carries its header).
  </acceptance_criteria>
  <done>ADR-0009 records the B4 cadence decision with the full Nygard section skeleton (SC-4 satisfied).</done>
</task>

<task type="auto">
  <name>Task 2: Wire CI — shellcheck on burrow-boot.sh + a pytest job for the boot + manifest tiers</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the static-gates job structure, SHA-pin convention with trailing version comments, working-directory: api pattern — lines 22-76)
    - 03-RESEARCH.md §Wave 0 Gaps (the shellcheck step + the CI test run are flagged gaps; shellcheck was unavailable on the Windows dev host in Plan 00-06, so CI is where it runs)
    - 03-VALIDATION.md §Sampling Rate (per-wave merge = full pytest + bash -n + ruff/mypy/reuse; phase gate = full pytest green + shellcheck clean)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`. Add a `shellcheck` step to the `static-gates` job that lints `cc-worker-config/lxc/worker-template/burrow-boot.sh` and `provision-template.sh` — use the Ubuntu-runner-preinstalled `shellcheck` (a plain `run: shellcheck cc-worker-config/lxc/worker-template/burrow-boot.sh cc-worker-config/lxc/worker-template/provision-template.sh`; shellcheck ships on `ubuntu-latest`, so no third-party action is needed — if a directive is required for a known false positive use an inline `# shellcheck disable=` in the script per the common.sh precedent, NOT a CI flag). Add a `pytest` step after the existing `api · install (frozen)` step (working-directory: api): `uv run pytest tests/boot tests/integration/test_manifest_schema.py -q` so the boot-harness + manifest-drift gates run on every push/PR (CICD-02/03 — the test pyramid CI job the ci.yml header reserved for later, scoped here to the Phase-3 tiers). Keep `permissions: contents: read`. Any new third-party action (none expected — shellcheck is preinstalled, pytest runs via the already-pinned setup-uv) MUST be SHA-pinned with a trailing version comment. Do NOT remove or reorder the existing static gates.
  </action>
  <verify>
    <automated>grep -c "shellcheck" .github/workflows/ci.yml && grep -c "uv run pytest tests/boot" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` has a step running `shellcheck` on `burrow-boot.sh` (and `provision-template.sh`) (grep returns ≥1).
    - It has a step running `uv run pytest tests/boot tests/integration/test_manifest_schema.py` under `working-directory: api`.
    - The YAML parses: `cd api && uv run python -c "import yaml; yaml.safe_load(open('../.github/workflows/ci.yml'))"` exits 0 (or another available YAML parser).
    - No third-party action was added unpinned (each added `uses:` line ends with a `# v...` comment); existing pins are untouched.
    - `permissions: contents: read` is still present on the static-gates job.
  </acceptance_criteria>
  <done>CI lints the boot script with shellcheck and runs the boot + manifest test tiers, so a regression or manifest drift fails the build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → third-party actions | a malicious/typo'd action ref could run arbitrary code in CI |
| CI pytest/shellcheck → committed boot script + manifest | the gate that catches a regression before it reaches the homelab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | CI third-party actions | mitigate | this plan adds NO new third-party action (shellcheck is preinstalled on `ubuntu-latest`; pytest runs via the already-SHA-pinned setup-uv). The repo's SHA-pin convention is preserved; `permissions: contents: read` (least privilege, Pitfall 14) is unchanged. |
| T-03-10 | Tampering | manifest/boot-script drift shipping unchecked | mitigate | the new CI pytest + shellcheck steps fail the build on manifest drift (Pitfall 5) or a boot-script regression, so the dev-homelab smoke is not the first place a break is found. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | no package-manager install is added in this plan (docs + CI YAML only); the Plan-01 jsonschema dev dep is gated by the existing `uv lock --check` static gate. |
</threat_model>

<verification>
- `uvx reuse lint` green (ADR-0009 carries its SPDX header).
- `.github/workflows/ci.yml` parses as valid YAML and adds the shellcheck + pytest steps without unpinning any action.
- On the first CI run after merge, the shellcheck step is clean on `burrow-boot.sh` and the pytest step is green (RESEARCH notes shellcheck was unverified on the Windows dev host — CI is the authority).
</verification>

<success_criteria>
- The plugin-cadence decision is recorded (ADR-0009) so reproducibility semantics are explicit (SC-4).
- CI lints the boot script (shellcheck) and runs the boot + manifest tiers (drift + regression fail the build).
</success_criteria>

<output>
Create `.planning/phases/03-reproducible-workers/03-03-SUMMARY.md` when done.
</output>
