---
phase: 00-contracts-seams-golden-template
plan: 04
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/tsconfig.json
  - ui/biome.json
  - ui/src/placeholder.ts
  - .github/workflows/ci.yml
  - REUSE.toml
  - .gitignore
autonomous: true
requirements: [CICD-01, CICD-06]
must_haves:
  truths:
    - "The static-gates CI job runs ruff (lint+format), mypy strict, tsc --noEmit, biome (lint+format), reuse lint, and lockfile freshness, and fails on any violation."
    - "A minimal ui/ scaffold exists so tsc and biome have a real target to gate."
    - "reuse lint passes: every source file carries the SPDX header and non-headerable files are declared in REUSE.toml."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "static-gates job (CICD-01) + conventional-commit / PR-title validation"
      contains: "static-gates"
    - path: "REUSE.toml"
      provides: "license declaration for non-headerable files (lockfiles, etc.) so reuse lint passes (CICD-06)"
      contains: "AGPL-3.0-or-later"
    - path: "ui/biome.json"
      provides: "Biome 2.x config (written fresh, not ported from 1.x)"
      contains: "$schema"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "api/pyproject.toml + ui/package.json"
      via: "uv sync --frozen / npm ci against committed lockfiles"
      pattern: "working-directory"
    - from: "REUSE.toml"
      to: "uv.lock / package-lock.json"
      via: "blanket license for non-headerable lockfiles"
      pattern: "lock"
---

<objective>
Land the Tier-0 static CI gates (CICD-01) and the SPDX compliance machinery (CICD-06): the `.github/workflows/ci.yml` `static-gates` job that runs ruff (lint+format), mypy strict, `tsc --noEmit`, biome (lint+format), `reuse lint`, and lockfile freshness; a minimal `ui/` scaffold (Biome 2 + TS 6, one placeholder source) so the JS gates have a real target; and a `REUSE.toml` declaring license for non-headerable files so `reuse lint` is green repo-wide.

Purpose: Make every later phase CI-greenable on commit and enforce the SPDX header on every source file from day one.
Output: `ui/` scaffold (package.json + lockfile + tsconfig + biome.json + placeholder), `.github/workflows/ci.yml`, `REUSE.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md
@.planning/phases/00-contracts-seams-golden-template/00-CONTEXT.md
@docs/ci-cd-and-testing.md
@CONTRIBUTING.md

<interfaces>
<!-- Static-gates job step list (from RESEARCH §Static CI Gates + ci-cd §4.1). Each runs in api/ or ui/. -->
api/:  uv sync --frozen ; uv run ruff check . ; uv run ruff format --check . ; uv run mypy . --strict ; uv lock --check
ui/:   npm ci ; npx tsc --noEmit ; npx biome ci .
repo:  uvx reuse lint
commits: PR-title / conventional-commit validation (separate job or PR-title Action)

SPDX two-line header (CONTRIBUTING.md — authoritative):
  #-syntax  (.py .sh .toml .yaml/.yml): "# SPDX-FileCopyrightText: 2026 Brave Bear Studios" / "# SPDX-License-Identifier: AGPL-3.0-or-later"
  //-syntax (.ts .tsx .js):             "// SPDX-FileCopyrightText: 2026 Brave Bear Studios" / "// SPDX-License-Identifier: AGPL-3.0-or-later"
  <!-- -->  (.md where headerable):     block form
  non-headerable (lockfiles, *.json data): declared in REUSE.toml, NOT inline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: minimal ui/ scaffold (Biome 2 + TS 6 + placeholder)</name>
  <read_first>
    - .planning/research/STACK.md (Frontend Dev/Test pins: typescript 6.0.3, @biomejs/biome 2.4.16; Biome 2 schema written fresh) + §Headline (Biome 2 / Vite 8 / Tailwind v4 notes)
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Standard Stack (frontend static-gate tooling) + Pitfall 4 (Biome 1.x config ported to 2)
  </read_first>
  <files>ui/package.json, ui/package-lock.json, ui/tsconfig.json, ui/biome.json, ui/src/placeholder.ts, .gitignore</files>
  <action>
    Create a minimal `ui/` scaffold whose ONLY purpose is to give `tsc` and `biome` a real target (full UI runtime tree is Phase 2 — do NOT install React/Vite/xterm here). `ui/package.json`: name `burrow-ui`, private true, devDependencies pinned typescript@6.0.3 and @biomejs/biome@2.4.16. Run `npm install` to produce a committed `package-lock.json`, then confirm `npm ci` reproduces it. `ui/tsconfig.json`: strict, `noEmit` true, target/lib modern (ES2022+), `moduleResolution` bundler, include `src`. `ui/biome.json`: written FRESH against the Biome 2.x schema (set `$schema` to the installed 2.4.16 schema URL; do NOT port a 1.x config), enabling linter + formatter. `ui/src/placeholder.ts`: a trivially-typed export (e.g. `export const phase0Ready: boolean = true;`) carrying the `//` SPDX header so tsc/biome have content to gate. SPDX header: `//` on placeholder.ts; `#`-incompatible JSON files (package.json, tsconfig.json, biome.json) are non-headerable → covered by REUSE.toml in Task 3 (JSON cannot carry comments). Append `node_modules/` and `ui/dist/` to `.gitignore` if absent.
  </action>
  <acceptance_criteria>
    - `cd ui && npm ci` exits 0 against the committed `package-lock.json`.
    - `cd ui && npx tsc --noEmit` exits 0; `cd ui && npx biome ci .` exits 0.
    - `rg -n '"typescript": "6.0.3"' ui/package.json` and `rg -n '"@biomejs/biome": "2.4.16"' ui/package.json` match (exact pins).
    - `rg -n '"\$schema"' ui/biome.json` matches a 2.x schema URL.
    - `head -2 ui/src/placeholder.ts` shows the two `//` SPDX lines.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npm ci && npx tsc --noEmit && npx biome ci .</automated>
  </verify>
  <done>ui/ scaffold installs frozen; tsc --noEmit and biome ci both pass on the placeholder; Biome 2 config written fresh; pins exact; SPDX on the .ts source.</done>
</task>

<task type="auto">
  <name>Task 2: static-gates CI workflow + conventional-commit validation</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Static CI Gates (the static-gates job YAML + PR-title note)
    - docs/ci-cd-and-testing.md §3.2 (job DAG) + §4.1 (Tier-0 gate list)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml` triggered on pull_request and push to the default branch. Add a `static-gates` job (ubuntu-latest) with steps: checkout; install uv (astral-sh/setup-uv) and Node 22 (actions/setup-node, node-version '22'); then the api gates with `working-directory: api` — `uv sync --frozen`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy . --strict`, `uv lock --check`; then the ui gates with `working-directory: ui` — `npm ci`, `npx tsc --noEmit`, `npx biome ci .`; then the repo-wide SPDX gate `uvx reuse lint`. Pin third-party actions by commit SHA (record the version in a trailing comment) per ci-cd §7. Add Conventional-Commit validation as its own job: validate the PR title against Conventional-Commit grammar (a maintained PR-title-validation Action; squash-merge makes the PR title the commit) — pick the exact Action during implementation and SHA-pin it. Other ci.yml jobs (unit/integration/e2e/build) are out of scope this phase — either omit them or leave clearly-marked Phase-1 stubs that do not run. SPDX `#` header at the top of ci.yml.
  </action>
  <acceptance_criteria>
    - `rg -n 'static-gates' .github/workflows/ci.yml` matches.
    - The job contains all of: `ruff check`, `ruff format --check`, `mypy . --strict`, `uv lock --check`, `tsc --noEmit`, `biome ci`, `reuse lint` (`rg` each token).
    - Third-party `uses:` lines are SHA-pinned (`rg -n 'uses: .*@[0-9a-f]{40}' .github/workflows/ci.yml` matches; no `@v` floating tags on third-party actions).
    - A conventional-commit / PR-title validation job is present (`rg -ni 'pr.title|conventional|semantic' .github/workflows/ci.yml` matches).
    - `head -2 .github/workflows/ci.yml` shows the two SPDX `#` lines (after any leading `---` is not required for YAML; header on lines 1-2).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['static-gates']; steps=' '.join(str(s.get('run','')) for s in j['steps']); assert all(t in steps for t in ['ruff check','ruff format --check','mypy . --strict','uv lock --check','tsc --noEmit','biome ci','reuse lint']), steps; print('ok')"</automated>
  </verify>
  <done>static-gates job runs every Tier-0 gate and fails on violations; actions SHA-pinned; conventional-commit/PR-title validation present; SPDX-headered.</done>
</task>

<task type="auto">
  <name>Task 3: REUSE.toml + repo-wide reuse lint green</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §SPDX two-line header (per-syntax table + non-headerable files note) + §Static CI Gates
    - CONTRIBUTING.md §SPDX headers (exact header text per language)
  </read_first>
  <files>REUSE.toml, .gitignore</files>
  <action>
    Create `REUSE.toml` (REUSE 3.x format) at repo root declaring a blanket `SPDX-FileCopyrightText: 2026 Brave Bear Studios` + `SPDX-License-Identifier: AGPL-3.0-or-later` for non-headerable files that cannot carry a comment header: `**/uv.lock`, `**/package-lock.json`, and any committed `*.json` config that cannot hold comments (package.json, tsconfig.json, biome.json) — scope precisely; do NOT blanket-license source files (those carry inline headers). Then run `uvx reuse lint` at the repo root and resolve every finding by either adding the inline two-line header to the flagged source file (use the correct comment syntax per the §interfaces table) or declaring it in REUSE.toml if genuinely non-headerable. The end state: `uvx reuse lint` exits 0. SPDX `#` header on REUSE.toml itself.
  </action>
  <acceptance_criteria>
    - `REUSE.toml` exists and references `AGPL-3.0-or-later` and the lockfile/json globs (`rg -n 'package-lock.json|uv.lock' REUSE.toml` matches).
    - `uvx reuse lint` exits 0 at repo root (no files missing copyright/license info).
    - REUSE.toml does NOT blanket-license `**/*.py`/`**/*.ts`/`**/*.sh` (those must carry inline headers — `rg -n '\*\*/\*\.(py|ts|sh)' REUSE.toml` returns nothing).
    - SPDX header present on REUSE.toml.
  </acceptance_criteria>
  <verify>
    <automated>uvx reuse lint</automated>
  </verify>
  <done>reuse lint is green repo-wide; non-headerable files declared in REUSE.toml; source files keep inline headers; SPDX-headered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → ui/ install | Third-party package code enters the repo via `npm install` (typescript, @biomejs/biome) |
| GitHub Actions marketplace → CI | Third-party action code runs in CI with repo-scoped permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-SC | Tampering | npm installs in ui/ (typescript, @biomejs/biome) | mitigate | Both are spec-mandated, registry-verified ([VERIFIED: npm] per RESEARCH Package Legitimacy Audit — Microsoft/Biome official, 10+/2+ yrs); pin exact versions; `npm ci` from the committed lockfile only. No [ASSUMED]/[SUS] packages in this plan → no blocking-human checkpoint required. |
| T-00-CI | Tampering / Elevation | third-party GitHub Actions in ci.yml | mitigate | SHA-pin every third-party `uses:` (no floating `@v` tags); least-privilege `permissions:` on jobs (full per-job hardening is Phase 4); first-party `actions/*` and `astral-sh/setup-uv` pinned by SHA. |
| T-00-SECRET | Information Disclosure | CI logs / committed files | accept (v1) | No secrets handled in this plan; gitleaks as a CI secrets job is tracked for Phase 1/4 (ci-cd §5). Phase 0 introduces no secret-bearing surface here. |
</threat_model>

<verification>
- `cd api && uv sync --frozen && uv run ruff check . && uv run ruff format --check . && uv run mypy . --strict && uv lock --check` all pass.
- `cd ui && npm ci && npx tsc --noEmit && npx biome ci .` all pass.
- `uvx reuse lint` exits 0 at repo root.
- The `static-gates` job YAML contains every Tier-0 gate; third-party actions are SHA-pinned.
</verification>

<success_criteria>
- CICD-01: a single `static-gates` CI job runs ruff (lint+format), mypy strict, `tsc --noEmit`, biome (lint+format), reuse lint, and lockfile freshness, failing on any violation, plus conventional-commit/PR-title validation.
- CICD-06: `reuse lint` is green — every source file carries the SPDX two-line header and non-headerable files are declared in REUSE.toml.
- The minimal `ui/` scaffold gives the JS gates a real target without pulling the Phase-2 UI tree.
</success_criteria>

<output>
Create `.planning/phases/00-contracts-seams-golden-template/00-04-SUMMARY.md` when done.
</output>
