---
phase: 00-contracts-seams-golden-template
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/ADR-0001-sqlite-first.md
  - docs/adr/ADR-0002-boot-config-pull-at-boot.md
  - docs/adr/ADR-0003-proxmox-acl-scoping.md
  - docs/adr/ADR-0004-static-ip-from-vmid.md
  - docs/adr/ADR-0005-clone-mode-full.md
  - docs/adr/ADR-0006-ttyd-persistent-drop-once.md
  - docs/adr/ADR-0007-ttyd-lan-bind.md
  - docs/adr/ADR-0008-stack-version-bumps.md
autonomous: true
requirements: [WORK-04]
must_haves:
  truths:
    - "Every Phase-0 spec deviation (pull-at-boot, ACL scoping, static-IP-from-VMID, --full clone, ttyd persistent, ttyd LAN bind, stack bumps) is recorded as a Nygard-style ADR."
    - "ADR-0002 records the no-pct-over-API finding and locks pull-at-boot as the boot-config mechanism."
    - "ADR-0007 records the ttyd LAN-bind decision (WORK-04) with its security dimension."
  artifacts:
    - path: "docs/adr/ADR-0002-boot-config-pull-at-boot.md"
      provides: "the highest-priority ADR — pull-at-boot mechanism (SC-4/SC-5)"
      contains: "pull-at-boot"
    - path: "docs/adr/ADR-0007-ttyd-lan-bind.md"
      provides: "ttyd LAN bind decision (WORK-04 / SC-9)"
      contains: "WORK-04"
    - path: "docs/adr/ADR-0008-stack-version-bumps.md"
      provides: "consolidated stack-bump ADR (Vite 8, TS 6, Biome 2, etc.)"
      contains: "mypy"
  key_links:
    - from: "docs/adr/ADR-0002-boot-config-pull-at-boot.md"
      to: "PROXMOX-PRIMING.md §1 / SC-4"
      via: "cites the no-pct-over-API finding"
      pattern: "pct exec"
---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<!-- REUSE-IgnoreStart -->
<objective>
Author the eight Nygard-style ADRs that record every Phase-0 deviation from the tech-spec happy-path, so the frozen decisions (pull-at-boot, ACL scoping, static-IP-from-VMID, `--full` clone, persistent ttyd, ttyd LAN bind, the stack version bumps) are documented before merge as CONTRIBUTING.md requires. ADR-0007 (ttyd LAN bind) directly records WORK-04.

Purpose: Make the "implement the Spec Corrections, not the spec" decisions auditable and durable; these ADRs are the source of truth Phases 1/3 build their boot/clone/inject behavior on.
Output: `docs/adr/ADR-0001` through `ADR-0008`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md
@.planning/phases/00-contracts-seams-golden-template/00-CONTEXT.md
@.planning/research/PROXMOX-PRIMING.md
@.planning/research/STACK.md
@docs/tech-spec.md
@CONTRIBUTING.md

<interfaces>
<!-- ADR format (from tech-spec Appendix A / ADR-0001): Status / Context / Decision / Consequences / Revisit trigger.
     Every ADR file carries the <!-- --> SPDX block header (Markdown). Filename: docs/adr/ADR-NNNN-<slug>.md. -->

ADR list + core decision (RESEARCH §ADRs to Author):
- 0001 sqlite-first            : SQLite v1 store behind DbProvider; Postgres additive (spec Appendix A — author the file)
- 0002 boot-config pull-at-boot: pct exec/push NOT in HTTPS API (PROXMOX-PRIMING §1, SC-4); injectBootConfig = DB write; adds GET /api/v1/internal/bootconfig/{vmid} (Phase 1). HIGHEST PRIORITY.
- 0003 proxmox-acl-scoping     : /pool/burrow-workers + /storage/<rootfs> + /nodes/<node> (tight) vs /vms (broad); clone must add VMID to pool (PROXMOX-PRIMING §2.2)
- 0004 static-ip-from-vmid     : VMID->IP formula + off-host DHCP exclusion (SC-6, PROXMOX-PRIMING §4)
- 0005 clone-mode-full         : --full (CT-template clone defaults to linked); cheap on thin storage (PROXMOX-PRIMING §3.2)
- 0006 ttyd-persistent         : drop --once; detach != terminate; tab-close must not kill claude (SC-8)
- 0007 ttyd-lan-bind           : bind LAN interface not lo; resolves spec §9.3<->§6.4 (SC-9 / WORK-04); security dimension
- 0008 stack-version-bumps     : consolidated — Vite 8, TS 6, Biome 2, Vitest 4, @xterm 6, mypy 2, react-mosaic 6.2.0, Tailwind v4 (no tailwind.config.ts); evidence = STACK.md live reads
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0001 + ADR-0002 + ADR-0003 (data store + boot-config + ACL scoping)</name>
  <read_first>
    - docs/tech-spec.md Appendix A (ADR-0001 format + content) + §7 (data model)
    - .planning/research/PROXMOX-PRIMING.md §1 (no pct over API) + §2.2 (ACL scoping) + §10 (ADR candidates)
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §ADRs to Author
  </read_first>
  <files>docs/adr/ADR-0001-sqlite-first.md, docs/adr/ADR-0002-boot-config-pull-at-boot.md, docs/adr/ADR-0003-proxmox-acl-scoping.md</files>
  <action>
    Author three ADRs in the Nygard format (Status: Accepted / Context / Decision / Consequences / Revisit trigger), each with the `<!-- -->` SPDX block header. ADR-0001 (sqlite-first): SQLite via aiosqlite is the v1 store behind `DbProvider`; Postgres is an additive stub — lift the rationale from tech-spec Appendix A. ADR-0002 (boot-config pull-at-boot, HIGHEST PRIORITY): record that `pct exec`/`pct push` are NOT in the Proxmox HTTPS API (PROXMOX-PRIMING §1, SC-4), so `injectBootConfig` becomes a DB write and the worker fetches non-secret config + a short-lived git credential from `GET /api/v1/internal/bootconfig/{vmid}` at boot (endpoint impl = Phase 1; boot pull step = Phase 3); consequence: secrets stay off the worker env. ADR-0003 (proxmox-acl-scoping): decide `/pool/burrow-workers` + `/storage/<rootfs>` + `/nodes/<node>` (tight) over `/vms` (broad); consequence: the clone path must also add each new VMID to the pool (PROXMOX-PRIMING §2.2). Use placeholders only — no real hostnames/IPs/VMIDs.
  </action>
  <acceptance_criteria>
    - All three files exist with a `## Status`, `## Context`, `## Decision`, `## Consequences` section each (`rg -n '## Decision' docs/adr/ADR-0001-sqlite-first.md docs/adr/ADR-0002-boot-config-pull-at-boot.md docs/adr/ADR-0003-proxmox-acl-scoping.md` matches in all three).
    - `rg -n 'pct exec' docs/adr/ADR-0002-boot-config-pull-at-boot.md` and `rg -n 'bootconfig' docs/adr/ADR-0002-boot-config-pull-at-boot.md` match.
    - `rg -n 'burrow-workers' docs/adr/ADR-0003-proxmox-acl-scoping.md` matches.
    - Each file opens with the `<!-- -->` SPDX block (`rg -n 'SPDX-License-Identifier: AGPL-3.0-or-later' docs/adr/ADR-0001-sqlite-first.md` matches).
    - No real topology leaks (`rg -ni 'pve1\.local|192\.168|10\.0\.0' docs/adr/` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>uvx reuse lint docs/adr/ADR-0001-sqlite-first.md docs/adr/ADR-0002-boot-config-pull-at-boot.md docs/adr/ADR-0003-proxmox-acl-scoping.md</automated>
  </verify>
  <done>ADR-0001/0002/0003 authored in Nygard format with SPDX block headers; pull-at-boot and ACL-scoping decisions recorded; placeholders only.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0004 + ADR-0005 + ADR-0006 + ADR-0007 (static-IP, clone, ttyd persistent, ttyd LAN bind)</name>
  <read_first>
    - .planning/research/PROXMOX-PRIMING.md §3.2 (clone mode) + §4 (static-IP scheme)
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §ADRs to Author + §Security Domain (ttyd LAN bind security dimension)
    - docs/tech-spec.md §9.3 / §6.4 (the contradiction ADR-0007 resolves)
  </read_first>
  <files>docs/adr/ADR-0004-static-ip-from-vmid.md, docs/adr/ADR-0005-clone-mode-full.md, docs/adr/ADR-0006-ttyd-persistent-drop-once.md, docs/adr/ADR-0007-ttyd-lan-bind.md</files>
  <action>
    Author four ADRs (same Nygard format + `<!-- -->` SPDX block). ADR-0004 (static-ip-from-vmid): unprivileged LXC has no guest agent and DHCP discovery is unreliable, so Burrow computes the IP from the VMID (SC-6); record the VMID->IP formula intent (placeholders) and the off-host DHCP-exclusion obligation. ADR-0005 (clone-mode-full): CT-template clones default to linked; use `--full` for independent ephemeral workers whose destroy frees space, cheap on thin storage (budget clone time into the UPID wait). ADR-0006 (ttyd-persistent): drop `--once` — closing a browser tab DETACHES and must not terminate the live claude session; destroy is the only kill path (SC-8). ADR-0007 (ttyd-lan-bind): bind ttyd to the worker LAN interface, not `lo`, so the control-plane proxy can reach `:7681` — resolves the spec §9.3↔§6.4 contradiction (SC-9 / WORK-04); record the security dimension (accept the LAN boundary under the v1 LAN-only posture; never expose `:7681` beyond the LAN).
  </action>
  <acceptance_criteria>
    - All four files exist with `## Decision` + `## Consequences` (`rg -n '## Consequences' docs/adr/ADR-0004-static-ip-from-vmid.md docs/adr/ADR-0005-clone-mode-full.md docs/adr/ADR-0006-ttyd-persistent-drop-once.md docs/adr/ADR-0007-ttyd-lan-bind.md` matches in all four).
    - `rg -n 'WORK-04' docs/adr/ADR-0007-ttyd-lan-bind.md` matches; `rg -ni 'lan|interface' docs/adr/ADR-0007-ttyd-lan-bind.md` matches.
    - `rg -ni 'once' docs/adr/ADR-0006-ttyd-persistent-drop-once.md` matches; `rg -ni 'full' docs/adr/ADR-0005-clone-mode-full.md` matches.
    - Each opens with the `<!-- -->` SPDX block.
  </acceptance_criteria>
  <verify>
    <automated>uvx reuse lint docs/adr/ADR-0004-static-ip-from-vmid.md docs/adr/ADR-0005-clone-mode-full.md docs/adr/ADR-0006-ttyd-persistent-drop-once.md docs/adr/ADR-0007-ttyd-lan-bind.md</automated>
  </verify>
  <done>ADR-0004/0005/0006/0007 authored; static-IP, --full, persistent ttyd, and ttyd LAN bind (WORK-04) decisions recorded with security note; SPDX block headers.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-0008 consolidated stack-version-bumps</name>
  <read_first>
    - .planning/research/STACK.md §Spec-vs-reality deltas (the bump table + evidence) + §Headline Findings
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §ADRs to Author (0008 row)
  </read_first>
  <files>docs/adr/ADR-0008-stack-version-bumps.md</files>
  <action>
    Author ONE consolidated ADR recording the spec-`^`-range deviations with one shared rationale (spec ranges are a major version behind current stable) and one evidence base (STACK.md live registry reads 2026-06-09). Enumerate each bump and its pin: Vite 8, TypeScript 6, Biome 2 (config schema rewritten fresh), Vitest 4, @xterm 6 (scoped packages; legacy `xterm` deprecated), mypy 2, react-mosaic-component 6.2.0 (stable, NOT 7.0.0-beta0), Tailwind v4 via `@tailwindcss/vite` with NO `tailwind.config.ts`. Consequences: Biome/Tailwind config patterns change; the spec §4.1 `tailwind.config.ts` is dropped. Nygard format + `<!-- -->` SPDX block.
  </action>
  <acceptance_criteria>
    - `rg -n 'mypy' docs/adr/ADR-0008-stack-version-bumps.md` and `rg -ni 'biome' docs/adr/ADR-0008-stack-version-bumps.md` and `rg -ni 'react-mosaic' docs/adr/ADR-0008-stack-version-bumps.md` all match.
    - `rg -ni 'tailwind.config' docs/adr/ADR-0008-stack-version-bumps.md` matches (records its removal).
    - File has `## Decision` + `## Consequences` and the `<!-- -->` SPDX block.
  </acceptance_criteria>
  <verify>
    <automated>uvx reuse lint docs/adr/ADR-0008-stack-version-bumps.md</automated>
  </verify>
  <done>ADR-0008 consolidates all eight stack bumps with the STACK.md evidence base; records the tailwind.config.ts removal; SPDX block header.</done>
</task>

</tasks>

<verification>
- `uvx reuse lint docs/adr/` is green (all eight ADRs carry the SPDX block header).
- Each ADR uses the Nygard format (Status/Context/Decision/Consequences) and cites its source SC/PROXMOX-PRIMING/STACK section.
- No real hostnames/IPs/VMIDs appear (placeholders only).
</verification>

<success_criteria>
- All eight Phase-0 ADRs exist in `docs/adr/`, each recording one frozen deviation with rationale and revisit trigger.
- ADR-0002 locks pull-at-boot (the highest-priority decision Phase 1's `injectBootConfig` + bootconfig endpoint build on).
- ADR-0007 records the ttyd LAN-bind decision satisfying WORK-04's documentation half.
- The CONTRIBUTING.md "deviations need an ADR before merge" rule is satisfied for every Phase-0 deviation.
</success_criteria>

<output>
Create `.planning/phases/00-contracts-seams-golden-template/00-05-SUMMARY.md` when done.
</output>

<!-- REUSE-IgnoreEnd -->
