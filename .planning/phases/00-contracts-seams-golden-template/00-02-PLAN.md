---
phase: 00-contracts-seams-golden-template
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - api/compute/__init__.py
  - api/compute/provider.py
  - api/compute/fakeProvider.py
  - api/compute/proxmoxProvider.py
  - api/db/__init__.py
  - api/db/provider.py
  - api/db/sqliteProvider.py
  - api/db/postgresProvider.py
  - api/db/migrations/001_init.sql
autonomous: true
requirements: [PLAT-06, PLAT-07, PLAT-08]
must_haves:
  truths:
    - "ComputeProvider and DbProvider are abstract base classes that cannot be instantiated directly."
    - "FakeComputeProvider implements every ComputeProvider method in-memory and deterministically (same inputs -> same IP/task result)."
    - "SqliteProvider applies 001_init.sql to a temp DB and round-trips a workspace row through createWorkspace/getWorkspace."
    - "No proxmoxer/aiosqlite symbol or raw SQL appears outside its owning provider impl file."
  artifacts:
    - path: "api/compute/provider.py"
      provides: "ComputeProvider ABC + typed ComputeError hierarchy (PLAT-07, SC-13)"
      contains: "class ComputeProvider(ABC)"
    - path: "api/compute/fakeProvider.py"
      provides: "in-memory deterministic FakeComputeProvider (PLAT-08)"
      contains: "class FakeComputeProvider"
    - path: "api/db/provider.py"
      provides: "DbProvider ABC (PLAT-06)"
      contains: "class DbProvider(ABC)"
    - path: "api/db/sqliteProvider.py"
      provides: "aiosqlite impl + migration runner"
      contains: "aiosqlite"
    - path: "api/db/migrations/001_init.sql"
      provides: "tech-spec §7.1 schema (no partial-unique-index — that is Phase 1)"
      contains: "CREATE TABLE workspaces"
  key_links:
    - from: "api/compute/fakeProvider.py"
      to: "api/compute/provider.py"
      via: "class FakeComputeProvider(ComputeProvider)"
      pattern: "ComputeProvider"
    - from: "api/db/sqliteProvider.py"
      to: "api/db/migrations/001_init.sql"
      via: "reads + executes the migration SQL"
      pattern: "001_init"
---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<!-- REUSE-IgnoreStart -->
<objective>
Author the two load-bearing provider seams so ~80% of the backend is contract-stable before any real Proxmox call: the first-class `api/compute/` package (`ComputeProvider` ABC exposing every method the Phase-1 saga will call, an in-memory deterministic `FakeComputeProvider`, and a `ProxmoxComputeProvider` skeleton that raises `NotImplementedError`), and the `api/db/` seam (`DbProvider` ABC, `SqliteProvider` over `aiosqlite` with `001_init.sql`, and a `PostgresProvider` stub). Providers return Pydantic DTOs only — no driver types leak past the interface.

Purpose: Lock the ABC surfaces now (PLAT-06/07, SC-13) so the saga lands against a frozen contract and the integration/e2e tiers run hermetically over the Fake (PLAT-08).
Output: `api/compute/*`, `api/db/*`, `api/db/migrations/001_init.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md
@.planning/phases/00-contracts-seams-golden-template/00-CONTEXT.md
@api/models/compute.py
@api/models/workspace.py

<interfaces>
<!-- ComputeProvider ABC method set (api/compute/provider.py) — from RESEARCH §Provider Seam Interfaces.
     Every method async; returns DTOs from api/models/compute.py; raises ComputeError subclasses. -->
- getNextVmid(pool_start: int, pool_end: int, used: set[int]) -> int      # first free in-range; raise NoFreeVmidError if exhausted
- usedVmids() -> set[int]
- cloneCt(template_vmid: int, new_vmid: int, name: str, node: str, full: bool = True) -> ComputeTask   # --full (SC)
- injectBootConfig(vmid: int, config: BootConfig) -> None                 # DB-write-only seam; Fake no-ops (pull-at-boot, SC-4)
- startCt(node: str, vmid: int) -> ComputeTask
- stopCt(node: str, vmid: int) -> ComputeTask
- destroyCt(node: str, vmid: int) -> ComputeTask
- getStatus(node: str, vmid: int) -> ComputeStatus
- getIp(node: str, vmid: int) -> str | None                              # computed from VMID (SC-6), not polled
- getNodeMemory(node: str) -> float
- waitTask(node: str, upid: str, timeout: float) -> ComputeTask          # UPID blocking wait (SC-1); Fake returns OK immediately
- healthcheck() -> bool

Error hierarchy (api/compute/provider.py):
- ComputeError(Exception) -> NoFreeVmidError, CloneError, TaskFailedError, LxcNotReadyError

DbProvider ABC (api/db/provider.py) — tech-spec §6.3 + healthcheck (PLAT-03 forward-compat):
- createWorkspace(data: dict) -> Workspace
- getWorkspace(workspaceId: str) -> Workspace | None        # excludes soft-deleted
- listWorkspaces(status: str | None = None) -> list[Workspace]
- updateWorkspace(workspaceId: str, updates: dict) -> Workspace
- softDeleteWorkspace(workspaceId: str) -> None
- logEvent(workspaceId: str, eventType: str, data: dict) -> None
- healthcheck() -> bool
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComputeProvider ABC + typed errors + FakeComputeProvider (PLAT-07, PLAT-08)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Provider Seam Interfaces (ComputeProvider table + FakeComputeProvider behavior contract)
    - api/models/compute.py (ComputeTask/ComputeStatus/BootConfig DTOs)
  </read_first>
  <files>api/compute/__init__.py, api/compute/provider.py, api/compute/fakeProvider.py</files>
  <behavior>
    - ComputeProvider cannot be instantiated (TypeError on ComputeProvider()).
    - FakeComputeProvider().getIp for a given (node, vmid) returns the SAME value on repeated calls (deterministic, computed from vmid).
    - cloneCt records a stopped container; startCt -> getStatus reports running and getIp returns a non-None address; destroyCt frees the vmid (usedVmids no longer contains it).
    - getNextVmid skips ids in `used` and raises NoFreeVmidError when the range is exhausted.
    - injectBootConfig is a no-op that accepts a BootConfig and returns None.
    - waitTask returns a ComputeTask with status "ok" immediately (no sleep).
    - The constructor accepts an optional failure-injection config (e.g. raise on the Nth startCt/getIp) — shape it now so Phase 1 compensation tests don't refactor it.
  </behavior>
  <action>
    Create `api/compute/provider.py`: `ComputeProvider(ABC)` declaring every method in §interfaces as `@abstractmethod async def`, returning the DTOs from `api/models/compute.py`. Define the error hierarchy `ComputeError(Exception)` with subclasses `NoFreeVmidError`, `CloneError`, `TaskFailedError`, `LxcNotReadyError`. Create `api/compute/fakeProvider.py`: `FakeComputeProvider(ComputeProvider)` backed by a `dict[int, _FakeContainer]`; all behavior is a pure function of inputs (no `random`, no `asyncio.sleep`). Compute the fake IP deterministically from the VMID (e.g. `10.99.0.<vmid % 256>`) so getIp is reproducible. Mark containers stopped on clone, running on start, removed on destroy; getStatus returns a `ComputeStatus`; getNodeMemory returns a configurable in-memory value defaulting well under any threshold. Add an optional `failures` constructor arg for injectable failure hooks. `injectBootConfig` is a no-op. SPDX `#` header on all files. Unit test `tests/unit/test_fake_compute.py` is authored in Plan 03.
  </action>
  <acceptance_criteria>
    - `rg -n 'class ComputeProvider\(ABC\)' api/compute/provider.py` matches; every §interfaces method appears as `@abstractmethod`.
    - `rg -n 'class FakeComputeProvider\(ComputeProvider\)' api/compute/fakeProvider.py` matches.
    - `rg -n 'import random|asyncio.sleep' api/compute/fakeProvider.py` returns nothing (determinism, no sleeps).
    - `cd api && uv run python -c "import asyncio; from compute.provider import ComputeProvider; from compute.fakeProvider import FakeComputeProvider as F; \
import pytest\ntry:\n ComputeProvider()\n raise SystemExit('abstract failed')\nexcept TypeError: pass\nf=F()\nasync def m():\n await f.cloneCt(9000,210,'w','pve1'); await f.startCt('pve1',210); a=await f.getIp('pve1',210); b=await f.getIp('pve1',210); assert a==b and a is not None; print('ok')\nasyncio.run(m())"` prints ok.
    - SPDX header present on provider.py and fakeProvider.py.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy compute --strict && uv run python -c "from compute.provider import ComputeProvider, NoFreeVmidError, CloneError, TaskFailedError, LxcNotReadyError; from compute.fakeProvider import FakeComputeProvider; print('ok')"</automated>
  </verify>
  <done>ComputeProvider is abstract with the full saga method set; FakeComputeProvider is in-memory, deterministic, no sleeps, with injectable failures; mypy-strict clean; SPDX-headered.</done>
</task>

<task type="auto">
  <name>Task 2: ProxmoxComputeProvider skeleton (NotImplementedError only)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Anti-Patterns (no real proxmoxer calls this phase) + Pitfall 2
    - api/config.py (reads proxmox_ca_cert_path — never verify_ssl=False)
  </read_first>
  <files>api/compute/proxmoxProvider.py</files>
  <action>
    Create `api/compute/proxmoxProvider.py`: `ProxmoxComputeProvider(ComputeProvider)` whose `__init__(self, settings)` stores settings and (where it constructs a client at all) reads `settings.proxmox_ca_cert_path` for CA pinning — NEVER `verify_ssl=False`. Import `proxmoxer` at module top to prove the dep resolves, but EVERY method body is exactly `raise NotImplementedError("ProxmoxComputeProvider.<method> — Phase 1")`. This is a skeleton: no clone/start/wait logic, no respx/responses tests, no Proxmox host needed. Real bodies + mocked-Proxmox tests are Phase 1; real validation is the dev-homelab. SPDX `#` header.
  </action>
  <acceptance_criteria>
    - `rg -n 'class ProxmoxComputeProvider\(ComputeProvider\)' api/compute/proxmoxProvider.py` matches.
    - `rg -c 'raise NotImplementedError' api/compute/proxmoxProvider.py` returns the method count (every method stubbed).
    - `rg -n 'verify_ssl' api/compute/proxmoxProvider.py` returns nothing; `rg -n 'proxmox_ca_cert_path' api/compute/proxmoxProvider.py` matches.
    - `cd api && uv run python -c "from compute.proxmoxProvider import ProxmoxComputeProvider; print('ok')"` prints ok (import resolves proxmoxer).
    - SPDX header present.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy compute/proxmoxProvider.py --strict && uv run python -c "from compute.proxmoxProvider import ProxmoxComputeProvider as P; from config import settings; p=P(settings); import asyncio\ntry:\n asyncio.run(p.healthcheck())\n raise SystemExit('should raise')\nexcept NotImplementedError: print('ok')"</automated>
  </verify>
  <done>ProxmoxComputeProvider satisfies the ABC shape with NotImplementedError bodies; imports proxmoxer; reads the CA cert path; no real calls; mypy-strict clean; SPDX-headered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DbProvider ABC + 001_init.sql + SqliteProvider + Postgres stub (PLAT-06)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Provider Seam Interfaces (DbProvider table + Phase-1 forward-compat note: NO partial-unique-index in 001)
    - docs/tech-spec.md §6.3 (DbProvider ABC) + §7.1 (001_init.sql schema, verbatim)
    - api/models/workspace.py + api/models/event.py
  </read_first>
  <files>api/db/__init__.py, api/db/provider.py, api/db/sqliteProvider.py, api/db/postgresProvider.py, api/db/migrations/001_init.sql</files>
  <behavior>
    - DbProvider cannot be instantiated (abstract).
    - Applying 001_init.sql to a fresh temp SQLite file creates workspaces/events/templates and seeds templates('default', 9000).
    - SqliteProvider.createWorkspace(...) then getWorkspace(id) returns the same Workspace; listWorkspaces filters by status; softDeleteWorkspace excludes the row from getWorkspace/listWorkspaces.
    - logEvent appends an events row; healthcheck() returns True against an initialized DB.
  </behavior>
  <action>
    Create `api/db/migrations/001_init.sql` copying tech-spec §7.1 VERBATIM (workspaces, events, templates, the two indexes, and the `INSERT INTO templates ('default', 9000)` seed). Do NOT add the partial unique index `UNIQUE(vmid) WHERE deletedAt IS NULL` — that is a Phase-1 `002_*` migration (SC-4). Add the SPDX header to `001_init.sql` as the two-line `--` comment form on lines 1-2 EXACTLY: `-- SPDX-FileCopyrightText: 2026 Brave Bear Studios` then `-- SPDX-License-Identifier: AGPL-3.0-or-later` (`--` is the only valid SQL line comment; `#` is NOT valid SQL — do NOT use it). Do NOT fall back to a REUSE.toml entry for this file: it carries the inline header directly so `reuse lint` passes on the header, not a blanket glob. Create `api/db/provider.py` `DbProvider(ABC)` with the §interfaces method set (all `@abstractmethod async def`). Create `api/db/sqliteProvider.py` `SqliteProvider(DbProvider)` using `aiosqlite`: a migration runner that executes `001_init.sql` against `settings.database_path`, and all CRUD/log/healthcheck methods returning `Workspace`/`WorkspaceEvent` DTOs (map rows via the models, never leak `aiosqlite.Row`). Create `api/db/postgresProvider.py` `PostgresProvider(DbProvider)` with every method `raise NotImplementedError` (hosted-path stub; do NOT import asyncpg). `aiosqlite` and raw SQL appear ONLY in sqliteProvider.py and migrations/. SPDX header on every file. Unit test `tests/unit/test_db_provider.py` is authored in Plan 03.
  </action>
  <acceptance_criteria>
    - `rg -n 'class DbProvider\(ABC\)' api/db/provider.py` matches with all seven §interfaces methods as `@abstractmethod`.
    - `rg -n 'CREATE TABLE workspaces' api/db/migrations/001_init.sql` matches; `rg -n "INSERT INTO templates" api/db/migrations/001_init.sql` matches the default/9000 seed.
    - `rg -n 'UNIQUE\(vmid\)' api/db/migrations/001_init.sql` returns nothing (partial index deferred to Phase 1).
    - `head -2 api/db/migrations/001_init.sql` shows the two `--` SPDX lines (`-- SPDX-FileCopyrightText` then `-- SPDX-License-Identifier`); `rg -n '^# SPDX' api/db/migrations/001_init.sql` returns nothing (no invalid `#` SQL comment).
    - `rg -n 'aiosqlite' api/db/sqliteProvider.py` matches; `rg -n 'aiosqlite' api/db/postgresProvider.py api/db/provider.py` returns nothing (no driver leak).
    - `rg -c 'raise NotImplementedError' api/db/postgresProvider.py` returns the method count.
    - SPDX headers present on all five files.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy db --strict && uv run python -c "import asyncio,tempfile,os; from config import settings; from db.sqliteProvider import SqliteProvider\np=os.path.join(tempfile.mkdtemp(),'t.db'); settings.database_path=p; s=SqliteProvider(settings)\nasync def m():\n await s.migrate() if hasattr(s,'migrate') else None\n w=await s.createWorkspace({'name':'n','project_repo':'r','node':'pve1'}); g=await s.getWorkspace(w.id); assert g and g.id==w.id; assert await s.healthcheck(); print('ok')\nasyncio.run(m())"</automated>
  </verify>
  <done>DbProvider is abstract; 001_init.sql is the §7.1 schema (no partial index); SqliteProvider round-trips a workspace and passes healthcheck; Postgres stub raises NotImplementedError; no driver leak; mypy-strict clean; SPDX-headered.</done>
</task>

</tasks>

<verification>
- `cd api && uv run mypy . --strict` and `uv run ruff check .` pass on `compute/` and `db/`.
- Both ABCs are abstract (cannot instantiate); Fake + Sqlite satisfy them; Proxmox + Postgres stubs raise NotImplementedError.
- `001_init.sql` matches tech-spec §7.1 with no partial unique index.
- Seam leakage: `proxmoxer`/`ProxmoxAPI` only in proxmoxProvider.py; `aiosqlite`/raw SQL only in sqliteProvider.py + migrations/ (formally gated by the seam-leakage test in Plan 03).
- SPDX header on every authored file.
</verification>

<success_criteria>
- PLAT-07 / SC-13: `ComputeProvider` ABC lives in the first-class `api/compute/` package and exposes the complete saga method set.
- PLAT-08: `FakeComputeProvider` is in-memory, deterministic (same inputs -> same IP/task), with injectable failure hooks, no sleeps.
- PLAT-06: `DbProvider` ABC + `SqliteProvider` (aiosqlite + 001_init.sql) round-trip a workspace; Postgres stub behind the seam.
- No proxmoxer/aiosqlite type or raw SQL leaks past its owning impl file.
</success_criteria>

<output>
Create `.planning/phases/00-contracts-seams-golden-template/00-02-SUMMARY.md` when done.
</output>

<!-- REUSE-IgnoreEnd -->
