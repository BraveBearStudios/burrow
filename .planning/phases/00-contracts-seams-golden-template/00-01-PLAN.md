---
phase: 00-contracts-seams-golden-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/pyproject.toml
  - api/uv.lock
  - api/config.py
  - api/lib/__init__.py
  - api/lib/envelope.py
  - api/models/__init__.py
  - api/models/base.py
  - api/models/workspace.py
  - api/models/event.py
  - api/models/template.py
  - api/models/compute.py
  - .gitignore
autonomous: true
requirements: [PLAT-02, PLAT-09]
must_haves:
  truths:
    - "A response wrapped by the envelope helper has keys data, meta (with requestId + timestamp), and error."
    - "A Pydantic model accepts snake_case field names on input and serializes camelCase JSON on output."
    - "Settings load from .env using the exact keys committed in .env.example; BURROW_COMPUTE/BURROW_DB select providers."
  artifacts:
    - path: "api/pyproject.toml"
      provides: "uv project + pinned deps + ruff/mypy/pytest config"
      contains: "[tool.pytest.ini_options]"
    - path: "api/lib/envelope.py"
      provides: "respond()/respond_error() + Meta/ApiError models (PLAT-02)"
      min_lines: 25
    - path: "api/models/base.py"
      provides: "CamelModel base with alias_generator=to_camel (PLAT-09)"
      contains: "alias_generator"
    - path: "api/config.py"
      provides: "pydantic-settings Settings reading .env"
      contains: "BaseSettings"
  key_links:
    - from: "api/models/workspace.py"
      to: "api/models/base.py"
      via: "class Workspace(CamelModel)"
      pattern: "CamelModel"
    - from: "api/config.py"
      to: ".env.example keys"
      via: "field names match committed env contract"
      pattern: "proxmox_ca_cert_path"
---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<objective>
Lay the backend foundation every other Phase 0 backend plan builds on: the uv-managed `api/` project (pinned deps + ruff/mypy/pytest config), the `pydantic-settings` config that selects providers by env, the `data`/`meta`/`error` response-envelope helper (PLAT-02), and the camelCase Pydantic model layer (PLAT-09) — including the compute DTOs the `ComputeProvider` ABC will return.

Purpose: Get the load-bearing wire shapes (envelope, snake↔camel, settings keys) correct once, so the provider seams (Plan 02) and the saga (Phase 1) build against a stable, CI-verified contract.
Output: `api/pyproject.toml` + `api/uv.lock`, `api/config.py`, `api/lib/envelope.py`, `api/models/*` (base, workspace, event, template, compute DTOs), `.gitignore` updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md
@.planning/phases/00-contracts-seams-golden-template/00-CONTEXT.md
@.env.example
@CONTRIBUTING.md
@CLAUDE.md

<interfaces>
<!-- Wire shapes downstream plans (02, 03) and Phase 1 consume directly. -->

Envelope (api/lib/envelope.py — PLAT-02):
- respond(data, request_id: str | None = None) -> dict  ->  {"data": data, "meta": {"requestId","timestamp"}, "error": None}
- respond_error(code: str, message: str, request_id: str | None = None) -> dict  ->  {"data": None, "meta": {...}, "error": {"code","message"}}
- Meta(BaseModel): requestId: str ; timestamp: str (datetime.now(UTC).isoformat())
- ApiError(BaseModel): code: str ; message: str

CamelModel base (api/models/base.py — PLAT-09):
- ConfigDict(alias_generator=to_camel, populate_by_name=True, from_attributes=True)
- serialize at the boundary with model_dump(by_alias=True)

Compute DTOs (api/models/compute.py — consumed by ComputeProvider ABC in Plan 02):
- ComputeTask: { upid: str | None, status: Literal["ok","failed"], exitstatus: str | None }
- ComputeStatus: { status: str, uptime: int, mem: int, maxmem: int, cpu: float }
- BootConfig: { configRepo, configBranch, projectRepo, projectBranch }  (NON-secret only — pull-at-boot, SC-4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv project scaffold + pinned deps + tool config</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Standard Stack (pinned versions) + §Static CI Gates (tool config locations)
    - .planning/research/STACK.md (Backend Core + Dev/Test/Lint pin tables)
    - docs/ci-cd-and-testing.md §8 (pytest config lives in api/pyproject.toml)
    - .gitignore (current contents, before editing)
  </read_first>
  <files>api/pyproject.toml, api/uv.lock, .gitignore</files>
  <action>
    Create `api/pyproject.toml` as a uv-managed project named `burrow-api`, requires-python `==3.12.*`. Add runtime deps pinned exactly per STACK.md: fastapi==0.136.3, uvicorn[standard]==0.49.0, pydantic==2.13.4, pydantic-settings==2.14.1, aiosqlite==0.22.1, proxmoxer==2.3.0, httpx==0.28.1. Add a `[dependency-groups]` dev group (PEP 735) pinned: ruff==0.15.16, mypy==2.1.0, pytest==9.0.3, pytest-asyncio==1.4.0, reuse==6.2.0. Do NOT add websockets/respx/responses/playwright/pip-audit (those land Phase 1). Add `[tool.ruff]` (target py312, line length 100), `[tool.mypy]` with `strict = true`, and `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`. Run `uv lock` then `uv sync --frozen` to generate the committed lockfile. Add the SPDX two-line `#` header (lines 1-2) to `pyproject.toml`. Append to `.gitignore`: `api/.venv/`, `__pycache__/`, `*.egg-info/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/` (only entries not already present — read .gitignore first).
  </action>
  <acceptance_criteria>
    - `cd api && uv sync --frozen` exits 0; `api/uv.lock` exists and is tracked.
    - `rg -n 'asyncio_mode = "auto"' api/pyproject.toml` matches.
    - `rg -n 'strict = true' api/pyproject.toml` matches under `[tool.mypy]`.
    - `rg -n 'fastapi==0.136.3' api/pyproject.toml` matches (exact pin, no `^`/`>=`).
    - `head -2 api/pyproject.toml` shows the two SPDX `#` lines.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv sync --frozen && uv run ruff check . && uv lock --check</automated>
  </verify>
  <done>uv project installs from a frozen lockfile; ruff/mypy/pytest configured; SPDX header present; .gitignore covers build artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: response envelope helper + Meta/ApiError (PLAT-02)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Architecture Patterns Pattern 2 (envelope helper shape)
    - docs/tech-spec.md §5.1 (envelope contract: data/meta/error, requestId, timestamp)
  </read_first>
  <files>api/lib/__init__.py, api/lib/envelope.py</files>
  <behavior>
    - respond({"x": 1}) returns a dict whose keys are exactly {"data","meta","error"}, error is None, data == {"x":1}.
    - respond(...)["meta"] has keys "requestId" (str) and "timestamp" (str); passing request_id="abc" echoes requestId=="abc".
    - respond_error("E_CODE","msg") returns data is None and error == {"code":"E_CODE","message":"msg"}.
    - timestamp parses as an ISO-8601 UTC datetime.
  </behavior>
  <action>
    Create `api/lib/envelope.py` with `Meta(BaseModel)` (fields `requestId: str`, `timestamp: str`), `ApiError(BaseModel)` (fields `code: str`, `message: str`), a `make_meta(request_id: str | None = None) -> Meta` that defaults `requestId` to `str(uuid4())` and `timestamp` to `datetime.now(timezone.utc).isoformat()`, and the two boundary helpers `respond(data, request_id=None) -> dict` and `respond_error(code, message, request_id=None) -> dict` matching the §interfaces shapes. Keep this a pure boundary helper — no router/service logic. Create empty `api/lib/__init__.py`. SPDX `#` header on both files. The envelope unit test (`tests/unit/test_envelope.py`) is authored in Plan 03; this task only delivers the helper.
  </action>
  <acceptance_criteria>
    - `rg -n 'def respond\(' api/lib/envelope.py` and `rg -n 'def respond_error\(' api/lib/envelope.py` both match.
    - `rg -n 'requestId' api/lib/envelope.py` matches (camelCase meta keys).
    - `cd api && uv run python -c "from lib.envelope import respond, respond_error; r=respond({'x':1}); assert set(r)=={'data','meta','error'} and r['error'] is None and set(r['meta'])=={'requestId','timestamp'}; e=respond_error('C','m'); assert e['data'] is None and e['error']=={'code':'C','message':'m'}; print('ok')"` prints ok.
    - SPDX header present on `api/lib/envelope.py`.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy lib/envelope.py --strict && uv run python -c "from lib.envelope import respond; assert set(respond(None))=={'data','meta','error'}; print('ok')"</automated>
  </verify>
  <done>Envelope helper produces the exact {data, meta:{requestId,timestamp}, error} shape; mypy-strict clean; SPDX-headered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CamelModel base + Workspace/Event/Template/Compute models (PLAT-09)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Architecture Patterns Pattern 3 (CamelModel) + §Provider Seam Interfaces (Compute DTOs)
    - docs/tech-spec.md §7.1 (schema columns) + §6.3 (model usage)
  </read_first>
  <files>api/models/__init__.py, api/models/base.py, api/models/workspace.py, api/models/event.py, api/models/template.py, api/models/compute.py</files>
  <behavior>
    - A Workspace constructed with snake_case kwargs (lxc_ip, project_repo) round-trips to camelCase JSON (model_dump(by_alias=True) emits "lxcIp","projectRepo").
    - The same model also accepts camelCase input (populate_by_name) and validates.
    - WorkspaceStatus is a Literal with exactly creating|running|stopped|error|destroyed.
    - ComputeTask/ComputeStatus/BootConfig instantiate with the §interfaces fields.
  </behavior>
  <action>
    Create `api/models/base.py` `CamelModel(BaseModel)` with `model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True, from_attributes=True)` (import `to_camel` from `pydantic.alias_generators`). Create `workspace.py` with `WorkspaceStatus` (Literal of creating|running|stopped|error|destroyed), `Workspace(CamelModel)` (fields mirroring tech-spec §7.1: id, name, status, vmid: int|None, node, lxc_ip: str|None, project_repo, project_branch, plugin_set, created_at, stopped_at: str|None, destroyed_at: str|None, deleted_at: str|None), and `WorkspaceCreate(CamelModel)` (name, project_repo, project_branch="main", plugin_set="default", node). Create `event.py` `WorkspaceEvent(CamelModel)` (id, workspace_id, type, data: dict, created_at). Create `template.py` `Template(CamelModel)` (id, name, proxmox_tid: int, plugin_manifest: dict, created_at). Create `compute.py` with the three DTOs from §interfaces: `ComputeTask`, `ComputeStatus`, `BootConfig` (all `CamelModel`). Do NOT hand-map fields — rely on the alias generator. SPDX `#` header on every file. The round-trip unit test (`tests/unit/test_models.py`) is authored in Plan 03.
  </action>
  <acceptance_criteria>
    - `rg -n 'alias_generator=to_camel' api/models/base.py` matches.
    - Model classes inherit `CamelModel` (`rg -n 'class .*\(CamelModel\)' api/models` matches in workspace/event/template/compute).
    - `cd api && uv run python -c "from models.workspace import Workspace; w=Workspace(id='1',name='n',status='creating',vmid=None,node='pve1',lxc_ip=None,project_repo='r',project_branch='main',plugin_set='default',created_at='t',stopped_at=None,destroyed_at=None,deleted_at=None); d=w.model_dump(by_alias=True); assert 'lxcIp' in d and 'projectRepo' in d, d; print('ok')"` prints ok.
    - `cd api && uv run python -c "from models.compute import ComputeTask,ComputeStatus,BootConfig; print('ok')"` prints ok.
    - SPDX header present on all six files.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy models --strict && uv run python -c "from models.workspace import Workspace; assert 'lxcIp' in Workspace(id='1',name='n',status='creating',vmid=None,node='n',lxc_ip='x',project_repo='r',project_branch='main',plugin_set='default',created_at='t',stopped_at=None,destroyed_at=None,deleted_at=None).model_dump(by_alias=True); print('ok')"</automated>
  </verify>
  <done>All models inherit CamelModel and round-trip snake↔camel via the alias generator; compute DTOs match the ABC contract; mypy-strict clean; SPDX-headered.</done>
</task>

<task type="auto">
  <name>Task 4: pydantic-settings config + .env contract (provider switch)</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Code Examples (config.py) + §Runtime State Inventory (env key list)
    - .env.example (authoritative env key names — config fields MUST match these)
  </read_first>
  <files>api/config.py</files>
  <action>
    Create `api/config.py` with a `Settings(BaseSettings)` (`model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`). Fields: provider switches `compute: str = Field(default="fake", validation_alias="BURROW_COMPUTE")` (values fake|proxmox) and `db_kind: str = Field(default="sqlite", validation_alias="BURROW_DB")` — bind the BURROW_* env names via `Field(validation_alias=...)` (import `Field` from `pydantic`); pydantic-settings 2.14.x reads `validation_alias` from the environment, so `BURROW_COMPUTE=proxmox` in the env sets `settings.compute == "proxmox"`. Do NOT use a bare `compute: str = "fake"` (that binds to the lowercase `compute` env name, NOT `BURROW_COMPUTE`). Proxmox fields read but unused this phase, names matching .env.example exactly: proxmox_host, proxmox_user="burrow@pve", proxmox_token_name="burrow", proxmox_token_value="", proxmox_ca_cert_path="/etc/burrow/pve-ca.pem" (NEVER verify_ssl=False — read the CA path). LXC/worker: template_vmid: int=9000, worker_pool_start: int=200, worker_pool_end: int=299, default_node="pve1". DB: database_path="/data/burrow.db". config_repo, config_branch="main". Export a module-level `settings = Settings()`. SPDX `#` header.
  </action>
  <acceptance_criteria>
    - `rg -n 'proxmox_ca_cert_path' api/config.py` matches and `rg -n 'verify_ssl' api/config.py` returns nothing (never reintroduce verify_ssl=False).
    - `rg -n 'class Settings\(BaseSettings\)' api/config.py` matches.
    - Field names map to every non-commented key in `.env.example` (proxmox_host, proxmox_user, proxmox_token_name, proxmox_token_value, proxmox_ca_cert_path, config_repo, config_branch, template_vmid, worker_pool_start, worker_pool_end, default_node, database_path).
    - `cd api && uv run python -c "from config import settings; assert settings.compute in ('fake','proxmox') and settings.proxmox_user=='burrow@pve'; print('ok')"` prints ok.
    - The BURROW_COMPUTE alias binds from the environment (not just the default): `cd api && BURROW_COMPUTE=proxmox uv run python -c "from config import Settings; assert Settings().compute=='proxmox'; print('ok')"` prints ok.
    - SPDX header present.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy config.py --strict && uv run python -c "from config import settings; assert settings.db_kind=='sqlite'; print('ok')"</automated>
  </verify>
  <done>Settings load from .env with field names matching .env.example; BURROW_COMPUTE/BURROW_DB select providers; CA-cert path read, never verify_ssl=False; mypy-strict clean; SPDX-headered.</done>
</task>

</tasks>

<verification>
- `cd api && uv sync --frozen` installs from the committed lockfile; `uv lock --check` is clean.
- `cd api && uv run ruff check . && uv run ruff format --check . && uv run mypy . --strict` all pass on the authored files.
- Envelope produces `{data, meta:{requestId,timestamp}, error}`; models round-trip snake↔camel by alias; config reads the `.env.example` key set with no `verify_ssl=False`.
- Every authored source file carries the SPDX two-line `#` header (line 1-2 for `.py`/`.toml`).
</verification>

<success_criteria>
- The `api/` uv project installs frozen and the three tool configs (ruff, mypy strict, pytest asyncio) are present (CICD-01 substrate; gates wired in Plan 04).
- PLAT-02: the envelope helper returns the exact three-key shape with `requestId` + `timestamp` in `meta`.
- PLAT-09: a `CamelModel`-based model accepts snake_case and serializes camelCase via the alias generator (no hand-mapping).
- The compute DTOs (`ComputeTask`/`ComputeStatus`/`BootConfig`) exist for the `ComputeProvider` ABC in Plan 02.
- Config field names match `.env.example` and the provider-selection envs.
</success_criteria>

<output>
Create `.planning/phases/00-contracts-seams-golden-template/00-01-SUMMARY.md` when done.
</output>
