---
phase: 00-contracts-seams-golden-template
plan: 03
type: execute
wave: 3
depends_on: ["00-01", "00-02"]
files_modified:
  - api/main.py
  - api/tests/__init__.py
  - api/tests/conftest.py
  - api/tests/unit/__init__.py
  - api/tests/unit/test_envelope.py
  - api/tests/unit/test_models.py
  - api/tests/unit/test_fake_compute.py
  - api/tests/unit/test_db_provider.py
  - api/tests/unit/test_seam_leakage.py
autonomous: true
requirements: [PLAT-06, PLAT-07, PLAT-08, PLAT-02, PLAT-09]
must_haves:
  truths:
    - "main.py wires the compute and db providers from settings in exactly one place; BURROW_COMPUTE/BURROW_DB select the impl with no service edit."
    - "The unit suite (envelope, models, fake compute, db provider) runs green over the Fake/Sqlite providers with no Proxmox."
    - "The seam-leakage test fails if a forbidden driver symbol appears outside its owning provider file."
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI app factory + provider DI by env (the ONE place impls are named)"
      contains: "FastAPI"
    - path: "api/tests/conftest.py"
      provides: "Fake providers + ASGITransport client fixtures"
      contains: "FakeComputeProvider"
    - path: "api/tests/unit/test_seam_leakage.py"
      provides: "grep-based seam-leakage guard"
      contains: "proxmoxer"
  key_links:
    - from: "api/main.py"
      to: "api/config.py settings"
      via: "get_compute()/get_db() branch on settings.compute/db_kind"
      pattern: "settings.compute"
    - from: "api/tests/conftest.py"
      to: "api/main.py"
      via: "ASGITransport(app=create_app())"
      pattern: "ASGITransport"
---

<objective>
Wire the FastAPI app factory (the single place providers are selected by env — `BURROW_COMPUTE=fake|proxmox`, `BURROW_DB=sqlite`) and author the Phase-0 test substrate that makes the contracts CI-verifiable: `conftest.py` fixtures (Fake providers + `httpx.ASGITransport` client) plus the unit tests for the envelope (PLAT-02), camelCase models (PLAT-09), the Fake compute contract (PLAT-07/08), the SQLite provider (PLAT-06), and the seam-leakage guard.

Purpose: Close the Wave-0 test gaps from VALIDATION.md and prove the whole contract layer green over Fake/Sqlite with zero Proxmox — the "seams first" payoff.
Output: `api/main.py`, `api/tests/conftest.py`, `api/tests/unit/test_*.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md
@.planning/phases/00-contracts-seams-golden-template/00-VALIDATION.md
@api/config.py
@api/lib/envelope.py
@api/compute/provider.py
@api/compute/fakeProvider.py
@api/db/provider.py
@api/db/sqliteProvider.py

<interfaces>
<!-- App factory DI (api/main.py) — RESEARCH §Architecture Patterns Pattern 1. The ONLY place impls are named. -->
- create_app() -> FastAPI            # app factory; registers envelope middleware (routers are Phase 1)
- get_compute() -> ComputeProvider   # FakeComputeProvider() if settings.compute=="fake" else ProxmoxComputeProvider(settings)
- get_db() -> DbProvider             # SqliteProvider(settings) (postgres path additive, not wired in v1)

Seam-leakage table (test_seam_leakage.py asserts these — RESEARCH §Seam Leakage Audit):
- "proxmoxer" / "ProxmoxAPI"  allowed ONLY in api/compute/proxmoxProvider.py
- "aiosqlite" / raw SQL        allowed ONLY in api/db/sqliteProvider.py + api/db/migrations/
  red flag if found in: api/main.py, api/lib/, api/models/, (future) services/routers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI app factory + provider DI by env</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Architecture Patterns Pattern 1 (DI by env — the get_compute/get_db example)
    - api/config.py (settings.compute / settings.db_kind) + api/compute/* + api/db/* (provider constructors)
  </read_first>
  <files>api/main.py</files>
  <action>
    Create `api/main.py` with `create_app() -> FastAPI` (the app factory): construct the FastAPI app, register the response-envelope handling at the ASGI boundary (an exception handler that wraps raised errors into `respond_error(...)` shape, reusing `api/lib/envelope.py`; full success-wrapping middleware can be minimal here since routers land Phase 1 — do not author routers). Add `get_compute() -> ComputeProvider` returning `FakeComputeProvider()` when `settings.compute == "fake"` else `ProxmoxComputeProvider(settings)`, and `get_db() -> DbProvider` returning `SqliteProvider(settings)`. These two functions are the ONLY place concrete impls are named (FastAPI `Depends` targets for Phase 1). Do NOT import a concrete provider anywhere else. Expose `app = create_app()` at module level for uvicorn. SPDX `#` header.
  </action>
  <acceptance_criteria>
    - `rg -n 'def create_app\(' api/main.py` and `rg -n 'def get_compute\(' api/main.py` and `rg -n 'def get_db\(' api/main.py` all match.
    - `rg -n 'settings.compute' api/main.py` matches (env-driven selection).
    - `cd api && uv run python -c "from main import create_app, get_compute, get_db; from compute.provider import ComputeProvider; from db.provider import DbProvider; assert isinstance(get_compute(), ComputeProvider) and isinstance(get_db(), DbProvider); print('ok')"` prints ok.
    - SPDX header present.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run mypy main.py --strict && uv run python -c "from main import app; print(type(app).__name__)"</automated>
  </verify>
  <done>App factory selects providers from settings in one place; get_compute/get_db return the ABC-typed Fake/Sqlite impls; mypy-strict clean; SPDX-headered.</done>
</task>

<task type="auto">
  <name>Task 2: conftest fixtures + envelope/models/fake-compute/db unit tests</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-VALIDATION.md §Per-Task Verification Map + §Wave 0 Requirements
    - api/lib/envelope.py, api/models/workspace.py, api/compute/fakeProvider.py, api/db/sqliteProvider.py (what to assert)
  </read_first>
  <files>api/tests/__init__.py, api/tests/conftest.py, api/tests/unit/__init__.py, api/tests/unit/test_envelope.py, api/tests/unit/test_models.py, api/tests/unit/test_fake_compute.py, api/tests/unit/test_db_provider.py</files>
  <behavior>
    - test_envelope: respond/respond_error produce the exact {data,meta:{requestId,timestamp},error} shape; requestId echoes when passed; timestamp is ISO-8601 UTC.
    - test_models: Workspace round-trips snake_case input -> camelCase output (lxcIp, projectRepo); accepts camelCase input; WorkspaceStatus rejects an invalid status.
    - test_fake_compute: clone->start->getIp is deterministic and stable; destroyCt frees the vmid; getNextVmid skips used ids and raises NoFreeVmidError when exhausted; injectBootConfig is a no-op; waitTask returns status ok.
    - test_db_provider: 001_init.sql applies to a temp DB; create->get round-trips; listWorkspaces filters by status; softDelete hides the row; logEvent appends; healthcheck True.
  </behavior>
  <action>
    Create `api/tests/conftest.py` with pytest-asyncio fixtures: a `fake_compute` fixture (`FakeComputeProvider()`), a `sqlite_db` fixture that points `settings.database_path` at a `tmp_path` SQLite file and runs the migration, and an `client` fixture building `httpx.AsyncClient(transport=httpx.ASGITransport(app=create_app()))` for future router tests. Create `tests/__init__.py` and `tests/unit/__init__.py`. Author the four unit test files per the §behavior assertions, anchored to the VALIDATION.md verify map (file names MUST match: test_envelope.py, test_models.py, test_fake_compute.py, test_db_provider.py). Tests are hermetic — no network, no Proxmox, no sleeps. SPDX `#` header on every test file.
  </action>
  <acceptance_criteria>
    - All four test files exist at the exact paths in VALIDATION.md and each `cd api && uv run pytest tests/unit/<file> -x -q` passes.
    - `rg -n 'ASGITransport' api/tests/conftest.py` matches.
    - `rg -n 'def test_' api/tests/unit/test_fake_compute.py` shows >= 4 tests (determinism, vmid reuse, exhaustion, no-op inject).
    - SPDX header present on conftest.py and all four test files.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_envelope.py tests/unit/test_models.py tests/unit/test_fake_compute.py tests/unit/test_db_provider.py -x -q</automated>
  </verify>
  <done>conftest fixtures + four unit test files run green over Fake/Sqlite with no Proxmox; cover envelope (PLAT-02), models (PLAT-09), fake compute (PLAT-07/08), db (PLAT-06); SPDX-headered.</done>
</task>

<task type="auto">
  <name>Task 3: seam-leakage guard test</name>
  <read_first>
    - .planning/phases/00-contracts-seams-golden-template/00-RESEARCH.md §Seam Leakage Audit (the allowed/red-flag table)
    - api/compute/ + api/db/ (confirm which file legitimately holds each driver symbol)
  </read_first>
  <files>api/tests/unit/test_seam_leakage.py</files>
  <behavior>
    - The test scans api/ source (excluding tests/) and asserts "proxmoxer"/"ProxmoxAPI" appear ONLY in api/compute/proxmoxProvider.py.
    - It asserts "aiosqlite" and raw SQL (e.g. "CREATE TABLE", "INSERT INTO", "SELECT ") appear ONLY in api/db/sqliteProvider.py and api/db/migrations/.
    - The test FAILS (non-zero) if any forbidden symbol is found in main.py, lib/, models/, or any other module.
  </behavior>
  <action>
    Create `api/tests/unit/test_seam_leakage.py` that walks `api/` source files (skip `tests/`, `.venv/`, `__pycache__/`), and for each forbidden symbol asserts its only occurrences are within the allowed file(s) from the §interfaces seam-leakage table. Filter out comment-only lines where appropriate (use `grep -v '^#'`-equivalent logic in Python; never gate a count `== 0` on an unfiltered file). Provide a clear assertion message naming the offending file+symbol on failure. SPDX `#` header.
  </action>
  <acceptance_criteria>
    - `rg -n "proxmoxer" api/tests/unit/test_seam_leakage.py` matches (the symbol is referenced as a guarded token).
    - `cd api && uv run pytest tests/unit/test_seam_leakage.py -x -q` passes against the current tree.
    - The test's allowed-file lists name `api/compute/proxmoxProvider.py` and `api/db/sqliteProvider.py` (+ migrations) explicitly.
    - SPDX header present.
  </acceptance_criteria>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_seam_leakage.py -x -q</automated>
  </verify>
  <done>Seam-leakage test passes on the current tree and would fail if a driver symbol leaked past its owning provider file; SPDX-headered.</done>
</task>

</tasks>

<verification>
- `cd api && uv run pytest tests/unit -x -q` is fully green (envelope, models, fake compute, db, seam leakage).
- `cd api && uv run mypy . --strict && uv run ruff check . && uv run ruff format --check .` pass on `main.py` and `tests/`.
- `get_compute()`/`get_db()` return ABC-typed Fake/Sqlite impls selected from settings.
- SPDX header on every authored file.
</verification>

<success_criteria>
- The app factory is the single env-driven provider-selection point (swapping an impl is a one-env change, no service edit).
- The Wave-0 test gaps from VALIDATION.md are closed: all five `test_*.py` files exist at their mapped paths and pass.
- PLAT-02/09/06/07/08 each have a green automated unit test over Fake/Sqlite — zero Proxmox.
- The seam-leakage guard enforces the provider boundary in CI.
</success_criteria>

<output>
Create `.planning/phases/00-contracts-seams-golden-template/00-03-SUMMARY.md` when done.
</output>
