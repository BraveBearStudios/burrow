---
phase: 10-persistence-data-model-reaper-carve-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/tests/e2e/stop-start.spec.ts
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "The afterEach cleanup DELETE is asserted to succeed (200 deleted or 404 already-terminated) — no silent cleanup failure"
    - "After a stop, BOTH Start affordances (header icon + placeholder CTA) are asserted present"
    - "The suite stays order-independent (id-scoped cleanup + unique-per-run names)"
  artifacts:
    - path: "ui/tests/e2e/stop-start.spec.ts"
      provides: "07r-hardened stop/start e2e suite (W1 id tracking already present; W2 asserted DELETE; W3 two-affordance)"
      contains: "toHaveCount(2)"
  key_links:
    - from: "ui/tests/e2e/stop-start.spec.ts afterEach"
      to: "DELETE /api/v1/workspaces/{id}"
      via: "request.delete with an asserted status in [200, 404]"
      pattern: "expect\\(\\[200, 404\\]\\)\\.toContain"
    - from: "ui/tests/e2e/stop-start.spec.ts round-trip"
      to: "TerminalPanel Start affordances"
      via: "getByRole button name Start workspace, count 2"
      pattern: "Start workspace"
---

<objective>
Harden the stop/start e2e cleanup (TEST-02 / 07r). The suite already has W1 (per-test
`createdIds` tracking) and an `afterEach` DELETE; close the two narrow gaps: W2 — assert the
cleanup DELETE succeeds (so a silently-failing backend cleanup can never leak Fake state and
cause a flaky order-dependent failure far from the root cause); W3 — after a Stop, assert
BOTH Start affordances are present (the header icon button + the placeholder CTA inside the
`role="status"` "Workspace stopped" region).

Purpose: Bundled here as test-hardening (no better home; it touches the same stop/start e2e
suite this phase's persistence work conceptually exercises). It is independent of the
api/persistence changes (touches only `ui/tests/e2e/stop-start.spec.ts`) so it runs in Wave 1
in parallel with the mock-tier gate.

Output: an edited `ui/tests/e2e/stop-start.spec.ts` with the W2 asserted-DELETE and W3
two-affordance assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-persistence-data-model-reaper-carve-out/10-RESEARCH.md
@.planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The two Start affordances, verified in ui/src/components/TerminalPanel.tsx (cited RESEARCH/PATTERNS). -->
<!-- Both render aria-label="Start workspace". -->
- Header Start button:    TerminalPanel.tsx:373-384 (aria-label="Start workspace")
- Placeholder CTA button: TerminalPanel.tsx:461-476, inside the role="status" "Workspace stopped" region (aria-label="Start workspace")

<!-- Current suite anchors (verified in ui/tests/e2e/stop-start.spec.ts). -->
- afterEach (fire-and-forget DELETE today): stop-start.spec.ts:86-94
- round-trip test: stop-start.spec.ts:96+ ; "Workspace stopped" visible at :122-124
- placeholder region locator already used at :136-138:
    panel.getByRole("status").filter({ hasText: "Workspace stopped" })
- names are unique-per-run via `stamp` (:23-28) → suite is already order-independent (W1)

<!-- Strict-mode note: a bare two-match `Start workspace` click is rejected; the existing -->
<!-- code scopes the CLICK to the placeholder region. Keep that scoping. The W3 addition is -->
<!-- a COUNT assertion (toHaveCount(2)) + a placeholder-scoped visibility assertion. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: W2 — assert the cleanup DELETE succeeds in afterEach</name>
  <files>ui/tests/e2e/stop-start.spec.ts</files>
  <read_first>
    - ui/tests/e2e/stop-start.spec.ts:86-94 (the current fire-and-forget `afterEach` DELETE loop over `createdIds`)
    - .planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md (section "ui/tests/e2e/stop-start.spec.ts" — W2 snippet, lines 367-378)
  </read_first>
  <action>
    Edit the `afterEach` in `ui/tests/e2e/stop-start.spec.ts` (currently :86-94). Capture the
    `request.delete(`/api/v1/workspaces/${id}`)` response into `res` and assert its status with
    `expect([200, 404]).toContain(res.status())` — a 200 (deleted) or a 404 (the row was already
    terminated through the UI) is acceptable; anything else fails the test (W2: no silent cleanup
    failure). Keep the loop id-scoped over `createdIds` (never a broad DB wipe — that would race
    sibling tests, RESEARCH Pitfall 6). Do not change the W1 id-tracking already present.
  </action>
  <verify>
    <automated>cd ui && rtk lint biome ; cd ui && npx playwright test stop-start --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - the `afterEach` captures the DELETE response and asserts `expect([200, 404]).toContain(res.status())`
    - cleanup remains id-scoped over `createdIds` (no global/broad wipe introduced)
    - `npx playwright test stop-start` passes
    - biome lint clean on the file
  </acceptance_criteria>
  <done>The cleanup DELETE is asserted (200 or 404); a non-cleanup status now fails the test. The e2e suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: W3 — assert both Start affordances after a stop</name>
  <files>ui/tests/e2e/stop-start.spec.ts</files>
  <read_first>
    - ui/tests/e2e/stop-start.spec.ts:122-149 (the "Workspace stopped" visibility assertion at :122-124 and the placeholder-scoped Start click at :136-148)
    - ui/src/components/TerminalPanel.tsx:373-384 (header Start button) and :461-476 (placeholder CTA) — both aria-label "Start workspace"
    - .planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md (section "ui/tests/e2e/stop-start.spec.ts" — W3 snippet, lines 381-398)
  </read_first>
  <action>
    In the round-trip test, AFTER "Workspace stopped" is visible (:122-124) and BEFORE the
    placeholder Start click (:136-148), add the two-affordance assertion: locate
    `panel.getByRole("button", { name: "Start workspace" })` and assert
    `await expect(startButtons).toHaveCount(2)` (header `TerminalPanel.tsx:373-384` + placeholder
    CTA `:461-476`); then assert the placeholder CTA specifically with
    `panel.getByRole("status").filter({ hasText: "Workspace stopped" })` ->
    `.getByRole("button", { name: "Start workspace" })` -> `toBeVisible()`. Keep the existing
    placeholder-scoped CLICK exactly as-is (strict mode rejects a bare two-match click — the
    scoping is why the existing click targets the placeholder region; the count assertion is the
    only addition to that flow). Do not loosen any existing panel-scoped locator.
  </action>
  <verify>
    <automated>cd ui && rtk lint biome ; cd ui && npx playwright test stop-start --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - the round-trip test asserts `toHaveCount(2)` over `getByRole("button", { name: "Start workspace" })` after stop
    - the placeholder CTA Start button is asserted visible inside the `role="status"` "Workspace stopped" region
    - the existing placeholder-scoped Start CLICK is unchanged (still order-independent)
    - `npx playwright test stop-start` passes
    - biome lint clean on the file
  </acceptance_criteria>
  <done>After a stop, both Start affordances are asserted present (count == 2 + the placeholder CTA visible); the round-trip click still resolves unambiguously and the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright client -> control-plane API | The e2e cleanup DELETE crosses into the real API (Fake compute); a silently-failing cleanup leaks Fake state across tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02A | Denial of Service | leaked Fake state from a swallowed cleanup DELETE | mitigate | W2 asserts the DELETE status in [200, 404]; a non-cleanup status fails the test, surfacing the leak at the root cause instead of as a downstream flaky order-dependent failure (RESEARCH Pitfall 6). |
| T-10-02B | Tampering | a broad DB wipe between tests racing sibling tests | accept | Cleanup stays id-scoped over `createdIds`; no global wipe introduced (RESEARCH Don't-Hand-Roll: e2e per-test isolation). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — `@playwright/test` is already a committed `ui` dependency; RESEARCH Package Legitimacy Audit is N/A. |
</threat_model>

<verification>
- `npx playwright test stop-start` passes (the full stop/start suite, order-independent).
- `grep -n "toHaveCount(2)" ui/tests/e2e/stop-start.spec.ts` returns the W3 assertion.
- `grep -n "expect(\[200, 404\])" ui/tests/e2e/stop-start.spec.ts` returns the W2 assertion.
- `rtk lint biome` clean on `ui/tests/e2e/stop-start.spec.ts`.
</verification>

<success_criteria>
- The stop/start e2e cleanup is hardened (07r): per-test workspace-id tracking (already W1),
  an asserted cleanup `DELETE` success (W2), and an explicit two-Start-affordance assertion
  (W3); the suite is order-independent (SC5 / TEST-02).
</success_criteria>

<output>
Create `.planning/phases/10-persistence-data-model-reaper-carve-out/10-02-SUMMARY.md` when done
</output>
