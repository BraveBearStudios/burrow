---
phase: 01-control-plane-api
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - api/lib/statemachine.py
  - api/lib/errors.py
  - api/services/__init__.py
  - api/services/workspaceService.py
  - api/tests/unit/test_create_saga.py
  - api/tests/unit/test_compensation.py
  - api/tests/unit/test_state_machine.py
  - api/tests/unit/test_capacity_guard.py
autonomous: true
requirements: [WS-01, WS-02, WS-03, WS-06, WS-07, WS-08, WS-09, CAP-01, CAP-04, CICD-03]
user_setup: []

must_haves:
  truths:
    - "createWorkspace runs the 8-step saga over the Fake and reaches running (WS-02)"
    - "A forced failure at any post-reservation step compensates (stop+destroy idempotent), frees the VMID, and lands the row in error — never stuck creating, no orphan (WS-03)"
    - "The state machine rejects illegal transitions (stop-on-creating, start-on-destroyed, double-destroy) with a typed error (WS-09)"
    - "Create is refused when node memory exceeds the 0.80 threshold (CAP-01)"
    - "VMID reservation retries on VmidTakenError and the DB partial-index INSERT is the arbiter (WS-10 policy)"
    - "stop/start/destroy each enforce a transition guard, block on the UPID via the provider, and log an event"
  artifacts:
    - path: "api/services/workspaceService.py"
      provides: "create/stop/start/destroy saga + compensation + capacity + VMID policy + event logging"
      min_lines: 120
    - path: "api/lib/statemachine.py"
      provides: "TRANSITIONS table + assert_transition guard (SC-12)"
      contains: "TRANSITIONS"
    - path: "api/lib/errors.py"
      provides: "Service-tier typed errors (CapacityError, IllegalTransitionError, NoFreeVmidError-equiv, WorkspaceBootError)"
      contains: "CapacityError"
    - path: "api/tests/unit/test_compensation.py"
      provides: "Per-step FakeFailures compensation proof (WS-03)"
      min_lines: 50
  key_links:
    - from: "api/services/workspaceService.py"
      to: "db.createWorkspace (partial-unique INSERT)"
      via: "reserve VMID by INSERT; catch VmidTakenError → rescan + retry"
      pattern: "VmidTakenError"
    - from: "api/services/workspaceService.py"
      to: "compute.cloneCt/startCt/getIp + _wait_ttyd"
      via: "8-step saga, each UPID blocked inside the provider"
      pattern: "cloneCt"
    - from: "api/services/workspaceService.py"
      to: "lib.statemachine.assert_transition"
      via: "stop/start/destroy guard before mutating"
      pattern: "assert_transition"
---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<objective>
Build the orchestration core: the `WorkspaceService` create→stop→start→destroy saga with a server-enforced state machine, race-safe VMID reservation policy, capacity guard, node selection, per-step compensation, and event logging — depending ONLY on the two provider ABCs. This is the MVP heart of the phase: after this plan, a create→running→stop→start→destroy slice runs end-to-end over the FakeComputeProvider with zero Proxmox, fully unit-tested.

Purpose: WS-01/02/03/06/07/08/09, CAP-01/04. The genuinely novel logic of the phase is the saga orchestration and the compensation tree (RESEARCH: "everything else is wiring"). Implement the SC-corrected saga (SC-1/2/11/12), NOT the tech-spec §6.2 pseudocode (clone-before-row, sync-treated, cloud-init — all wrong).
Output: `services/workspaceService.py`, `lib/statemachine.py`, `lib/errors.py`, and four unit test files proving the saga, compensation, state machine, and capacity guard over the Fake.

Depends on Plan 01 (DB `VmidTakenError`, the partial-index reservation, `getByVmid`, `getEvents`, and the consolidated `Settings` keys). Runs over the Fake provider — Plan 02's real provider is not required here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-control-plane-api/01-CONTEXT.md
@.planning/phases/01-control-plane-api/01-RESEARCH.md
@.planning/phases/01-control-plane-api/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The ABCs the service depends on (NEVER a concrete impl — CLAUDE.md seam rule). -->

From api/compute/provider.py (ComputeProvider ABC — full method set):
```python
async def getNextVmid(self, pool_start, pool_end, used: set[int]) -> int   # PURE scan, NOT a reservation
async def usedVmids(self) -> set[int]
async def cloneCt(self, template_vmid, new_vmid, name, node, full=True) -> ComputeTask
async def injectBootConfig(self, vmid, config: BootConfig) -> None          # DB-write seam (pull-at-boot)
async def startCt(self, node, vmid) -> ComputeTask
async def stopCt(self, node, vmid) -> ComputeTask
async def destroyCt(self, node, vmid) -> ComputeTask                        # idempotent (no-op on missing)
async def getIp(self, node, vmid) -> str | None                            # computed from VMID
async def getNodeMemory(self, node) -> float                               # used-memory fraction
async def waitTask(self, node, upid, timeout) -> ComputeTask
async def healthcheck(self) -> bool
# errors: ComputeError, NoFreeVmidError, CloneError, TaskFailedError, LxcNotReadyError
```

From api/db/provider.py (DbProvider ABC, extended by Plan 01):
```python
async def createWorkspace(self, data: dict[str, Any]) -> Workspace   # raises VmidTakenError on dup active vmid
async def getWorkspace(self, workspaceId) -> Workspace | None
async def listWorkspaces(self, status=None) -> list[Workspace]
async def updateWorkspace(self, workspaceId, updates: dict[str, Any]) -> Workspace
async def softDeleteWorkspace(self, workspaceId) -> None
async def getByVmid(self, vmid: int) -> Workspace | None
async def getEvents(self, workspaceId) -> list[WorkspaceEvent]
async def logEvent(self, workspaceId, eventType, data: dict[str, Any]) -> None
async def healthcheck(self) -> bool
# VmidTakenError is declared on db.provider (import it from there)
```

From api/models/workspace.py: WorkspaceCreate(name, project_repo, project_branch="main", plugin_set="default", node); Workspace(id, name, status, vmid, node, lxc_ip, project_repo, project_branch, plugin_set, created_at, stopped_at, destroyed_at, deleted_at). Status: creating|running|stopped|error|destroyed.
From api/models/compute.py: BootConfig(config_repo, config_branch, project_repo, project_branch).
From api/config.py Settings (ALL keys already added by Plan 01 — READ them, do NOT add to config.py): template_vmid, worker_pool_start(200), worker_pool_end(299), default_node, config_repo, config_branch, capacity_threshold(0.80), ttyd_timeout(60), ttyd_interval(2), clone_timeout, task_timeout.

From api/compute/fakeProvider.py (the test driver): FakeComputeProvider(failures: FakeFailures|None, node_memory: float). FakeFailures(raise_on_nth_call={"cloneCt":1,...}) makes a named method raise on its Nth call (CloneError for cloneCt, LxcNotReadyError otherwise). getIp returns 10.99.0.<vmid%256> when running.
</interfaces>

<execution_note>
The saga's ttyd-health step (step 6) does an httpx GET against the resolved IP. Over the FakeComputeProvider the IP is `10.99.0.<vmid%256>` (no real ttyd). For the UNIT tier, make `_wait_ttyd` injectable / monkeypatchable so the saga unit tests can stub it to succeed instantly (the real httpx poll + stub-ttyd is exercised in Plan 04's integration tier). Do not hit the network in unit tests.

config.py is owned by Plan 01 (single-owner). This plan only READS Settings keys; it must NOT edit config.py.
</execution_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State machine transition table + service-tier typed errors</name>
  <read_first>
    - 01-RESEARCH.md Pattern 4 (the exact TRANSITIONS table + assert_transition; error state's only exit is destroy; in-flight lock note) and Assumption A4 (error→destroy only)
    - api/lib/envelope.py (respond_error code/message shape — the router maps these errors to codes in Plan 04)
    - api/tests/unit/test_state_machine.py target rows in 01-VALIDATION.md (stop-on-creating, start-on-destroyed, double-destroy)
  </read_first>
  <behavior>
    - assert_transition("running","stop") == "stopped"; ("stopped","start") == "running"; ("running","destroy")=="destroyed"; ("stopped","destroy")=="destroyed"; ("error","destroy")=="destroyed".
    - assert_transition("creating","stop") raises IllegalTransitionError; ("destroyed","start") raises; ("destroyed","destroy") raises (double-destroy); ("running","start") raises.
  </behavior>
  <files>api/lib/statemachine.py, api/lib/errors.py, api/tests/unit/test_state_machine.py</files>
  <action>
    Create `lib/statemachine.py` with the `TRANSITIONS: dict[tuple[str,str], str]` table from RESEARCH Pattern 4 and `assert_transition(state, action) -> str` raising `IllegalTransitionError(state, action)` when the pair is absent. `creating` is internal-only (reached by create, never an action target); `error`'s only legal exit is `destroy` (Assumption A4 — pin it). Create `lib/errors.py` with the service-tier typed exceptions the routers will map to envelope codes: `IllegalTransitionError`, `CapacityError`, `NoFreeVmidError` (service-level — distinct from the compute one, or re-export the compute one; pick one and document), `WorkspaceBootError`, `WorkspaceNotFoundError`. Each carries a stable `.code` string (e.g. `illegal_transition`, `capacity_exceeded`, `no_free_vmid`, `boot_failed`, `not_found`) so Plan 04's router maps `error.code` deterministically. Write `test_state_machine.py` covering the behavior cases above (WS-09).
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_state_machine.py -x -q && uv run mypy . --strict</automated>
  </verify>
  <acceptance_criteria>
    - `lib/statemachine.py` exports `TRANSITIONS` and `assert_transition`; the five legal transitions resolve and the four illegal ones raise `IllegalTransitionError`.
    - `lib/errors.py` exports the five typed errors, each with a stable `.code`.
    - `test_state_machine.py` passes all WS-09 cases (stop-on-creating, start-on-destroyed, double-destroy, running→start).
    - `mypy . --strict` clean; no driver imports in either lib file (seam guard green).
  </acceptance_criteria>
  <done>The transition table and service error types exist and are unit-proven for WS-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkspaceService — create saga, VMID reservation, capacity guard, compensation</name>
  <read_first>
    - 01-RESEARCH.md Pattern 2 (the 8-step saga table + compensation table + the createWorkspace skeleton), Pattern 3 (the _reserve_vmid_and_row retry loop), the ttyd-health code example, Pitfalls 1/3/4/7 (UPID race, VMID race/tombstone, saga-lands-in-creating, secrets in logs)
    - api/compute/provider.py + api/db/provider.py (the ABCs — depend ONLY on these)
    - api/config.py (READ-ONLY: the Settings keys Plan 01 already added — capacity_threshold, ttyd_timeout, ttyd_interval, clone_timeout, task_timeout, template_vmid, pool range, default_node, config_repo/branch)
    - api/compute/fakeProvider.py FakeFailures (drives the compensation tests)
  </read_first>
  <behavior>
    - createWorkspace(payload) over a clean Fake reaches status "running"; the workspace has a vmid in the pool range and a resolved lxc_ip; a workspace.created event is logged.
    - getNodeMemory > capacity_threshold (0.80) → CapacityError, no row created (CAP-01). The operator-selected payload.node is the node passed to getNodeMemory (CAP-04).
    - A VmidTakenError on the reservation INSERT triggers a rescan+retry (bounded attempts); exhausting the pool raises NoFreeVmidError.
    - With FakeFailures(raise_on_nth_call={step}) at clone / start / getIp / ttyd-health: the saga compensates (stop+destroy idempotent), frees the VMID, logs a redacted boot.error event, lands the row in "error" (never "creating"), and the Fake has zero leftover containers (no orphan).
  </behavior>
  <files>api/services/__init__.py, api/services/workspaceService.py, api/tests/unit/test_create_saga.py, api/tests/unit/test_compensation.py, api/tests/unit/test_capacity_guard.py</files>
  <action>
    Create `services/__init__.py` (SPDX) and `services/workspaceService.py` with `class WorkspaceService` taking `compute: ComputeProvider`, `db: DbProvider`, `settings` (constructor injection; depends only on the ABCs — no `aiosqlite`/`proxmoxer` import; seam guard enforces this).

    Implement `createWorkspace(payload: WorkspaceCreate) -> Workspace` exactly as RESEARCH Pattern 2's 8-step saga: (0) capacity guard `if await compute.getNodeMemory(payload.node) > settings.capacity_threshold: raise CapacityError(payload.node)`; (1) reserve via `_reserve_vmid_and_row` (RESEARCH Pattern 3: union `compute.usedVmids()` with DB-used vmids, `getNextVmid`, INSERT `status="creating"` + vmid; on `VmidTakenError` rescan+retry up to N; exhaustion → `NoFreeVmidError`); (2) `cloneCt(settings.template_vmid, vmid, payload.name, payload.node)` (UPID blocks inside the provider); (3) `injectBootConfig(vmid, BootConfig(config_repo=settings.config_repo, config_branch=settings.config_branch, project_repo=payload.project_repo, project_branch=payload.project_branch))`; (4) `startCt(payload.node, vmid)`; (5) `ip = getIp(payload.node, vmid)` then `updateWorkspace(id, {"lxc_ip": ip})`; (6) `await self._wait_ttyd(ip)` (injectable; default httpx poll per the RESEARCH ttyd example, ttyd_timeout/ttyd_interval from Settings — monkeypatchable for unit tests); (7) `logEvent(id, "workspace.created", {})` then `updateWorkspace(id, {"status":"running"})`.

    On ANY exception after step 1: `await self._compensate(payload.node, vmid)` (idempotent stop-then-destroy; tolerate a not-yet-cloned CT — destroy of a missing CT is a no-op, Pitfall 7), `logEvent(id, "boot.error", {"reason": _safe(exc)})` where `_safe` redacts secrets/internals (NO credential, NO repo-embedded token — Pitfall 7/ASVS V7), `updateWorkspace(id, {"status":"error"})`, then re-raise. The row lands in `error`, never stuck `creating`; the VMID is freed by the destroy.

    Read all timeouts/threshold from `settings` (Plan 01 added them). Do NOT edit config.py.

    Write `test_create_saga.py` (WS-02 happy path over a clean Fake → running, vmid in pool, ip resolved, created event, `_wait_ttyd` stubbed), `test_capacity_guard.py` (CAP-01 — node_memory=0.9 Fake → CapacityError, no row; assert payload.node is the queried node for CAP-04), and `test_compensation.py` (WS-03 — drive FakeFailures at clone/start/getIp/ttyd in turn; assert status=="error", `usedVmids()` empty / no leftover container, boot.error event present, and that no secret-looking string is in the event data).
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_create_saga.py tests/unit/test_compensation.py tests/unit/test_capacity_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - createWorkspace over a clean Fake reaches `running` with a pool-range vmid and resolved ip; a `workspace.created` event exists (WS-02, WS-01).
    - node_memory > 0.80 raises `CapacityError` and creates no row (CAP-01); the queried node equals `payload.node` (CAP-04).
    - VmidTakenError triggers retry; pool exhaustion raises NoFreeVmidError.
    - Each FakeFailures step (clone/start/getIp/ttyd) leaves the row in `error` (never `creating`), frees the VMID (Fake has no leftover container), and logs a redacted `boot.error` (WS-03).
    - The compensation test asserts no secret/credential string appears in the `boot.error` event data.
    - Service imports neither `aiosqlite` nor `proxmoxer` (seam guard green) and does not edit config.py; `mypy . --strict` clean.
  </acceptance_criteria>
  <done>The create saga + reservation + capacity guard + compensation are implemented and unit-proven over the Fake; WS-01/02/03 and CAP-01/04 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: stop/start/destroy with state guards, in-flight lock, and event logging</name>
  <read_first>
    - 01-RESEARCH.md Pattern 4 (assert_transition usage; the in-flight lock — asyncio.Lock keyed by workspace id PLUS a status-CAS update for cross-process safety, Assumption A2) and the WS-06/07/08 rows of the Phase Requirements table
    - api/lib/statemachine.py (assert_transition — from Task 1)
    - api/db/provider.py (updateWorkspace, softDeleteWorkspace, logEvent)
  </read_first>
  <behavior>
    - stopWorkspace(id): only legal from running (assert_transition guard) → stopCt UPID-blocked → status "stopped", stoppedAt set, "workspace.stopped" event. Stop on a creating/destroyed workspace raises IllegalTransitionError.
    - startWorkspace(id): only legal from stopped → startCt → resolve IP → await ttyd health → status "running", "workspace.started" event.
    - destroyWorkspace(id): legal from running/stopped/error → stopCt (if running) then destroyCt (idempotent) → softDeleteWorkspace + destroyedAt → "workspace.destroyed" event. Double-destroy raises IllegalTransitionError.
    - A per-workspace in-flight lock serializes concurrent mutations on one workspace id; the status-guarded UPDATE is the cross-process backstop.
  </behavior>
  <files>api/services/workspaceService.py, api/tests/unit/test_state_machine.py</files>
  <action>
    Add `stopWorkspace`, `startWorkspace`, `destroyWorkspace` to `WorkspaceService`. Each: load the workspace (raise `WorkspaceNotFoundError` if absent), call `assert_transition(ws.status, action)` (Task 1) BEFORE mutating, acquire the per-workspace in-flight lock (a `dict[str, asyncio.Lock]` keyed by workspace id — Claude's Discretion per CONTEXT; create the lock lazily), perform the provider mutation (UPID-blocked inside the provider), update status via `updateWorkspace` (use a status-guarded update where the DB supports it as the cross-process backstop, Assumption A2), set the relevant timestamp, and `logEvent` the lifecycle event. startWorkspace reuses the IP-resolve + `_wait_ttyd` steps (WS-07 awaits ttyd health). destroyWorkspace stops-if-running then destroys (idempotent) then soft-deletes (WS-08). Extend `test_state_machine.py` (or add focused service-level cases) to assert: stop-on-running succeeds and logs the event over the Fake; stop-on-creating / start-on-destroyed / double-destroy raise IllegalTransitionError at the service boundary (not just the table).
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit -x -q && uv run mypy . --strict</automated>
  </verify>
  <acceptance_criteria>
    - stop/start/destroy each call `assert_transition` before mutating; illegal transitions raise `IllegalTransitionError` at the service boundary.
    - Over the Fake: stop running→stopped (+stoppedAt+event), start stopped→running (+event), destroy→soft-deleted (+destroyedAt+event); destroy is idempotent.
    - A per-workspace asyncio lock serializes concurrent mutations on one id.
    - Full unit suite green; `mypy . --strict` clean; seam guard green (no driver import in the service).
  </acceptance_criteria>
  <done>stop/start/destroy are state-guarded, locked, UPID-blocked, and event-logged; WS-06/07/08/09 unit-proven over the Fake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → WorkspaceService | Validated WorkspaceCreate / workspace id crosses; the service is the policy authority |
| WorkspaceService → providers | Only the two ABCs; no driver type crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | VMID double-allocation across workers | mitigate | DB partial-unique INSERT is the arbiter; VmidTakenError → retry; in-process asyncio lock is only an optimization (SC-3, A2) |
| T-01-08 | Tampering | Concurrent stop/destroy double-fire | mitigate | assert_transition guard + per-workspace lock + status-guarded UPDATE as cross-process backstop (SC-12, A2) |
| T-01-09 | Information Disclosure | Secrets/internals in boot.error event data | mitigate | `_safe(exc)` redacts; only non-secret reason text logged; no credential/repo-token in event `data` (ASVS V7, Pitfall 7) |
| T-01-10 | Denial of Service | Orphaned LXC fills the VMID pool on failed create | mitigate | Per-step compensation (stop+destroy idempotent) frees the VMID; row→error not stuck creating (SC-9/SC-11) |
| T-01-11 | Denial of Service | Node overcommit by mass create | mitigate | Capacity guard (node mem > 0.80 → CapacityError, CAP-01); bounded VMID pool; LAN boundary (accepted-risk: no rate-limit in no-auth v1) |
| T-01-SC | Tampering | package installs | mitigate | No new packages this plan; `uv lock --check` CI gate |
</threat_model>

<verification>
- `cd api && uv run pytest tests/unit -q` — all four new unit files + existing green.
- `cd api && uv run ruff check . && uv run ruff format --check . && uv run mypy . --strict` clean.
- `test_seam_leakage.py` green — the service imports neither aiosqlite nor proxmoxer.
- `cd api && uv lock --check` consistent.
</verification>

<success_criteria>
- The create saga reaches `running` over the Fake and compensates to `error` (no orphan, vmid freed) on a forced failure at any step.
- The state machine rejects illegal transitions at the service boundary; stop/start/destroy are guarded, locked, and event-logged.
- Capacity guard refuses create at node mem > 0.80; the operator-selected node is honored.
- WS-01/02/03/06/07/08/09 + CAP-01/04 are unit-proven over the Fake; mypy strict + seam guard + lint green.
</success_criteria>

<output>
Create `.planning/phases/01-control-plane-api/01-03-SUMMARY.md` when done.
</output>
