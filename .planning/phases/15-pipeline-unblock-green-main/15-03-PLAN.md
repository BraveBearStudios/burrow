---
phase: 15-pipeline-unblock-green-main
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-pipeline-unblock-green-main/15-RELX-03-RULESET-RUNBOOK.md
autonomous: false
requirements: [RELX-03]
must_haves:
  truths:
    - "A durable operator runbook documents the exact gh api fetch-modify-PUT sequence and the repo-settings-UI fallback to exclude refs/heads/release-please--** from the oss ruleset (id 18189353)"
    - "After the operator applies the exclusion, the release-please workflow updates refs/heads/release-please--branches--main without the 'Error updating ref' rejection, and the release PR maintains cleanly"
    - "The exclusion is surgical: only the release-please branch glob is excluded; pull_request + non_fast_forward + required_linear_history stay enforced on main and all developer branches (no bypass actor added)"
  artifacts:
    - path: .planning/phases/15-pipeline-unblock-green-main/15-RELX-03-RULESET-RUNBOOK.md
      provides: "exact gh api + repo-settings-UI steps + confirmation for the oss-ruleset release-please exclusion"
      contains: "release-please--"
  key_links:
    - from: "the oss ruleset (id 18189353) conditions.ref_name.exclude"
      to: "the release-please bot ref update on refs/heads/release-please--branches--main"
      via: "excluded ref glob refs/heads/release-please--**"
      pattern: "release-please--"
---

<objective>
Unblock release-please (RELX-03): the active `oss` repo ruleset (id 18189353; rules pull_request +
non_fast_forward + required_linear_history, applied to ~all branches) rejects the release-please
bot's ref update to `refs/heads/release-please--branches--main` because `github-actions[bot]` is not
a bypass actor, so every release-please run fails at "Error updating ref". Per the CONTEXT locked
decision, exclude `refs/heads/release-please--**` from the `oss` ruleset (surgical: leaves branch
protection intact everywhere else) rather than adding a bypass actor.

Purpose: this is a GitHub repo-ADMIN change, NOT a repo file edit; the session `gh` token lacks
`admin:org` / repo-admin, so the operator runs it. This plan authors the exact commands as a durable
runbook (Task 1, autonomous), then the operator applies them (Task 2, blocking human-action) and
confirms release-please maintains its branch cleanly.

Output: a committed `15-RELX-03-RULESET-RUNBOOK.md` and an operator-applied ruleset exclusion.
`autonomous: false` (this plan pauses for the operator GitHub-admin action).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-pipeline-unblock-green-main/15-CONTEXT.md
@.github/workflows/release-please.yml

<interfaces>
<!-- GitHub Rulesets API facts the runbook must encode. Ruleset id is 18189353 ("oss"). -->

Read the ruleset:   gh api repos/<owner>/<repo>/rulesets/18189353
Update the ruleset: gh api --method PUT repos/<owner>/<repo>/rulesets/18189353 --input <file>

The exclusion lives at .conditions.ref_name.exclude (an array of fnmatch ref globs). To exclude the
release-please branch, append refs/heads/release-please--** to that array.

A GET returns server-managed read-only fields (id, node_id, source, source_type, created_at,
updated_at, _links, current_user_can_bypass) that a PUT must NOT include; keep only
name, target, enforcement, bypass_actors, conditions, rules.

release-please bot identity in release-please.yml is the run's GITHUB_TOKEN (github-actions[bot]);
the branch it maintains is refs/heads/release-please--branches--main.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the oss-ruleset exclusion runbook</name>
  <read_first>
    - .planning/phases/15-pipeline-unblock-green-main/15-CONTEXT.md (RELX-03 decision: surgical exclusion, operator-run, session token lacks admin)
    - .github/workflows/release-please.yml (the bot ref it updates: refs/heads/release-please--branches--main; token is the run GITHUB_TOKEN)
    - .planning/phases/15-pipeline-unblock-green-main/15-CONTEXT.md (Integration Points: the oss ruleset id 18189353 is GitHub repo config, not a repo file)
  </read_first>
  <action>
    Create `.planning/phases/15-pipeline-unblock-green-main/15-RELX-03-RULESET-RUNBOOK.md` opening with
    the phase-dir HTML-comment SPDX header (matching 15-CONTEXT.md: an HTML comment carrying
    `SPDX-FileCopyrightText: 2026 Brave Bear Studios` and `SPDX-License-Identifier: AGPL-3.0-or-later`).
    Document, as exact numbered operator commands (a GitHub repo-admin change, NOT a repo edit; the
    session gh token lacks admin:org/repo-admin, so the operator runs it against their authenticated gh):
      1. Fetch the ruleset to a file: `gh api repos/BraveBearStudios/burrow/rulesets/18189353 > ruleset.json`.
      2. Append the release-please branch to the exclude list AND strip the server read-only fields via
         jq, writing ruleset.new.json - document the jq transform that sets
         `.conditions.ref_name.exclude` to the existing exclude array (or empty) plus
         `refs/heads/release-please--**`, deduped, and then projects the body down to exactly
         `{name, target, enforcement, bypass_actors, conditions, rules}` so the PUT cannot carry read-only
         fields.
      3. Apply: `gh api --method PUT repos/BraveBearStudios/burrow/rulesets/18189353 --input ruleset.new.json`.
      4. Repo-settings-UI fallback: Settings -> Rules -> Rulesets -> open the `oss` ruleset -> Target
         branches -> Add target -> Exclude by pattern -> enter `refs/heads/release-please--**` -> Save.
      5. Confirmation: push a commit to main (or re-run the failed release-please run) and confirm the
         release-please job updates refs/heads/release-please--branches--main with NO
         "Error updating ref heads/release-please--branches--main"; the release PR opens/updates cleanly.
    Add a substitution note that `BraveBearStudios/burrow` is replaced with the actual owner/repo if
    different, and an emphasis line that ONLY the release-please glob is excluded so all other oss-ruleset
    protection (pull_request, non_fast_forward, required_linear_history) stays intact and no bypass actor
    is added. Do not use em-dashes in the file.
  </action>
  <acceptance_criteria>
    - The runbook file exists at the phase path with the HTML-comment SPDX header (SPDX-License-Identifier present)
    - It contains the exact `gh api ... repos/.../rulesets/18189353` fetch command and the `gh api --method PUT ... --input` apply command
    - It documents a jq transform that appends `refs/heads/release-please--**` to conditions.ref_name.exclude and projects the PUT body to name/target/enforcement/bypass_actors/conditions/rules only
    - It documents the Settings -> Rules -> Rulesets UI fallback and the post-apply confirmation (no "Error updating ref")
    - No em-dash characters appear in the file
  </acceptance_criteria>
  <verify>
    <automated>python -c "t=open('.planning/phases/15-pipeline-unblock-green-main/15-RELX-03-RULESET-RUNBOOK.md',encoding='utf-8').read(); assert 'SPDX-License-Identifier' in t; assert 'rulesets/18189353' in t; assert '--method PUT' in t; assert 'refs/heads/release-please--**' in t; assert 'ref_name.exclude' in t or 'exclude' in t; assert 'Error updating ref' in t; assert 'Rulesets' in t; assert '—' not in t, 'em-dash present'; print('OK')"</automated>
  </verify>
  <done>15-RELX-03-RULESET-RUNBOOK.md gives the operator the exact gh api fetch-modify-PUT plus UI fallback and confirmation to exclude the release-please branch from the oss ruleset; SPDX header present; no em-dashes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator excludes the release-please branch from the oss ruleset</name>
  <read_first>
    - .planning/phases/15-pipeline-unblock-green-main/15-RELX-03-RULESET-RUNBOOK.md (the exact commands authored in Task 1)
  </read_first>
  <what-built>
    Task 1 authored the runbook with the exact gh api fetch-modify-PUT sequence, the repo-settings-UI
    fallback, and the post-apply confirmation. Claude cannot apply it: this is a GitHub repo-admin
    change and the session gh token lacks admin:org / repo-admin.
  </what-built>
  <action>
    Operator-run GitHub repo-admin action. Follow 15-RELX-03-RULESET-RUNBOOK.md with your authenticated
    gh: run the fetch-modify-PUT to append `refs/heads/release-please--**` to the oss ruleset
    (id 18189353) `.conditions.ref_name.exclude`, or apply the Settings -> Rules -> Rulesets UI
    fallback. Then trigger release-please (push a commit to main or re-run the failed run) and confirm
    the bot updates `refs/heads/release-please--branches--main` with no "Error updating ref".
  </action>
  <how-to-verify>
    1. `gh api repos/<owner>/burrow/rulesets/18189353` shows `refs/heads/release-please--**` inside
       `conditions.ref_name.exclude`.
    2. The oss ruleset's other rules (pull_request, non_fast_forward, required_linear_history) and its
       enforcement are unchanged for all non-excluded branches, and no new bypass_actor was added.
    3. A release-please run completes without "Error updating ref heads/release-please--branches--main"
       and the release PR / release branch updates cleanly.
  </how-to-verify>
  <acceptance_criteria>
    - The oss ruleset (id 18189353) conditions.ref_name.exclude contains refs/heads/release-please--**
    - pull_request + non_fast_forward + required_linear_history and the ruleset enforcement stay intact for non-excluded branches; no bypass actor added
    - A release-please run maintains its branch without the ref-update rejection; the release PR advances cleanly
  </acceptance_criteria>
  <resume-signal>Type "applied" once the exclusion is live and a release-please run maintains refs/heads/release-please--branches--main without the "Error updating ref" rejection, or paste the error to iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-admin ruleset config -> branch protection posture | The oss ruleset enforces pull_request / linear-history / no-force-push across branches; a mis-scoped exclusion could weaken protection on real branches. |
| release-please bot (github-actions[bot]) -> release branch ref | The bot force-updates its own transient release branch; the ruleset currently blocks that ref update. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege | oss ruleset exclusion scope | mitigate | The exclusion is a single surgical glob refs/heads/release-please--** (the bot's transient branches only); pull_request + non_fast_forward + required_linear_history stay enforced on main and all developer branches, and no bypass_actor is added. |
| T-15-08 | Tampering | operator PUT of the full ruleset body | mitigate | The runbook fetch-modify-PUT preserves the existing name/target/enforcement/rules/bypass_actors and only appends the exclude glob (deduped); server read-only fields are stripped so the PUT cannot silently reset protections. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (one runbook doc + an operator GitHub-admin action); no new `uses:` action; no Package Legitimacy Audit required. |
</threat_model>

<verification>
Structural + human-gated (the live proof is operator-run; there is no repo artifact to test the
GitHub-side ruleset change from the executor's structural pass):
- The runbook exists with the SPDX header, the exact gh api fetch + PUT commands, the jq exclude
  transform, the UI fallback, and the confirmation step; no em-dashes.

**Human-gated (Task 2, blocking):** the operator applies the exclusion with their admin-scoped gh
and confirms a release-please run updates refs/heads/release-please--branches--main without the
"Error updating ref" rejection. The session gh token lacks admin:org/repo-admin, so this cannot be
automated here.
</verification>

<success_criteria>
- A durable, exact runbook exists for the oss-ruleset release-please exclusion (RELX-03 documented).
- The operator applies the surgical exclusion; other branch protection stays intact; no bypass actor added.
- A release-please run maintains its branch without the ref-update rejection (RELX-03 live-confirmed / human-gated).
</success_criteria>

<output>
Create `.planning/phases/15-pipeline-unblock-green-main/15-03-SUMMARY.md` when done.
Record: the runbook path + the exact commands captured; that Task 2 is operator-run (session token
lacks repo-admin); and the operator's confirmation result (release-please branch maintained without
"Error updating ref"), or the pending human-gated status if not yet applied.
</output>
