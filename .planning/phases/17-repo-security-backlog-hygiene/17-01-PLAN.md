<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->
# Phase 17 Plan 01: Repo Security & Backlog Hygiene

**Requirements:** SEC-01, SEC-02, SEC-03, ROB-01, ROB-02
**Approach:** config + mechanical fixes on disjoint surfaces; failing-first tests for the ROB code changes.

## Task 1 — Dependabot config (SEC-01)
Create `.github/dependabot.yml` (version 2): three `updates` entries —
`uv` at `/api`, `npm` at `/ui`, `github-actions` at `/` — each `schedule.interval: weekly`
with a `groups` block collapsing that ecosystem's updates into one PR. SPDX header (YAML `#`).

## Task 2 — Dependabot automated-security-fixes (SEC-02)
Enable repo-level: `gh api -X PUT /repos/{owner}/{repo}/vulnerability-alerts` then
`-X PUT /repos/{owner}/{repo}/automated-security-fixes`. If the token lacks repo-admin,
record as an operator follow-up in SUMMARY/VERIFICATION (do not fail the phase).

## Task 3 — CodeQL SAST (SEC-03)
Create `.github/workflows/codeql.yml`: matrix languages `python` +
`javascript-typescript`; triggers `push`/`pull_request` on `main` + weekly `schedule`;
`github/codeql-action/{init,analyze}` SHA-pinned with `# vX` trailing comments;
`queries: security-and-quality`; `harden-runner` step 0 (audit) mirroring ci.yml;
minimal `security-events: write` perms. SPDX header. First-run findings triaged on `main`
(recorded/dismissed/fixed); ADR-0016 only if a baseline deviation is recorded.

## Task 4 — ROB-01: precise CT-locked predicate + failing-first test
In `api/compute/proxmoxProvider.py::_is_running_or_locked`, delete `or "lock" in text`
(bare, over-broad). Keep `"is locked"` + `"can't lock"`. Add
`api/tests/unit/test_proxmox_predicates.py`: assert benign `"unlock"`/`"locked out of
account"` do NOT match (RED before the fix), and `"CT is locked"`/`"is running"`/
`"can't lock file"`/`"not stopped"` DO. Move `proxmox-provider-preexisting-robustness.md`
+ `07r-harden-stop-start-e2e-cleanup.md` from `todos/pending/` to `todos/completed/`.

## Task 5 — ROB-02: real leak assertion + em-dash sweep
In `api/tests/boot/test_burrow_boot.py::test_no_credential_leak`, replace the
tautological `worker.env` assertion (harness writes it, boot never touches it) with a
walk over `$HOME` asserting the sentinel credential + project URL appear in NO file the
boot actually writes. Sweep em-dashes in worker-template shell **comment** lines only
(`cc-worker-config/lxc/worker-template/{burrow-boot.sh,provision-template.sh}`) — leave
`log "..."` strings intact. Move `boot-credential-leak-test-tautology.md` to completed.

## Verification
`ruff check` + `mypy --strict` (api) clean; api unit + boot suites green (new predicate
test RED-then-GREEN across the one-line fix); `.github/dependabot.yml` +
`.github/workflows/codeql.yml` parse (yaml valid, actionlint if available); SPDX headers
present; no em-dash remains in worker-template comment lines.
