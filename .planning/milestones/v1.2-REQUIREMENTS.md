# Requirements Archive: v1.2 Backlog Fixes + Release Automation

**Archived:** 2026-06-16
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

# Requirements: Burrow — v1.2

**Defined:** 2026-06-15
**Milestone:** v1.2 Backlog Fixes + Release Automation
**Core Value:** One operator can create, watch, and manage many concurrent Claude Code sessions from a browser, each in an ephemeral, reproducible container that is gone when destroyed.

> v1.2 clears the v1.1 backlog debt (the two deferred code-review findings), hardens the
> release/CI surface (release automation + a locked-down runner), and adds capacity-aware
> auto node selection. All scope is dev-box-buildable and CI-provable over the
> FakeComputeProvider; real-infra acceptance (ACC-01/02/03) and the real-boot v2 items
> (WSX-02/03) remain deferred. v1.0/v1.1 requirements + archives live in `milestones/`.

## User Stories

- As an operator, when a terminal drops, my workspace list reflects the new status immediately instead of waiting for the next poll tick.
- As an operator, I create a workspace without hand-picking a node — the control plane chooses the least-loaded node that fits.
- As a maintainer, a merge to main opens an automated release PR (version bump + changelog) so cutting a release is one click, and my CI jobs run under a locked-down, egress-restricted runner with SHA-pinned actions.

## v1.2 Requirements

### Workspace Lifecycle UI (UI)

- [x] **UI-12**: The workspace list reconciles immediately on a terminal error/close — `LeafPanel` wires `onTerminalEvent` → `useInvalidateWorkspaces` (the documented Pitfall-4 fast-reconcile), so the sidebar/status refresh without waiting for the ~3s poll (WR-01)

### Compute & Capacity (WSX)

- [x] **WSX-01**: Workspace creation can auto-select the worker node — when the operator does not pick a node, the control plane chooses a capacity-fitting node (least-loaded that passes the node-RAM threshold), proven over the FakeComputeProvider's multi-node capacity. Manual node pick remains available. (Real multi-node validation is the dev-homelab smoke.)

### Release & Runner (RELX)

- [x] **RELX-01**: Release automation via **release-please** — a workflow that, from Conventional Commits on main, maintains a release PR (semantic version bump + generated changelog) and tags `v*` on merge. Config is buildable + lintable locally; the first real release PR is the on-runner acceptance.
- [x] **RELX-02**: CI workflows run under `step-security/harden-runner` with an egress allowlist (audit-then-block policy) and all third-party actions pinned to a commit SHA; the policy is documented. (First real enforcement is on the runner.)

### CI / Test Robustness (CICD)

- [x] **CICD-09**: The stop/start Playwright e2e is hardened — panel-scoped locators (no unscoped `.first()`/global count assertions) and per-test backend isolation (cleanup or DB reset) so the suite is robust to parallelization and multi-panel state (WR-02)

## Future Requirements

Tracked, not in the v1.2 roadmap.

### Real-infra acceptance (off the dev box)

- **ACC-01**: Dev-homelab smoke against real Proxmox (incl. the WSX-01 multi-node selection on real nodes)
- **ACC-02**: First real CI run on a live runner (the pinned reuse step + harden-runner enforcement + release-please PR)
- **ACC-03**: Real GHCR release (publish + `cosign verify` + `gh attestation verify`)

### v2 Candidates (real-boot)

- **WSX-02**: Persistent / snapshotted workspaces that survive destroy
- **WSX-03**: Full terminal scrollback restore after refresh (requires tmux/zellij in the worker template)

## Out of Scope

Carried forward — explicit boundaries.

| Feature | Reason |
|---------|--------|
| Authentication / login / multi-tenancy / RLS | v1 is LAN-only single-user by design; auth is hosted-path scope (tech-spec §13) |
| Postgres as primary DB | SQLite is the v1 store (ADR-0001); Postgres impl is a stub behind `DbProvider` |
| Cloud / container compute backend | v1 targets Proxmox LXC only; `ComputeProvider` seam exists but no other impl |
| Real Proxmox exercised in CI | CI is hermetic (FakeComputeProvider + mocks); real infra validated in the dev homelab |
| Native mobile app | Browser-first, responsive web only |
| Secrets manager | v1 uses a gitignored `.env`; secrets manager is hosted-path scope |

## Definition of Done

v1.2 is releasable when:

- All v1.2 requirements above are implemented and mapped to a completed phase.
- CI is green over the Fake provider: the UI-12 reconcile test, the hardened stop/start e2e (CICD-09), the WSX-01 auto-select unit/integration tests; the release-please + harden-runner workflows are syntactically valid and lint-clean.
- Every new/changed source file carries the SPDX header (planning artifacts via REUSE.toml per CICD-08); any baseline-architecture deviation gets an ADR.
- Affected docs (tech-spec node-selection note, CONTRIBUTING release process, README badges if added) updated to match shipped reality.

## Traceability

Which phases cover which requirements. **Populated during roadmap creation.**

| Requirement | Phase | Status |
|-------------|-------|--------|
| UI-12 | Phase 7 | Complete |
| CICD-09 | Phase 7 | Complete |
| RELX-01 | Phase 8 | Complete |
| RELX-02 | Phase 8 | Complete |
| WSX-01 | Phase 9 | Complete |

**Coverage:**

- v1.2 requirements: 5 total
- Mapped to phases: 5 (Phase 7: UI-12, CICD-09 · Phase 8: RELX-01, RELX-02 · Phase 9: WSX-01)
- Unmapped: 0

---
*Requirements defined: 2026-06-15 for milestone v1.2; mapped to phases 7-9 during roadmap creation.*
