---
phase: 10-persistence-data-model-reaper-carve-out
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - api/services/reconciler.py
  - api/tests/unit/test_reconciler.py
  - api/tests/integration/test_workspaces_api.py
autonomous: true
requirements: [WSX-04]
must_haves:
  truths:
    - "A persistent stopped workspace keeps a live row, so its VMID stays in live_vmids and the orphan reaper spares it (RED if the predicate ever regresses to status-based reaping)"
    - "A soft-deleted persistent workspace drops out of listWorkspaces(), so its VMID leaves live_vmids and the CT becomes orphan-eligible (delete is not a persistence shield)"
    - "The reaper predicate stays a single ownership-keyed bound — NO status/persistent branch added"
    - "A persistent workspace survives stop->start with the same id/vmid and persistent still true"
  artifacts:
    - path: "api/services/reconciler.py"
      provides: "a carve-out comment at the predicate making the safety bound's intent explicit (comment ONLY)"
      contains: "persisten"
    - path: "api/tests/unit/test_reconciler.py"
      provides: "two negative-control tests: persistent-stopped never reaped; soft-deleted-persistent orphan-eligible"
      contains: "test_persistent_stopped_workspace_is_never_reaped"
    - path: "api/tests/integration/test_workspaces_api.py"
      provides: "persistent create round-trip + default-ephemeral + stop->start preserves persistent"
      contains: "persistent"
  key_links:
    - from: "api/tests/unit/test_reconciler.py"
      to: "api/services/reconciler.py _reap predicate"
      via: "reconcile_once() over a persistent stopped row + injected FakeContainer; assert CT survives"
      pattern: "reconcile_once"
    - from: "api/tests/integration/test_workspaces_api.py"
      to: "stop/start lifecycle"
      via: "create persistent -> stop -> start; same vmid, persistent true"
      pattern: "persistent"
---

<objective>
Lock the safety-critical reaper carve-out (WSX-04) and the persistence behavior. The reaper
predicate at `reconciler.py:107-109` ALREADY keys on ownership (`vmid in live_vmids`), never on
`stopped` state, and `listWorkspaces()` excludes soft-deleted rows — so a persistent stopped
workspace is already spared and a soft-deleted persistent workspace already becomes
orphan-eligible. Phase 10's deliverable for WSX-04 is therefore (1) a carve-out COMMENT making
the bound's intent explicit, and (2) the RED-if-regressed negative-control tests that fail the
instant the predicate ever regresses to status-based reaping. Plus the WSX-02 behavioral proof:
a persistent workspace survives stop->start with the same id/vmid and `persistent` still true.

Purpose: This is the v1.3 HARD GATE #1 — the reaper-destroys-a-persistent-workspace hazard is
irreversible data loss. It depends on Plan 03 (the `persistent` column is required to create
persistent rows in the tests) and on Plan 01 (the mock-tier gate). The carve-out MUST NOT add a
`status == "stopped"` or `persistent` branch to the predicate — that is the exact regression the
negative-control test guards (RESEARCH Pitfall 1; CONTEXT: keep the single safety bound in one loop).

Output: a carve-out comment in `reconciler.py` (comment only — logic unchanged), two
negative-control tests in `test_reconciler.py`, and persistence round-trip tests in
`test_workspaces_api.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-persistence-data-model-reaper-carve-out/10-RESEARCH.md
@.planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The single safety bound (DO NOT add a branch). Source: reconciler.py:107-113 (verified). -->
The orphan predicate (reconciler.py:107-113):
  for node, vmid in sorted(managed, key=lambda nv: nv[1]):
      if vmid in live_vmids or vmid not in pool:
          continue  # SAFETY BOUND: never touch a live-owned or out-of-pool CT (V4).
      await self.compute.destroyCt(node, vmid)  # idempotent, correct node

`live_vmids` (reconciler.py:97-98) = `{row.vmid for row in db.listWorkspaces() if row.vmid is not None}`.
`SqliteProvider.listWorkspaces` filters `WHERE deletedAt IS NULL` (sqliteProvider.py:192) — so:
  - a STOPPED persistent workspace keeps a live row -> vmid in live_vmids -> SPARED.
  - a SOFT-DELETED persistent workspace drops out of the list -> vmid leaves live_vmids -> orphan-eligible.
Both fall out of the EXISTING bound with zero new code. The Phase 10 edit is a COMMENT ONLY.

<!-- Reconciler unit-test fixtures + helpers (reuse exactly). Source: test_reconciler.py (verified). -->
- `db` fixture (test_reconciler.py:50-54): real `SqliteProvider` over `tmp_path`, `await provider.migrate()` (applies 003 once it exists).
- `compute` fixture (:57-59): `FakeComputeProvider(node_memory=0.25)`.
- `_reconciler(compute, db, now)` (:66-77) -> `.reconcile_once()`.
- `real_settings.worker_pool_start` for in-pool VMIDs; `_FakeContainer` from `compute.fakeProvider`.
- container-injection idiom: `compute._containers[vmid] = _FakeContainer(vmid=vmid, name=..., node="pve1")`.
- direct-create idiom (pass status + persistent): `await db.createWorkspace({..., "vmid": vmid, "status": "stopped", "persistent": True})` (mirrors `owned_ws` at :194-202).
- `db.softDeleteWorkspace(ws.id)` (sqliteProvider.py:240-249) to drop the row from listWorkspaces().

<!-- Workspaces-API integration helpers. Source: test_workspaces_api.py:13-34 (verified). -->
- `_CREATE_BODY` (:13-18) camelCase; `_create(client, **overrides)` (:28-34) merges overrides into the body, asserts 200, returns `payload["data"]`.
- `test_stop_then_start_round_trip` (analog ~:78-89): stop then start over the real app + Fake.
- NOTE: `_create(**overrides: str)` is typed `str`; widen the annotation (e.g. `object`/`bool`) or
  build the body inline so `persistent=True` (a bool) type-checks. The body key is `"persistent"`
  (single word = identical camelCase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the reaper carve-out comment (comment only — logic unchanged)</name>
  <files>api/services/reconciler.py</files>
  <read_first>
    - api/services/reconciler.py:75-127 (the `_reap` method; the predicate + `continue` SAFETY BOUND at :107-113)
    - api/db/sqliteProvider.py:192 (`listWorkspaces` filters `WHERE deletedAt IS NULL`) — the reason a stopped persistent row stays live and a soft-deleted one drops out
    - .planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md (section "api/services/reconciler.py" — the carve-out comment intent, lines 164-186)
  </read_first>
  <action>
    At the orphan-predicate `continue` line in `reconciler.py` (~:109), add a carve-out comment
    (a few lines of `#` prose) making the safety bound's intent explicit: persistence protects a
    workspace across stop->start via the existing live-row ownership bound (a stopped persistent
    workspace keeps a live row, so its vmid is in `live_vmids` and is never an orphan); an explicit
    operator delete intentionally removes that protection (a soft-deleted row leaves
    `listWorkspaces()`, so its vmid leaves `live_vmids` and becomes orphan-eligible — delete is not
    a persistence shield); the bound keys on OWNERSHIP, never on `stopped` state — proven by the
    negative-control tests. Reference WSX-04. Add the COMMENT ONLY. Do NOT add a `status == "stopped"`
    check, a `persistent` exclusion set, a new setting, or any branch — that is the exact regression
    the negative-control test guards (RESEARCH Pitfall 1; the single safety bound stays in one loop).
    Do not modify the predicate logic, `live_vmids`, the destroy call, or case (B).
  </action>
  <verify>
    <automated>cd api && rtk pytest tests/unit/test_reconciler.py -x; cd api && rtk mypy api/services/reconciler.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/services/reconciler.py` has a new carve-out comment at/near the orphan predicate (~:109) mentioning persistence + stop->start + delete-is-not-a-shield + WSX-04
    - the predicate code is UNCHANGED — `grep -n "if vmid in live_vmids or vmid not in pool" api/services/reconciler.py` still matches and no `status`/`persistent` token appears inside the orphan branch of `_reap`
    - `grep -c "persistent" api/services/reconciler.py` counts only comment occurrences (no code branch)
    - the existing reconciler unit suite still passes; `rtk mypy` clean
  </acceptance_criteria>
  <done>The safety bound carries an explicit carve-out comment (WSX-04 intent) and its logic is byte-for-byte unchanged. No status/persistent branch was added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Negative-control reaper tests (RED-if-regressed)</name>
  <files>api/tests/unit/test_reconciler.py</files>
  <read_first>
    - api/tests/unit/test_reconciler.py:50-77 (fixtures + `_reconciler`), :120 (container-injection idiom), :132-142 (`test_reap_spares_in_pool_ct_with_a_live_row` analog), :194-202 (`owned_ws` direct-create idiom)
    - api/db/sqliteProvider.py:240-249 (`softDeleteWorkspace`)
    - .planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md (section "api/tests/unit/test_reconciler.py" — copy-ready Test A + Test B, lines 286-328)
  </read_first>
  <behavior>
    - Test A (persistent stopped is NEVER reaped): create a persistent (`persistent: True`) workspace in `status: "stopped"` with an in-pool vmid + an injected stopped `_FakeContainer`; run one `reconcile_once()`; assert `vmid in compute._containers` (CT survives) AND the DB row is intact (`status == "stopped"`, `persistent is True`). RED the instant the predicate regresses to status-based reaping.
    - Test B (soft-deleted persistent BECOMES orphan-eligible): same setup, then `await db.softDeleteWorkspace(ws.id)` before the pass; assert `vmid not in compute._containers` (CT reclaimed) — persistence is not a delete-shield.
  </behavior>
  <action>
    Extend `api/tests/unit/test_reconciler.py` with two async tests reusing the `compute`/`db`
    fixtures and `_reconciler(compute, db, now=datetime.now(timezone.utc))`. Test A
    `test_persistent_stopped_workspace_is_never_reaped`: `vmid = real_settings.worker_pool_start + 20`;
    `await db.createWorkspace({"name": "persistent-stopped", "node": "pve1", "project_repo":
    "git@example.com:acme/x.git", "vmid": vmid, "status": "stopped", "persistent": True})`; inject
    `compute._containers[vmid] = _FakeContainer(vmid=vmid, name="persistent-stopped", node="pve1",
    running=False)`; run `reconcile_once()`; assert `vmid in compute._containers` and the row
    read-back is `status == "stopped"` and `persistent is True`. Test B
    `test_soft_deleted_persistent_workspace_becomes_orphan_eligible`: same create with
    `vmid = real_settings.worker_pool_start + 21`, inject the container, then
    `await db.softDeleteWorkspace(ws.id)`, run `reconcile_once()`, assert `vmid not in
    compute._containers`. Use the direct-create idiom (pass `status` + `persistent`) exactly as the
    `owned_ws` pattern; do not reuse `_running_row`/`_creating_row` (they build rows without
    `persistent`).
  </action>
  <verify>
    <automated>cd api && rtk pytest tests/unit/test_reconciler.py::test_persistent_stopped_workspace_is_never_reaped tests/unit/test_reconciler.py::test_soft_deleted_persistent_workspace_becomes_orphan_eligible -x; cd api && rtk ruff check tests/unit/test_reconciler.py; cd api && rtk mypy tests/unit/test_reconciler.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_persistent_stopped_workspace_is_never_reaped` exists and asserts the CT survives + the row is intact (`status == "stopped"`, `persistent is True`)
    - `test_soft_deleted_persistent_workspace_becomes_orphan_eligible` exists and asserts the CT is reclaimed after `softDeleteWorkspace`
    - both tests pass: `rtk pytest tests/unit/test_reconciler.py -x` exits 0
    - the tests are RED-if-regressed (they would fail if the predicate added a `status`-based reap) — Test A spares a STOPPED CT solely because it is live-owned
    - `rtk ruff check` + `rtk mypy` clean
  </acceptance_criteria>
  <done>The reaper safety bound is locked by two RED-if-regressed negative-control tests: a persistent stopped CT survives the reap; a soft-deleted persistent CT becomes orphan-eligible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistence behavioral round-trip tests (WSX-02 SC2)</name>
  <files>api/tests/integration/test_workspaces_api.py</files>
  <read_first>
    - api/tests/integration/test_workspaces_api.py:13-34 (`_CREATE_BODY`, `_create`, `_assert_envelope`) and the `test_stop_then_start_round_trip` analog (~:78-89)
    - .planning/phases/10-persistence-data-model-reaper-carve-out/10-PATTERNS.md (section "api/tests/integration/test_workspaces_api.py" — copy-ready create round-trip + stop->start snippets, lines 331-358)
  </read_first>
  <behavior>
    - persistent create round-trips: `_create(client, persistent=True)` returns `data["persistent"] is True` (camelCase key round-trips DB->API)
    - default create is ephemeral: `_create(client)` (no persistent in `_CREATE_BODY`) returns `data["persistent"] is False`
    - persistent survives stop->start: a persistent workspace stopped then started returns to `running` with the SAME id/vmid and `persistent` still true (Tier-1 reuses the VMID)
  </behavior>
  <action>
    Extend `api/tests/integration/test_workspaces_api.py`. First widen `_create`'s `**overrides`
    annotation from `str` to `object` (or build the body inline) so `persistent=True` (a bool)
    type-checks under mypy. Add `test_persistent_create_round_trips_camelcase`: call
    `_create(integration_client, persistent=True)` and assert `data["persistent"] is True`. Add
    `test_default_create_is_ephemeral`: call `_create(integration_client)` (the default
    `_CREATE_BODY` has no `persistent`) and assert `data["persistent"] is False`. Add a persistent
    variant of the stop->start round-trip (mirroring `test_stop_then_start_round_trip`): create with
    `persistent=True`, capture `created["vmid"]`, POST `/stop` then `/start`, and assert the started
    `data["status"] == "running"`, `data["persistent"] is True`, and `data["vmid"] == created["vmid"]`
    (same VMID reused — Tier-1, no soft-delete). Keep the `_assert_envelope` discipline on every
    response.
  </action>
  <verify>
    <automated>cd api && rtk pytest tests/integration/test_workspaces_api.py -x; cd api && rtk ruff check tests/integration/test_workspaces_api.py; cd api && rtk mypy tests/integration/test_workspaces_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_persistent_create_round_trips_camelcase` asserts `data["persistent"] is True` for a `persistent=True` create
    - `test_default_create_is_ephemeral` asserts `data["persistent"] is False` for the default body
    - a persistent stop->start test asserts `status == "running"`, `persistent is True`, and the SAME `vmid` as created
    - `_create`'s override annotation is widened so the bool override type-checks (`rtk mypy` clean)
    - `rtk pytest tests/integration/test_workspaces_api.py -x` exits 0; ruff clean
  </acceptance_criteria>
  <done>The WSX-02 behavior is proven: `persistent=true` round-trips as camelCase JSON, the default stays ephemeral, and a persistent workspace survives stop->start with the same id/vmid intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler -> compute.destroyCt | The single safety bound decides which CTs are destroyed; a wrong predicate = irreversible data loss for a persistent stopped workspace (the v1.3 hard-gate hazard). |
| reaper -> reaper.* event/log | Any row field read into a `reaper.*` event must not leak operator strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04A | Tampering / Denial of Service | reaper destroying a persistent stopped workspace's disk | mitigate | The ownership-keyed bound stays unchanged + the RED-if-regressed negative-control test (Test A) fails the instant the predicate regresses to status-based reaping (WSX-04 hard gate; RESEARCH Pitfall 1). |
| T-10-04B | Tampering | a future `status`/`persistent` branch (or TTL) added to the orphan branch | mitigate | Carve-out comment documents "ownership, never state"; the acceptance gate forbids a `status`/`persistent` token inside the `_reap` orphan branch; Test A spares a STOPPED CT solely on ownership. |
| T-10-04C | Information Disclosure | reading `persistent` into a `reaper.*` event leaking a secret | accept | `persistent` is a bool (no operator string); the existing `_safe()` redaction + `test_reaper_*_carries_no_secret` guard remain in force; no new field widens the leak surface (V7 inherited). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — every lib is already committed; RESEARCH Package Legitimacy Audit is N/A. |
</threat_model>

<verification>
- `rtk pytest tests/unit/test_reconciler.py tests/integration/test_workspaces_api.py -x` is green.
- The predicate is unchanged: `git diff api/services/reconciler.py` shows ONLY added `#` comment lines (no code-line change to the orphan branch).
- No status/persistent branch in `_reap`: the orphan branch contains no `status ==` or `persistent` code token (comment occurrences only).
- Full backend suite green before the phase gate: `cd api && rtk pytest`, then `rtk ruff check .` + `rtk mypy .`.
</verification>

<success_criteria>
- A negative-control regression test proves the orphan reaper never destroys a persistent stopped
  workspace — the predicate keys on "no owning DB row," not on `stopped` state (RED if it ever
  regresses to state-based reaping) (SC3 / WSX-04).
- A soft-deleted persistent workspace correctly becomes orphan-eligible (delete is not a
  persistence shield).
- A persistent workspace survives stop->start with the same id/vmid and `persistent` still true
  (SC2 behavioral half / WSX-02).
</success_criteria>

<output>
Create `.planning/phases/10-persistence-data-model-reaper-carve-out/10-04-SUMMARY.md` when done
</output>
