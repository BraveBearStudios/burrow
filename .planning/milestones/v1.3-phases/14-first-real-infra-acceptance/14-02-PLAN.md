---
phase: 14-first-real-infra-acceptance
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md
  - .planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md
  - .planning/milestones/v1.0-phases/03-reproducible-workers/03-HUMAN-UAT.md
  - .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03]
must_haves:
  truths:
    - "14-ACCEPTANCE.md is the operator release/verify runbook: it gives the exact cosign verify + gh attestation verify commands BY DIGEST, documents the gh-attestation exit-0-on-failure trap (assert on OUTPUT not exit code), the verify-by-digest-not-tag rule, the harden-runner audit->block flip procedure, the exactly-4-perms + SHA-pin checklist, and the actionlint gate, each ACC item with command + expected output + pass/fail"
    - "14-HUMAN-UAT.md is the consolidated ACC-01/02/03 operator checklist: the H9 five-step homelab gate (create->terminal->stop->start->destroy) + reaper/auto-stop/capacity/real node selection, the real persistent stop->start with disk intact AND scrollback restored, and the live-release verify; it rolls up the still-pending Phase 03 + Phase 04 HUMAN-UAT items"
    - "The pending Phase 03 and Phase 04 HUMAN-UAT files are marked superseded-by-v1.3-Phase-14 so the rolled-up items have a single source of truth"
    - "ACC-01, ACC-02, ACC-03 are documented as MANUAL-ONLY / human_needed (operator-run on real Proxmox + a live release); nothing in this plan claims them auto-passed"
  artifacts:
    - path: .planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md
      provides: "operator release/verify runbook for ACC-02/03 (cosign + attestation verify by digest, flip procedure, checklists)"
      contains: "gh attestation verify"
    - path: .planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md
      provides: "consolidated ACC-01/02/03 human-UAT checklist (H9 gate + persistent+scrollback + live release)"
      contains: "create"
    - path: .planning/milestones/v1.0-phases/03-reproducible-workers/03-HUMAN-UAT.md
      provides: "superseded-by-v1.3-Phase-14 marker"
      contains: "superseded"
    - path: .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md
      provides: "superseded-by-v1.3-Phase-14 marker"
      contains: "superseded"
  key_links:
    - from: "14-ACCEPTANCE.md flip procedure"
      to: ".github/workflows harden-runner allowlist-prep comments (14-01)"
      via: "the discover-allowlist -> fill -> flip egress block -> re-run steps"
      pattern: "egress-policy"
    - from: "14-HUMAN-UAT.md H9 gate"
      to: "cc-worker-config/PRIMING.md STEP 4"
      via: "the five-step create->terminal->stop->start->destroy smoke"
      pattern: "destroy"
---

<objective>
Author the Phase 14 acceptance artifacts (the operator-run, human_needed slice of ACC-01/02/03):
a release/verify runbook (14-ACCEPTANCE.md) and a consolidated human-UAT checklist (14-HUMAN-UAT.md),
and mark the rolled-up Phase 03/04 HUMAN-UAT files superseded.

Purpose: this phase is acceptance, not feature code. CI never touches real Proxmox and this run does
not trigger a live release, so ACC-01/02/03 cannot be auto-passed. These docs give the operator the
exact, trap-aware procedure to run the real homelab smoke and the first live signed release, and to
flip the per-phase real-infra UAT items to passed. Verification of THIS plan is doc-review; the ACC
criteria themselves remain human_needed.

Output: new 14-ACCEPTANCE.md + 14-HUMAN-UAT.md; supersede markers on 03/04-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-first-real-infra-acceptance/14-CONTEXT.md
@.planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md

<interfaces>
<!-- Source material the runbook + checklist draw from (do NOT re-derive; quote/mirror): -->
<!-- release.yml :146-165 already documents the cosign verify + gh attestation verify commands. -->
<!-- cc-worker-config/PRIMING.md STEP 4 (:88-110) is the canonical H9 five-step gate. -->
<!-- 14-RESEARCH.md has the trap table (1+2 done; 3 exit-0 assertion; 4 audit->block allowlist). -->
<!-- The 4 already-correct facts: exactly 4 publish perms, verify-by-digest, cosign keyless, SHA-pins. -->
<!-- Project rules: NO em dashes; NO horizontal rules in .md bodies that the SPDX/lint forbids; SPDX header where the repo convention applies to planning md (match existing 03/04-HUMAN-UAT.md header style). -->

Allowlist endpoints (mirror from 14-01 / 14-RESEARCH): fulcio.sigstore.dev, rekor.sigstore.dev,
tuf.sigstore.dev, token.actions.githubusercontent.com, ghcr.io, github.com, objects.githubusercontent.com.

Verify commands (mirror from release.yml :146-165):
  cosign verify --certificate-identity-regexp 'https://github.com/<owner>/burrow/.*' \
    --certificate-oidc-issuer https://token.actions.githubusercontent.com \
    ghcr.io/<owner>/burrow-api@sha256:<digest>
  gh attestation verify oci://ghcr.io/<owner>/burrow-api@sha256:<digest> --owner <owner>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 14-ACCEPTANCE.md (release/verify runbook for ACC-02/03)</name>
  <read_first>
    - .planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md (the trap table + the automatable work sections)
    - .github/workflows/release.yml (the publish job + the verification runbook comment :146-165)
    - .github/workflows/ci.yml (the actionlint + allowlist-prep comments added by 14-01)
    - .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md (header/format style to match)
  </read_first>
  <action>
    Create `.planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md` matching the existing
    HUMAN-UAT header/frontmatter style (SPDX comment header if the sibling 04-HUMAN-UAT.md carries one;
    a frontmatter block with `status: pending`, `phase: 14-first-real-infra-acceptance`, `created`).
    Write the operator release/verify runbook with these sections, each step giving the EXACT command,
    the EXPECTED output, and an explicit PASS/FAIL line: (a) Preconditions: the v1.3 build is green on
    main, release-please has an open release PR. (b) First live release (ACC-02): merge the release-please
    PR -> it bumps the version, regenerates CHANGELOG, and pushes the `v*` tag -> the `v*` tag triggers
    release.yml. Expected: a version-bump commit + CHANGELOG entry + a `vX.Y.Z` tag + a green release.yml
    run. (c) Harden-runner audit->block flip (ACC-02 criterion 3): run the workflow once on the real tag
    with egress audit -> open the Step Security insights / the run annotations -> read the discovered
    egress allowlist -> fill `allowed-endpoints` from the placeholder (the 7 endpoints
    fulcio/rekor/tuf.sigstore.dev, token.actions.githubusercontent.com, ghcr.io, github.com,
    objects.githubusercontent.com, plus any the audit surfaced) -> flip `egress-policy: audit` to `block`
    in all five jobs -> re-run and confirm green. Expected: the block-mode run still signs + publishes.
    PASS only if block-mode is green. (d) actionlint gate (ACC-02): the live CI run executes the
    SHA-pinned actionlint step and passes. (e) GHCR publish + signature/provenance verify (ACC-03):
    give the cosign verify + gh attestation verify commands BY DIGEST (mirror release.yml :146-165),
    substituting the real `@sha256:<digest>` from the publish run. CRITICALLY document the trap:
    `gh attestation verify` can EXIT 0 even on a meaningless verification, so the operator MUST assert on
    the command OUTPUT (look for the explicit success/verified line and inspect the JSON), not just `$?`;
    pair it with `cosign verify` which fails loudly. Note verify-BY-DIGEST-not-tag (a tag is mutable; the
    digest is the immutable manifest). (f) A standing checklist confirming the already-correct invariants:
    exactly 4 publish perms (contents:read, packages:write, id-token:write, attestations:write), every
    third-party `uses:` SHA-pinned, SBOM (SPDX + CycloneDX) attached, cosign keyless sign + SLSA
    provenance bound to the digest. Use colons/commas, NEVER em dashes. Reference cc-worker-config and
    the release.yml line anchors where useful.
  </action>
  <acceptance_criteria>
    - `.planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md` exists with a frontmatter block (status + phase + created)
    - It contains the literal strings `cosign verify`, `gh attestation verify`, `@sha256:`, and `egress-policy: block`
    - It documents the gh-attestation exit-0 trap: the file contains both `exit` and `output` near an assertion instruction (assert on output, not exit code)
    - It lists the harden-runner allowlist endpoints including `fulcio.sigstore.dev` and `rekor.sigstore.dev`
    - It states the exactly-4-publish-perms invariant (the four perm names appear)
    - `grep -c $'—' 14-ACCEPTANCE.md` is 0 (no em dashes)
    - Each ACC item (ACC-02, ACC-03) has a command, an expected output, and a PASS/FAIL line
  </acceptance_criteria>
  <verify>
    <automated>cd E:/repos/burrow && f=.planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md && test -f "$f" && grep -q "cosign verify" "$f" && grep -q "gh attestation verify" "$f" && grep -q "@sha256:" "$f" && grep -q "egress-policy: block" "$f" && grep -q "fulcio.sigstore.dev" "$f" && grep -q "packages: write" "$f" && [ "$(grep -c $'—' "$f")" = "0" ] && echo OK</automated>
  </verify>
  <done>14-ACCEPTANCE.md gives the operator the trap-aware release/verify runbook for ACC-02/03, by digest, with the exit-0 assertion and the flip procedure; no em dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Author 14-HUMAN-UAT.md (consolidated ACC-01/02/03 checklist)</name>
  <read_first>
    - cc-worker-config/PRIMING.md (STEP 4 :88-110, the H9 five-step gate; preconditions :22-30)
    - .planning/milestones/v1.0-phases/03-reproducible-workers/03-HUMAN-UAT.md (the 5 pending items + the format)
    - .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md (the 5 pending items + the format)
    - .planning/REQUIREMENTS.md (ACC-01/02/03 exact criterion text)
  </read_first>
  <action>
    Create `.planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md` in the existing HUMAN-UAT
    format (the frontmatter `status: partial`, `phase`, `source`, `started`, `updated`; a `## Tests`
    list of `### N. <item>` with `expected:` + `result: [pending]`; a `## Summary` count; a `## Gaps`
    section). Consolidate ACC-01/02/03 as the operator checklist: (ACC-01) the H9 five-step homelab gate
    mirroring PRIMING.md STEP 4 (1 create a worker CT --full from a small repo+branch; 2 the terminal
    panel shows an interactive resizable claude session; 3 stop -> status stopped, LXC stopped, disk
    preserved; 4 start -> status running, terminal reconnects to the SAME session; 5 destroy -> LXC gone,
    VMID freed, row soft-deleted), PLUS reaper destroys a real injected orphan LXC and frees its VMID,
    idle auto-stop after the real window, capacity holds under real concurrent creates, and real
    least-loaded node selection; (ACC-01, the WSX-02/03 live proof) a real PERSISTENT workspace survives
    a real stop->start with its disk intact AND its terminal scrollback restored on reconnect (the tmux
    -A reattach), and confirm the reaper never destroys a persistent stopped workspace on real CTs;
    (ACC-02) the first live release-please PR merges, the v* tag triggers release.yml, harden-runner is
    flipped to block with the discovered allowlist, and actionlint passes on the live runner; (ACC-03)
    real GHCR publish + cosign verify + gh attestation verify pass by digest (point to 14-ACCEPTANCE.md
    for the commands). Each item: `expected:` describes the observable PASS; `result: [pending]`. Add a
    note that this consolidates and supersedes the still-pending Phase 03 + Phase 04 HUMAN-UAT items
    (list which item maps where). Use colons/commas, NEVER em dashes.
  </action>
  <acceptance_criteria>
    - `.planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md` exists with `status:` frontmatter
    - It contains the five H9 verbs: `create`, `stop`, `start`, `destroy`, and `scrollback`
    - It contains `persistent` and `reaper` (the WSX-02/04 live proofs) and `cosign`/`attestation` (ACC-03)
    - Every test item carries a `result: [pending]` (none pre-marked passed)
    - It references superseding Phase 03 + Phase 04 HUMAN-UAT (`grep -q "03-HUMAN-UAT"` and `04-HUMAN-UAT` or `Phase 03`/`Phase 04`)
    - `grep -c $'—' 14-HUMAN-UAT.md` is 0 (no em dashes)
  </acceptance_criteria>
  <verify>
    <automated>cd E:/repos/burrow && f=.planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md && test -f "$f" && grep -qi "create" "$f" && grep -qi "destroy" "$f" && grep -qi "scrollback" "$f" && grep -qi "persistent" "$f" && grep -qi "cosign" "$f" && grep -q "result: \[pending\]" "$f" && [ "$(grep -c $'—' "$f")" = "0" ] && echo OK</automated>
  </verify>
  <done>14-HUMAN-UAT.md is the single consolidated ACC-01/02/03 operator checklist (H9 + persistent/scrollback + live release), all items pending, no em dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 03/04 HUMAN-UAT superseded by v1.3 Phase 14</name>
  <read_first>
    - .planning/milestones/v1.0-phases/03-reproducible-workers/03-HUMAN-UAT.md
    - .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md
  </read_first>
  <action>
    Add a short note near the top of BOTH `03-HUMAN-UAT.md` and `04-HUMAN-UAT.md` (after the frontmatter,
    before the test list) stating that these pending items are rolled up into and superseded by
    `.planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md` (the v1.3 Phase 14 consolidated
    real-infra acceptance gate), so the operator runs them from the Phase 14 checklist. Do NOT delete the
    original items (keep the history). Do not change their `result:` values. Use colons/commas, NEVER em
    dashes. Keep the existing SPDX/frontmatter intact.
  </action>
  <acceptance_criteria>
    - Both 03-HUMAN-UAT.md and 04-HUMAN-UAT.md contain the word `superseded` and a reference to Phase 14 (`14-HUMAN-UAT` or `Phase 14`)
    - The original test items and their `result:` values are unchanged (the note is additive)
    - `grep -c $'—'` is 0 in both files
  </acceptance_criteria>
  <verify>
    <automated>cd E:/repos/burrow && for f in .planning/milestones/v1.0-phases/03-reproducible-workers/03-HUMAN-UAT.md .planning/milestones/v1.0-phases/04-hardening-release/04-HUMAN-UAT.md; do grep -qi "superseded" "$f" && grep -qi "Phase 14\|14-HUMAN-UAT" "$f" && [ "$(grep -c $'—' "$f")" = "0" ] || { echo "FAIL $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Both prior HUMAN-UAT files point to the consolidated Phase 14 checklist; items preserved; no em dashes.</done>
</task>

</tasks>

<threat_model>
Trust boundary: these are planning/acceptance docs; they add NO runtime attack surface and NO new
ingress. The one security-relevant content is the release-verify procedure.

| ID | Threat | STRIDE | Asset | Disposition | Mitigation |
|----|--------|--------|-------|-------------|------------|
| T-14-01 | A meaningless `gh attestation verify` exits 0 and the operator ships an unverified image | Spoofing / Tampering | the published release image | mitigate | The runbook documents the exit-0 trap and requires asserting on OUTPUT, paired with `cosign verify` (fails loudly) and verify-by-digest-not-tag |
| T-14-02 | A premature harden-runner `block` flip with a wrong allowlist breaks keyless signing or hides a needed egress | Denial of Service | the signed release pipeline | mitigate | The runbook flips to block ONLY after discovering the real allowlist from a live audit run, then re-runs to confirm green; egress stays `audit` in-repo until then |
| T-14-03 | Docs leak a secret (token) | Information Disclosure | the PVE token | accept/mitigate | No secret appears in these docs; the PVE token stays `.env`-only (Phase 12 gate); the runbook references it by name only |

No unmitigated HIGH threat. The publish path is already hardened (4 perms, keyless, SHA-pins, by-digest);
this plan documents how to verify it, it does not weaken it.
</threat_model>

<verification>
Doc-review + the structural greps in each task's `<automated>`. ACC-01/02/03 themselves are MANUAL-ONLY
(operator-run on real infra) and remain `human_needed` after this plan; this plan only delivers the
artifacts that let the operator run and record them. Phase 14 verification lands `human_needed` by design.
</verification>
