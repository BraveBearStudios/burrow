---
phase: 14-first-real-infra-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/workflows/release-please.yml
autonomous: true
requirements: [ACC-02]
must_haves:
  truths:
    - "ci.yml's static-gates job runs a SHA-pinned actionlint step early, before the language gates, so a workflow schema/expression error fails the build fast"
    - "All five harden-runner steps keep egress-policy: audit (no functional egress change) and each carries a commented placeholder allowed-endpoints allowlist plus a pointer to the audit->block flip procedure"
    - "Every workflow still parses as valid YAML and every third-party uses: stays pinned to a 40-hex commit SHA"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "actionlint static-gate step + harden-runner allowlist-prep comments"
      contains: "actionlint"
    - path: .github/workflows/release.yml
      provides: "harden-runner allowlist-prep comment on the publish job (publish path otherwise unchanged)"
      contains: "egress-policy: audit"
    - path: .github/workflows/release-please.yml
      provides: "harden-runner allowlist-prep comment on the release-please job"
      contains: "egress-policy: audit"
  key_links:
    - from: ".github/workflows/ci.yml static-gates"
      to: "actionlint binary on the GitHub Linux runner"
      via: "SHA-pinned actionlint action step"
      pattern: "actionlint@[0-9a-f]{40}"
    - from: "harden-runner audit comments"
      to: "14-ACCEPTANCE.md flip procedure"
      via: "commented allowed-endpoints placeholder (Fulcio/Rekor/TUF/OIDC/GHCR)"
      pattern: "fulcio\\.sigstore\\.dev"
---

<objective>
Add the automatable CI-hardening slice of Phase 14 (ACC-02): wire a SHA-pinned `actionlint`
step into `ci.yml`'s static-gates job, and add commented `harden-runner` allowlist-prep
across all five CI/release jobs without changing egress behavior.

Purpose: actionlint catches GitHub Actions workflow schema/expression misconfiguration on the
live runner; the allowlist-prep pre-seeds the cosign/OIDC/GHCR endpoints the operator will need
when they discover the real allowlist and flip `egress-policy: audit`->`block` (ACC-02 criterion 3).
This is the only code/CI change in Phase 14 — everything else is acceptance docs.

Output: edited `.github/workflows/ci.yml`, `release.yml`, `release-please.yml`. The actionlint
RUN itself is proven by the first live CI run (actionlint is not installable on the Windows dev
box — Phase 8 RELX-02). Local verification is STRUCTURAL only: workflows still parse, the
actionlint step is present and SHA-pinned, SHA-pins hold, egress stays audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-first-real-infra-acceptance/14-CONTEXT.md
@.planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md

<interfaces>
<!-- The harden-runner SHA-pin convention already used across all three workflows. -->
<!-- Every third-party uses: is `<owner>/<action>@<40-hex-sha> # vX.Y.Z`. -->
<!-- Do NOT change any existing SHA; only ADD the actionlint uses: + comments. -->

harden-runner step (identical in all 5 jobs — ci.yml :30/:121/:155, release.yml :59, release-please.yml :44):
  uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
  with:
    egress-policy: audit

Allowlist endpoints the cosign/OIDC/GHCR publish path needs once egress flips to block:
  fulcio.sigstore.dev          (keyless signing cert)
  rekor.sigstore.dev           (transparency log)
  tuf.sigstore.dev             (Sigstore TUF root)
  token.actions.githubusercontent.com   (GitHub OIDC for keyless + attest)
  ghcr.io                      (image + provenance/SBOM push)
  github.com                   (checkout / action downloads)
  objects.githubusercontent.com (release asset / blob fetch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SHA-pinned actionlint step to ci.yml static-gates</name>
  <read_first>
    - .github/workflows/ci.yml (the static-gates job, steps :28-109; harden-runner :29-32, checkout :34-35)
    - .planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md (section "1. actionlint in CI (ACC-02)" :30-36)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, inside the `static-gates` job, add an `actionlint` step that runs
    EARLY and fails fast: place it immediately after the `Checkout` step (after :35) so a workflow
    schema/expression error fails before the slower uv/npm install gates. Use a SHA-pinned third-party
    actionlint action — `rhysd/actionlint` — pinned to a full 40-hex commit SHA with a trailing
    `# vX.Y.Z` comment recording the human-readable version, matching the repo's existing SHA-pin
    convention (see the harden-runner and checkout lines for the exact format). Resolve the current
    `rhysd/actionlint` release tag to its commit SHA at implementation time via the GitHub API
    (`gh api repos/rhysd/actionlint/git/refs/tags/<tag>` or the releases page) and pin that SHA — do
    NOT use a floating tag or `@v1`. Name the step `actionlint · workflow schema/expression lint`.
    Add a brief leading comment explaining that actionlint validates GitHub Actions workflow syntax,
    runs on the GitHub Linux runner only (unavailable on the Windows dev box per Phase 8 RELX-02),
    and is fail-fast in static-gates. Do NOT change any other step, the job's permissions, or the
    harden-runner egress policy in this task.
  </action>
  <acceptance_criteria>
    - ci.yml's static-gates job contains a step named `actionlint · workflow schema/expression lint`
    - The actionlint `uses:` is pinned to a 40-hex commit SHA (regex `actionlint@[0-9a-f]{40}` matches) with a trailing `# v` version comment
    - The actionlint step is positioned after Checkout and before the `Install uv` step (fail-fast / early)
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (the workflow still parses)
    - No existing step, permission, or harden-runner block in ci.yml was modified
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); steps=d['jobs']['static-gates']['steps']; names=[s.get('name','') for s in steps]; uses=[s.get('uses','') for s in steps]; import re; assert any('actionlint' in n for n in names), 'no actionlint step'; assert any(re.search(r'actionlint@[0-9a-f]{40}', u) for u in uses), 'actionlint not SHA-pinned'; print('OK')"</automated>
  </verify>
  <done>ci.yml static-gates runs a SHA-pinned, fail-fast actionlint step after checkout; the workflow parses; no other step changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add harden-runner allowlist-prep comments to all five jobs</name>
  <read_first>
    - .github/workflows/ci.yml (harden-runner steps at :29-32, :120-123, :154-157)
    - .github/workflows/release.yml (harden-runner :58-61; do NOT touch the publish steps :63-144 — already correct per 14-RESEARCH "Already-correct")
    - .github/workflows/release-please.yml (harden-runner :43-46)
    - .planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md (section "2. harden-runner allowlist-prep + flip doc" :38-46)
  </read_first>
  <action>
    In each of the five harden-runner steps (ci.yml static-gates/pr-title/build-scan, release.yml
    publish, release-please.yml release-please), KEEP `egress-policy: audit` exactly as-is — do NOT
    set `block`. Directly below each `egress-policy: audit` line, add a commented placeholder
    `allowed-endpoints` block (YAML comment lines, so the policy stays audit and the comment is
    inert) listing the cosign/OIDC/GHCR endpoints the block-flip will need: `fulcio.sigstore.dev`,
    `rekor.sigstore.dev`, `tuf.sigstore.dev`, `token.actions.githubusercontent.com`, `ghcr.io`,
    `github.com`, `objects.githubusercontent.com`. End each block with a one-line comment pointing to
    the flip procedure: that the operator discovers the real allowlist from the first live audit run's
    Step Security insights, fills `allowed-endpoints`, then flips `egress-policy` to `block` — see
    `.planning/phases/14-first-real-infra-acceptance/14-ACCEPTANCE.md`. Keep the comment concise on
    the publish job (release.yml) and note there that the Fulcio/Rekor/TUF endpoints are the
    keyless-signing-critical ones. Do NOT modify any `uses:` SHA, any step logic, the publish job's
    perms, or the verification runbook comment at the bottom of release.yml.
  </action>
  <acceptance_criteria>
    - All five harden-runner steps still have `egress-policy: audit` (no `block` anywhere)
    - Each of the three workflow files contains the commented endpoints `fulcio.sigstore.dev`, `rekor.sigstore.dev`, `tuf.sigstore.dev`, and `token.actions.githubusercontent.com`
    - Each allowlist-prep comment block references the 14-ACCEPTANCE.md flip procedure
    - All three workflows parse: `yaml.safe_load` succeeds on each
    - The release.yml publish steps (build/push, SBOM, cosign sign, attest) and its 4-permission block are byte-unchanged
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml,glob,re; files=['.github/workflows/ci.yml','.github/workflows/release.yml','.github/workflows/release-please.yml']; [yaml.safe_load(open(f)) for f in files]; texts={f:open(f).read() for f in files}; assert all('egress-policy: block' not in t for t in texts.values()), 'block leaked'; assert all('egress-policy: audit' in t for t in texts.values()), 'audit missing'; assert all('fulcio.sigstore.dev' in t and 'rekor.sigstore.dev' in t and 'tuf.sigstore.dev' in t and 'token.actions.githubusercontent.com' in t for t in texts.values()), 'allowlist endpoints missing'; rel=texts['.github/workflows/release.yml']; assert 'packages: write' in rel and 'id-token: write' in rel and 'attestations: write' in rel, 'publish perms changed'; print('OK')"</automated>
  </verify>
  <done>All five harden-runner steps keep egress audit with a commented Fulcio/Rekor/TUF/OIDC/GHCR allowlist-prep block pointing at the flip runbook; publish path untouched; all workflows parse.</done>
</task>

<task type="auto">
  <name>Task 3: SHA-pin + parse + SPDX structural sweep</name>
  <read_first>
    - .github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/release-please.yml (all three, post-edit)
    - .planning/phases/14-first-real-infra-acceptance/14-RESEARCH.md (section "Validation Architecture" :74-80 — structural-only contract; do NOT claim actionlint passed locally)
  </read_first>
  <action>
    Run the structural validation gate that stands in for the (unavailable-locally) actionlint run.
    Confirm all three workflows `yaml.safe_load` cleanly; confirm every third-party `uses:` across the
    three files (including the newly added actionlint) is pinned to a 40-hex commit SHA — no `@v1`,
    no `@main`, no floating ref; and confirm the SPDX header gate passes via
    `uvx --with charset-normalizer reuse lint` (the repo's established SPDX lint, per ci.yml :108-109).
    The two edited-but-unchanged-header workflow files must stay reuse-compliant. If any `uses:` is not
    a full SHA, fix the pin (resolve the tag to its commit SHA). Record in the SUMMARY that the
    actionlint step's live RUN is intentionally deferred to the first GitHub CI run (it cannot execute
    on the Windows dev box) — the local proof is structural: parse-clean, SHA-pinned, SPDX-clean.
  </action>
  <acceptance_criteria>
    - All three workflows parse with `yaml.safe_load`
    - Every `uses:` line across the three workflows matches `uses: \S+@[0-9a-f]{40}` (full-SHA pinned)
    - `uvx --with charset-normalizer reuse lint` reports the repo SPDX-compliant (exit 0)
    - The SUMMARY frames the actionlint proof as structural-local + live-CI, NOT "actionlint passed locally"
  </acceptance_criteria>
  <verify>
    <automated>python -c "import glob,re; bad=[]; [bad.append((f,l.strip())) for f in glob.glob('.github/workflows/*.yml') for l in open(f) if re.search(r'uses:\s*\S+@', l) and not re.search(r'uses:\s*\S+@[0-9a-f]{40}', l)]; assert not bad, f'unpinned uses: {bad}'; print('all uses SHA-pinned')"</automated>
  </verify>
  <done>All three workflows parse, every `uses:` is a full 40-hex SHA, `reuse lint` is clean; the SUMMARY records the structural-local + live-CI proof contract for actionlint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-hosted runner -> internet egress | The CI/release jobs make outbound calls (action downloads, GHCR push, Sigstore/OIDC); harden-runner is the egress control point. |
| Third-party `uses:` action -> runner | Each marketplace action runs with the job's permissions; a compromised/typo-squatted action could exfiltrate the GITHUB_TOKEN or tamper with the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | new `rhysd/actionlint` action in ci.yml | mitigate | Pin to a full 40-hex commit SHA (not a floating tag); structural verify asserts the `@[0-9a-f]{40}` pin holds across all `uses:`. |
| T-14-02 | Tampering | harden-runner egress allowlist-prep | accept | Comment-only change; `egress-policy` stays `audit` (no functional egress change). The actual `block` flip with the discovered allowlist is the operator-run ACC-02 step on the live runner. |
| T-14-03 | Elevation of Privilege | ci.yml static-gates / publish job permissions | accept | No permission change in this plan; the publish job's exactly-4 scopes (Pitfall 7) are byte-unchanged and re-asserted by the Task 2 verify. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (workflow-edit + comment only); no `## Package Legitimacy Audit` required. The one new dependency is a SHA-pinned GitHub Action, covered by T-14-01. |
</threat_model>

<verification>
Structural-only (the real actionlint/egress gates are operator-run on the live runner):
- All three workflows parse: `python -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('.github/workflows/*.yml')]"`.
- ci.yml static-gates contains a SHA-pinned actionlint step, positioned fail-fast after Checkout.
- All five harden-runner steps keep `egress-policy: audit` with a Fulcio/Rekor/TUF/OIDC/GHCR allowlist-prep comment.
- Every `uses:` is a full 40-hex SHA; `uvx --with charset-normalizer reuse lint` is clean.
- release.yml publish path (4 perms, by-digest, cosign/SBOM/attest) is byte-unchanged.

**Human-needed (deferred to the operator / first live CI run):** actionlint's actual lint RUN on the
GitHub Linux runner; the harden-runner `audit`->`block` flip with the discovered real allowlist.
These are NOT automatable here and are captured in 14-02's runbook (ACC-02).
</verification>

<success_criteria>
- ci.yml static-gates runs a SHA-pinned, fail-fast actionlint step (ACC-02 actionlint half, structurally landed).
- All five harden-runner steps carry the commented Fulcio/Rekor/TUF/OIDC/GHCR allowlist-prep + flip pointer; egress stays audit.
- All three workflows parse; every `uses:` is SHA-pinned; SPDX clean.
- The already-correct publish path (4 perms, by-digest, cosign keyless + SBOM + SLSA attest) is untouched.
</success_criteria>

<output>
Create `.planning/phases/14-first-real-infra-acceptance/14-01-SUMMARY.md` when done.
Record: the actionlint action chosen + its pinned SHA; that the actionlint RUN is deferred to live
CI (structural-local proof only); the allowlist endpoints seeded; that the publish path was untouched.
</output>
