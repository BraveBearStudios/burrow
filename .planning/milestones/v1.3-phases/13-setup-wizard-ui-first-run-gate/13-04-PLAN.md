---
phase: 13-setup-wizard-ui-first-run-gate
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - ui/tests/e2e/setup-wizard.spec.ts
autonomous: true
requirements: [SETUP-06]
must_haves:
  truths:
    - "An unconfigured Burrow (setupCompletedAt null) shows the SetupWizard gate before the workspace list"
    - "Walking the four steps over the Fake and creating the first workspace marks setup complete"
    - "After complete, the gate vanishes and the workspace list is shown"
    - "A configured Burrow (setupCompletedAt set) skips the gate and lands on the workspace list"
  artifacts:
    - path: "ui/tests/e2e/setup-wizard.spec.ts"
      provides: "Playwright gate-flow journey over the Fake"
      contains: "Set up Burrow"
  key_links:
    - from: "ui/tests/e2e/setup-wizard.spec.ts"
      to: "/api/v1/setup/state + /api/v1/setup/complete"
      via: "page.request precondition + UI walkthrough"
      pattern: "setup/state|setup/complete"
---

<objective>
Add the Tier-3 Playwright journey `ui/tests/e2e/setup-wizard.spec.ts` proving the first-run
gate end-to-end over the FakeComputeProvider + stub-ttyd harness: an unconfigured Burrow shows
the wizard, the operator walks the four steps and creates the first workspace, setup is marked
complete, the gate vanishes to the workspace list, and a configured Burrow skips the gate.

Purpose: SETUP-06 (the gate is real in a browser) — only Chromium proves the full
state → gate → walk → complete → gate-off cycle that vitest (jsdom/MSW) cannot (real cross-origin
proxy, real ~poll-free invalidation, the real built SPA). This is the phase e2e gate.
Output: setup-wizard.spec.ts, run via `cd ui && npm run test:e2e -- setup-wizard`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-setup-wizard-ui-first-run-gate/13-UI-SPEC.md
@.planning/phases/13-setup-wizard-ui-first-run-gate/13-RESEARCH.md

<harness_notes>
<!-- The shared Playwright harness + the shared-DB setup-state concern. -->

From ui/playwright.config.ts:32-69:
- The Fake control plane boots with `BURROW_COMPUTE=fake`, `BURROW_DB=sqlite`, `DATABASE_PATH=./burrow-e2e.db` (a runtime SQLite file the server migrates on boot; migration 003 seeds settings.setupCompletedAt NULL on a fresh DB).
- `fullyParallel: false`; each spec file runs serially; the Fake DB persists across tests within a run.
- BASE_URL is http://localhost:4173 (vite preview over the built ui/dist — `npm run build` must have produced dist with the Plan 03 wizard).

CRITICAL shared-DB concern (why this test must control its own precondition):
- setup-state is a SINGLE shared row in the shared burrow-e2e.db. Marking setup complete is global and persists. The OTHER e2e suites (stop-start, terminal, activity-drawer) create workspaces and therefore require the gate to be OFF (setupCompletedAt set) — or they would be blocked by the wizard.
- Therefore this test MUST NOT assume a pristine state and MUST leave a deterministic state. The robust contract: the test (a) establishes its own preconditions via direct API calls, (b) asserts the gate behavior, and (c) leaves setup COMPLETE so sibling suites are unblocked.
- There is NO setup-reset endpoint. To exercise the unconfigured→gate path deterministically the test reads the live state first; if already complete, it still proves the configured-skips-gate path, and the gate path is proven by the deterministic complete→gate-vanishes assertion. (See the action for the exact ordering that works regardless of DB starting state.)

From ui/tests/e2e/stop-start.spec.ts:30-70:
- `test.describe.configure({ mode: "serial" })`; unique names per run via `Date.now()`.
- `page.request.get("/api/v1/workspaces")` reads the live list; `page.request.post(...)` hits the API directly. Use `page.request` for the setup-state precondition/assertion alongside the UI walkthrough.
- the create modal fields: `#ws-name`, `#ws-repo`, `#ws-branch`, the Node select (Auto default); the modal is `getByRole("dialog")`.

From 13-UI-SPEC.md copy (assert against these):
- gate dialog aria-label / header `Set up Burrow`; step CTAs `Validate connection` / `Verify template` / `Re-check` / `Create workspace`.
- the Fake testConnection returns success=true + missingPrivileges [] (per the integration tests), verifyTemplate returns usable=true, health is degrade-not-500 db/compute ok over the Fake — so the happy walkthrough auto-advances cleanly.
</harness_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright gate-flow journey (unconfigured → walk → complete → gate off; configured skips)</name>
  <files>ui/tests/e2e/setup-wizard.spec.ts</files>
  <read_first>
    - ui/tests/e2e/stop-start.spec.ts:1-100 (the serial-mode + page.request + unique-name + per-test isolation idioms — model on this)
    - ui/playwright.config.ts:32-69 (the Fake 3-process harness + DATABASE_PATH=./burrow-e2e.db + BASE_URL)
    - .planning/phases/13-setup-wizard-ui-first-run-gate/13-UI-SPEC.md (the gate copy + step CTAs to assert)
  </read_first>
  <action>
    Create ui/tests/e2e/setup-wizard.spec.ts (SPDX two-line header, biome tabs/double-quotes), `test.describe.configure({ mode: "serial" })`, unique workspace name via `Date.now()`. Structure the spec to be deterministic regardless of the shared burrow-e2e.db starting state:

    Test A — "configured Burrow skips the gate": FIRST, via `page.request.post("/api/v1/setup/complete")`, ensure setup is marked complete (idempotent — safe whether or not it was already set). Then `page.goto("/")`, and assert the normal shell renders (e.g. the `New workspace` button / the workspace list landmark is visible) and the `Set up Burrow` dialog is NOT present (`getByRole("dialog", { name: /Set up Burrow/ })` has count 0). This proves SETUP-06's configured-skip path and leaves the DB in the complete state sibling suites need.

    Test B — "unconfigured Burrow shows the gate, walking it completes setup and the gate vanishes": this test must FIRST drive the wizard from an unconfigured view. Since there is no reset endpoint, prove the gate via the UI walkthrough using a fresh browser context whose setup state is observed: read `page.request.get("/api/v1/setup/state")`; the gate-render assertion is conditional ONLY on a null state, but the COMPLETE→VANISH assertion is unconditional. Concretely: if `setupCompletedAt` is null (fresh DB / CI), `page.goto("/")` and assert the `Set up Burrow` dialog is visible BEFORE the workspace list; fill step 1 (Host `https://pve.lan:8006`, User `burrow@pve`, Token name `burrow-token`, Token value `dummy`), click `Validate connection`, assert auto-advance to `Verify template`; fill step 2 (Template VMID `9000`, Node `pve`), click `Verify template`, assert advance to step 3 health; click `Re-check` if needed and assert advance to step 4 `Create workspace`; fill the create fields (`#ws-name` unique name, `#ws-repo`, leave Node Auto), click `Create workspace`; then assert the gate VANISHES — the `Set up Burrow` dialog reaches count 0 and the workspace list / the new panel is visible — AND `page.request.get("/api/v1/setup/state")` now returns a non-null `setupCompletedAt`. If the live state is already non-null at the start (a re-run against a persisted local DB), skip the gate-visible/walkthrough portion (the gate is legitimately off) and instead assert directly: drive the gate via the complete→vanish contract is already covered by Test A, so log/skip the walkthrough — use `test.skip(stateNonNull, "DB already configured; gate walkthrough covered on a fresh CI DB")` for the gate-visible branch so the suite stays green on both a fresh and a persisted DB. Track and clean up the created workspace id in `afterAll`/`afterEach` via `page.request.delete("/api/v1/workspaces/<id>")` asserting `expect([200,404]).toContain(res.status())` (the 07r cleanup robustness pattern), id-scoped over the created name.

    Keep both tests order-independent and self-contained. Use accessible locators (getByRole dialog with the `Set up Burrow` name, getByRole button for the CTAs, `#ws-name`/`#ws-repo` for the create inputs, getByLabel for the step-1/2 fields). Do not introduce a new harness — reuse playwright.config.ts.
  </action>
  <verify>
    <automated>cd ui && npm run build && npx playwright test setup-wizard</automated>
  </verify>
  <acceptance_criteria>
    - `ui/tests/e2e/setup-wizard.spec.ts` exists with a configured-skips-gate test and an unconfigured-shows-gate→walk→complete→vanish test
    - the spec asserts the `Set up Burrow` dialog presence/absence and a non-null /setup/state after completion
    - the gate-visible/walkthrough branch is guarded so the suite is green on both a fresh (CI) DB and a persisted (local) DB
    - created workspaces are cleaned up id-scoped with `expect([200,404]).toContain(res.status())`
    - `cd ui && npm run build && npx playwright test setup-wizard` exits 0
  </acceptance_criteria>
  <done>The Playwright spec proves the gate end-to-end over the Fake (unconfigured shows it, walking completes it, the gate vanishes; configured skips it) and is deterministic against the shared DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → Fake control plane | the e2e harness drives real HTTP/WS over the Fake provider; no real Proxmox, no real secret |
| the wizard step-1 token field (in e2e) | a dummy token is typed; the Fake ignores it — no real credential in the harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information disclosure | the e2e token value | mitigate | The spec types only a literal dummy token (`dummy`) into the step-1 field — never a real PVE credential. The Fake provider ignores token values. No secret enters the repo, the harness, or a fixture (CLAUDE.md: no real tokens in tests/fixtures). |
| T-13-11 | Tampering | shared burrow-e2e.db setup row | accept | setup-state is a benign timestamp on a Fake-backed runtime DB; the test leaves it in the complete state that sibling suites require. No production data, no real infra. |
| T-13-SC | Tampering | npm installs | mitigate | No new npm packages — uses the existing @playwright/test already wired in the harness. No legitimacy checkpoint required. |

No unmitigated HIGH. Only a literal dummy token touches the harness; no real secret in the test.
</threat_model>

<verification>
- `cd ui && npm run build && npx playwright test setup-wizard` passes (both gate tests).
- `cd ui && npx biome check tests/e2e/setup-wizard.spec.ts` clean.
- The spec is green on a fresh CI DB and a persisted local DB (the gate-visible branch is guarded); created workspaces are cleaned up id-scoped.
- Running the full e2e suite (`npm run test:e2e`) stays green — the gate test leaves setup complete so sibling suites are unblocked.
</verification>

<success_criteria>
- `setup-wizard.spec.ts` proves SETUP-06 in a real browser: unconfigured → gate, walk the four steps → create → complete → gate vanishes to the workspace list; configured → no gate.
- The test is deterministic against the shared Fake DB (guarded gate-visible branch, idempotent complete precondition, id-scoped cleanup with the 07r `expect([200,404])` pattern).
- No real secret in the harness; only a literal dummy token is typed.
</success_criteria>

<output>
Create `.planning/phases/13-setup-wizard-ui-first-run-gate/13-04-SUMMARY.md` when done
</output>
