<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->
---
milestone: v1.3
audited: 2026-06-26
status: tech_debt
scores:
  requirements: 12/15 CI-met (3 deferred to real-infra acceptance)
  phases: 5/5
  integration: pass
  flows: pass
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 14-first-real-infra-acceptance
    items:
      - "ACC-01 (deferred to dev-homelab smoke): the H9 five-step gate (create->terminal->stop->start->destroy) + reaper/auto-stop/capacity/real least-loaded node selection on real CTs, and the real persistent stop->start with disk intact + scrollback restored (live proof of WSX-02/03 + reaper carve-out). Operator-run on real Proxmox; CI never touches real infra by design. Checklist: .planning/phases/14-first-real-infra-acceptance/14-HUMAN-UAT.md."
      - "ACC-02 (deferred to first live release): first release-please PR merge (version bump + changelog + v* tag), harden-runner egress audit->block with the operator-discovered allowlist, and actionlint passing on the live GitHub runner. Repo-side prep landed (SHA-pinned reviewdog/action-actionlint gate + commented allowlist-prep, egress stays audit). Runbook: 14-ACCEPTANCE.md."
      - "ACC-03 (deferred to first live release): real GHCR publish + cosign verify + gh attestation verify by @sha256 digest (verify-by-digest, assert-on-output for the gh-attestation exit-0 trap). Publish path already correct (4 perms, by-digest, cosign keyless, SHA-pins). Runbook: 14-ACCEPTANCE.md."
  - phase: cross-milestone
    items:
      - "Captured (not swept): host-prime signed-release delivery, the boot credential-leak test tautology (WR-01), and pre-existing ProxmoxProvider robustness nits (getNodeMemory KeyError, lock-substring) - all in .planning/todos/pending/."
      - "Minor supply-chain note: reviewdog/action-actionlint is SHA-pinned, but its inner runs.image is a floating docker tag - acceptable under the audit-egress posture; revisit if egress flips to block."
---

# v1.3 Go Live — Milestone Close-Out Audit

**Verdict: SHIPPED with the real-infra acceptance deferred** (status `tech_debt`), consistent with how
v1.0/v1.1/v1.2 closed — every prior milestone shipped its CI-provable scope with the dev-homelab smoke +
live release carried as tracked acceptance. The full ★ real-infra gate is recorded in
`14-HUMAN-UAT.md` + `14-ACCEPTANCE.md` for the operator to run on hardware.

## What shipped (5 phases, all CI-provable scope green)

| Phase | Delivered | Verification |
|-------|-----------|--------------|
| 10 Persistence Data Model + Reaper Carve-out | `003` migration (`settings` singleton + `persistent` column), reaper-never-reaps-a-persistent-stopped-workspace negative-control (proven RED-if-regressed), mocked-proxmoxer integration tier | passed 5/5 |
| 11 Scrollback Restore | worker-side `tmux new-session -A -s burrow` in burrow-boot.sh + baked `/etc/tmux.conf` (history-limit 50000 + window-size latest); hermetic argv + reattach proof; relay untouched | passed 5/5 |
| 12 Setup Wizard Backend | `testConnection`/`verifyTemplate` on Fake + Proxmox (ephemeral read-only client, 9-priv assertion), `/api/v1/setup/*`, SETUP-07 token gate (SecretStr + logger suppression + sentinel-leak test + 422 strip) | passed 5/5 |
| 13 Setup Wizard UI + First-Run Gate | `SetupWizard.tsx` full-page gate (4 auto-advancing steps, re-probe, complete-after-create guard, hard-gate a11y) + `persistent` checkbox + backend setter; e2e over the Fake | passed 6/6 |
| 14 First Real-Infra Acceptance | actionlint CI gate + harden-runner allowlist-prep (egress audit) + 14-ACCEPTANCE.md runbook + consolidated 14-HUMAN-UAT.md; ACC-01/02/03 themselves are operator-run | human_needed (deferred) |

ADRs authored in-phase: ADR-0011 (setup-state store), ADR-0012 (ComputeProvider setup caps), ADR-0013
(persistence model), ADR-0014 (tmux scrollback). Suites green over the Fake: api 250+, ui 128, boot 22,
setup e2e (fresh + configured DB), seam-leakage. Five code-review-fix passes landed, including the
SETUP-07 422 token-leak close and the WR-01 duplicate-workspace guard. Several pre-existing issues were
captured as todos rather than swept into scope.

## Requirements traceability

CI-met (12): WSX-02, WSX-03, WSX-04, TEST-01, TEST-02, SETUP-01, SETUP-02, SETUP-03, SETUP-04, SETUP-05,
SETUP-06, SETUP-07.
Deferred to real-infra acceptance (3): ACC-01, ACC-02, ACC-03 — the operator's homelab UAT + first live
signed release. This is the milestone's by-design human gate, not a gap in the built work.

## Integration + flows

The setup-wizard first-run gate, persistence stop/start, scrollback reattach, and the signed-release
path all integrate cleanly over the Fake provider and the hermetic harnesses. The end-to-end operator
flow (unconfigured Burrow -> wizard -> first workspace -> normal view, and create -> persistent
stop/start) is proven in vitest + Playwright. The only unproven-in-CI flows are the real-Proxmox + live
release ones, which are the ACC gate by design.

## Carry-forward

ACC-01/02/03 carry to the operator's first homelab acceptance run (not to a future milestone's code).
On pass, flip the `14-HUMAN-UAT.md` items and re-verify Phase 14. The captured todos
(host-prime delivery, credential-leak test, ProxmoxProvider nits) remain backlog.
