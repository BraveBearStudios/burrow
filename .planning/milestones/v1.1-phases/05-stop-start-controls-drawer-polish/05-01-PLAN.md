---
phase: 05-stop-start-controls-drawer-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/tests/msw/handlers.ts
  - ui/src/components/TerminalPanel.test.tsx
  - ui/src/components/WorkspaceLayout.test.tsx
  - ui/src/hooks/useTerminal.test.tsx
  - ui/src/components/ActivityDrawer.test.tsx
  - ui/tests/css-rules.test.ts
  - ui/tests/e2e/stop-start.spec.ts
autonomous: true
requirements: [UI-07, UI-08, UI-09, UI-10, UI-11]

must_haves:
  truths:
    - "Every gating / pending / round-trip unit test has a live MSW handler (no onUnhandledRequest error)"
    - "Failing-first tests exist for Stop/Start gating, pending, placeholder, the stopped hook gate, and the V2/V3/V4 CSS source rules"
    - "The e2e stop-start spec scaffold exists and is wired into the Playwright harness"
  artifacts:
    - path: "ui/tests/msw/handlers.ts"
      provides: "POST /workspaces/:id/stop and /start handlers returning the {data,meta,error} envelope"
      contains: "workspaces/:id/stop"
    - path: "ui/tests/css-rules.test.ts"
      provides: "CSS-source assertions for --w-drawer, the 375px media override, :focus-visible, and the scrollbar rules"
      contains: "--w-drawer"
    - path: "ui/tests/e2e/stop-start.spec.ts"
      provides: "Playwright create->stop->start journey + 375px drawer-width viewport test scaffold"
      contains: "stop-start"
  key_links:
    - from: "ui/src/components/TerminalPanel.test.tsx"
      to: "ui/tests/msw/handlers.ts"
      via: "MSW stop/start handler resolves the mutation"
      pattern: "Stop workspace|Start workspace"
    - from: "ui/tests/css-rules.test.ts"
      to: "ui/src/index.css"
      via: "readFileSync source assert"
      pattern: "readFileSync"
---

<objective>
Land the Wave 0 test infrastructure so every downstream feature test can run and fail-first. This plan adds the two missing MSW lifecycle handlers, the failing-first unit tests for UI-07..UI-11 (Stop/Start gating, pending, the stopped placeholder, the `useTerminal` stopped gate, and the V2/V3/V4 CSS-source rules), and the e2e spec scaffold.

Purpose: `vitest` runs with `onUnhandledRequest: "error"`, so any test that fires `POST /stop` or `/start` hard-fails on a missing handler. The feature tests must exist (RED) before the implementation plans (02, 03) turn them GREEN. This is the locked Wave 0 from 05-VALIDATION.md.
Output: 2 new MSW handlers, 1 new CSS-source test file, 1 new e2e spec scaffold, and added failing-first describes in three existing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-VALIDATION.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-RESEARCH.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from source. -->

From ui/tests/msw/handlers.ts (existing):
- `envelope<T>(data)` helper wraps `{ data, meta, error }` (line 16)
- `seedWorkspaces` already contains `ws-running` (status running) and `ws-stopped` (status stopped) (lines 29, 31, 61)
- the 404 not-found shape is inlined in the DELETE handler (lines 164-176) — reuse it verbatim
- handlers array exported as the default; add the two POST handlers before the GET /nodes line (187)

From ui/src/components/TerminalPanel.test.tsx (existing — EXTEND, do not recreate):
- `renderPanel(ui)` wraps in `QueryClientProvider` (lines 33-40) — reuse for every new render
- xterm/fit/css mocks at lines 27-29 — already in place
- existing describes: happy path (42), overlays (98), terminate confirm + detach (143)

From ui/src/hooks/useTerminal.test.tsx (existing — EXTEND):
- the MockWebSocket registry helper (`installMockWebSocket`, `WS.instances`, `.closed`, `emitOpen`) is the load-bearing seam for the stopped gate
- jsdom `offsetWidth` is 0 unless spied (existing pattern in the file)

From ui/src/components/ActivityDrawer.test.tsx (existing — EXTEND):
- existing describes: data states (99), badge map (131), poll gating (184), a11y (201)
- inline `.style.width` is readable in jsdom even though computed width is not

From ui/tests/e2e/terminal.spec.ts (analog for the e2e scaffold):
- `createWorkspace(page, name)` helper (lines 32-45)
- `waitForResponse` POST-assertion pattern + `test.describe.configure({ mode: "serial" })`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MSW stop/start handlers + the WorkspaceLayout mutation-spy tests</name>
  <files>ui/tests/msw/handlers.ts, ui/src/components/WorkspaceLayout.test.tsx</files>
  <read_first>
    - ui/tests/msw/handlers.ts (the file being modified — `envelope()` at 16, `seedWorkspaces` at 29, the DELETE handler + 404 shape at 162-184)
    - ui/src/components/WorkspaceLayout.test.tsx (the file being modified — the `Destroy issues DELETE` mutation-spy test + the `beforeEach` layout-store seed)
    - 05-PATTERNS.md "WorkspaceLayout.test.tsx — stop/start mutation integration test" (the `server.use` spy analog, lines 445-475) and "MSW envelope + seed" shared pattern
    - 05-RESEARCH.md "Stop/Start MSW handlers (Wave 0)" code example
  </read_first>
  <action>
    In `ui/tests/msw/handlers.ts`, add two handlers to the exported array, immediately before the `GET /api/v1/nodes` handler (currently line 187): `http.post("/api/v1/workspaces/:id/stop", …)` returns `envelope({ ...found, status: "stopped", stoppedAt: <fixed iso> })`, and `http.post("/api/v1/workspaces/:id/start", …)` returns `envelope({ ...found, status: "running", stoppedAt: null })`. Each looks up `seedWorkspaces.find(w => w.id === params.id)` and returns the same inlined 404 not-found envelope (code `not_found`, status 404) the DELETE handler uses when not found. Keep the SPDX header intact.
    In `ui/src/components/WorkspaceLayout.test.tsx`, add a describe block with two failing-first integration tests mirroring the existing `Destroy issues DELETE` mutation-spy: (a) seed `mosaicNode: "ws-running"` via `useLayoutStore.setState`, register a `server.use(http.post("/api/v1/workspaces/:id/stop", …))` that records the captured id, render the layout, click the `Stop workspace` button, `waitFor` the recorded id equals `ws-running`, and assert the panel stays mounted (NOT pruned — `screen.getByText` for its name still present); (b) the analogous `Start workspace` test seeding `mosaicNode: "ws-stopped"`. These reference the `Stop workspace` / `Start workspace` controls that Plan 02 will add, so they go RED now.
    Reuse the existing `beforeEach` (localStorage.clear, layout-store reset, installMockWebSocket, installMockResizeObserver, resetXtermMocks). Add an SPDX header only if the file lacks one (it already has one — do not duplicate).
  </action>
  <verify>
    <automated>cd ui && npm run typecheck && npm test -- WorkspaceLayout 2>&1 | grep -Ev '^#' | grep -Eq 'Stop workspace|Start workspace|stop|start'</automated>
  </verify>
  <acceptance_criteria>
    - `ui/tests/msw/handlers.ts` contains a `http.post(".../workspaces/:id/stop"` and a `.../start"` handler, each returning the `{data,meta,error}` envelope (stop→status "stopped", start→status "running") and the shared 404 shape on a missing id.
    - `ui/src/components/WorkspaceLayout.test.tsx` has two new tests asserting a click on `Stop workspace` / `Start workspace` fires `POST /workspaces/ws-running/stop` / `POST /workspaces/ws-stopped/start` (captured id asserted) and the panel stays mounted.
    - `cd ui && npm run typecheck` exits 0. The two new WorkspaceLayout tests are RED (the buttons don't exist yet — failing-first), which is the expected Wave 0 state, not a defect.
  </acceptance_criteria>
  <done>The stop/start MSW handlers exist; the WorkspaceLayout stop/start mutation tests exist and fail only because the buttons are not yet rendered (Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Failing-first TerminalPanel + useTerminal feature tests (gating, pending, placeholder, stopped gate)</name>
  <files>ui/src/components/TerminalPanel.test.tsx, ui/src/hooks/useTerminal.test.tsx</files>
  <read_first>
    - ui/src/components/TerminalPanel.test.tsx (the file being modified — `renderPanel` harness at 33-40, the terminate/detach interaction tests at 143-217 as the analog)
    - ui/src/hooks/useTerminal.test.tsx (the file being modified — the MockWebSocket registry harness + the existing running-status describes)
    - ui/src/hooks/useTerminal.ts (source of truth — the `status !== "running"` gate at 178, teardown at 288-302, `[workspaceId, status]` dep at 303)
    - 05-PATTERNS.md "TerminalPanel.test.tsx — Stop/Start + placeholder unit tests" + 05-RESEARCH.md "No-socket-when-stopped + teardown-on-flip" and "Gating + pending feedback" code examples
    - 05-UI-SPEC.md "Acceptance-Checkable Visual Criteria" 1, 3, 5, 6 (the contract the assertions encode)
  </read_first>
  <action>
    In `ui/src/components/TerminalPanel.test.tsx`, add a describe `TerminalPanel — Stop/Start controls + stopped placeholder (UI-07/UI-08)` with failing-first tests using `renderPanel`: (1) gating — render `status="running"` asserts `screen.getByRole("button", { name: "Stop workspace" })` present and `queryByRole … "Start workspace"` null; render `status="stopped"` asserts the inverse; loop `["creating","error","destroyed"]` asserting neither button present; (2) Stop fires immediately with no confirm — `fireEvent.click(getByRole button "Stop workspace")` calls the `onStop` prop once with the id and shows NO confirm overlay (contrast terminate); (3) pending — with the panel rendered in a pending state (drive via the `stopPending`/`startPending` prop Plan 02 will add), assert the button is `toBeDisabled()`, has `aria-busy="true"`, and a second `fireEvent.click` does not fire a second `onStop`; (4) placeholder — render `status="stopped"` asserts `getByText("Workspace stopped")`, the locked body copy, a `Start workspace` CTA, and `role="status"` on the placeholder region, AND that the connecting/reconnecting/error overlays are NOT present. Do NOT assert an immediate Stop→Start swap on click (optimistic-flip trap, Pitfall 1) — assert the mutation fired + the pending state only.
    In `ui/src/hooks/useTerminal.test.tsx`, add a describe `useTerminal — stopped gate (UI-07/UI-08)` with: (a) render the hook at `status="stopped"` and assert `WS.instances` has length 0 (no socket while stopped); (b) render at `status="running"`, `emitOpen` the socket, rerender at `status="stopped"`, assert the prior socket `.closed === true` and no new open socket exists (teardown on running→stopped); (c) rerender stopped→running and assert a fresh socket is constructed (reconnect). Use the existing MockWebSocket registry helper and the `offsetWidth` spy pattern already in the file.
    These tests reference props/render output Plan 02 adds, so they go RED. Keep the existing SPDX header.
  </action>
  <verify>
    <automated>cd ui && npm run typecheck && npm test -- useTerminal 2>&1 | grep -Ev '^#' | grep -Eq 'stopped|no socket|tears down|reconnect'</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/components/TerminalPanel.test.tsx` contains tests asserting: Stop present only when `status==="running"`, Start only when `status==="stopped"`, neither for creating/error/destroyed; Stop fires `onStop` once with no confirm; disabled + `aria-busy` while pending with no double-fire; the `Workspace stopped` placeholder (heading + locked copy + `Start workspace` CTA + `role="status"`) renders instead of the overlays.
    - `ui/src/hooks/useTerminal.test.tsx` contains tests asserting: 0 sockets while `status="stopped"`; the prior socket `.closed` after running→stopped with no new open socket; a fresh socket on stopped→running.
    - No test calls `getComputedStyle` for `:focus-visible`/scrollbar/media (jsdom boundary honored). No test asserts an immediate status swap on click.
    - `cd ui && npm run typecheck` exits 0; the new tests are RED pending Plan 02 (expected Wave 0 state).
  </acceptance_criteria>
  <done>TerminalPanel gating/pending/placeholder tests and useTerminal stopped-gate tests exist and fail only because Plan 02's render changes are not yet in.</done>
</task>

<task type="auto">
  <name>Task 3: CSS-source test (V2/V3/V4), ActivityDrawer width-token test, and the e2e scaffold</name>
  <files>ui/tests/css-rules.test.ts, ui/src/components/ActivityDrawer.test.tsx, ui/tests/e2e/stop-start.spec.ts</files>
  <read_first>
    - ui/tests/tokens.test.ts (the readFileSync CSS-source-assert precedent to clone for the new file)
    - ui/src/components/ActivityDrawer.test.tsx (the file being modified — the style-assertion tests + `renderDrawer` MSW harness)
    - ui/tests/e2e/terminal.spec.ts (analog for the new spec — `createWorkspace`, `waitForResponse`, serial mode) and ui/tests/e2e/activity-drawer.spec.ts (the open-drawer flow)
    - 05-RESEARCH.md "CSS-source asserts for V2/V3/V4", "Playwright: drawer full-width at 375px", "Playwright: stop→start round-trip" code examples
    - 05-UI-SPEC.md "Acceptance-Checkable Visual Criteria" 8, 9, 10 (the rule shapes to assert)
  </read_first>
  <action>
    Create `ui/tests/css-rules.test.ts` (SPDX header) following the `tokens.test.ts` `readFileSync(resolve(process.cwd(), "src/index.css"))` pattern, with failing-first source assertions: `--w-drawer: min(360px, 100vw)` declared; `@media (max-width: 375px)` block re-declaring `--w-drawer: 100vw` on `:root`; a global `:focus-visible` rule with `outline: 2px solid var(--accent-line)` and `outline-offset: 2px`; `::-webkit-scrollbar`/`-track`/`-thumb` rules with the thumb `background: var(--border-mid)`; and Firefox `scrollbar-width: thin` + `scrollbar-color: var(--border-mid) transparent`. Use tolerant whitespace regexes (per the research examples). These assert the rules Plan 03 will add, so they go RED.
    In `ui/src/components/ActivityDrawer.test.tsx`, add a failing-first test asserting the drawer `<aside>` (`getByRole("dialog")`) reads `width: var(--w-drawer)` via the readable inline `.style.width` (NOT computed width — jsdom cannot compute it). This goes RED until Plan 03 swaps the literal for the token.
    Create `ui/tests/e2e/stop-start.spec.ts` (SPDX header) scaffolding the Playwright journey, reusing the `createWorkspace` helper shape and `test.describe.configure({ mode: "serial" })`: (1) create→click `Stop workspace`→`waitForResponse` `POST …/workspaces/[^/]+/stop$`→assert `Workspace stopped` placeholder visible (poll-driven timeout)→click `Start workspace`→`waitForResponse` `…/start$`→assert placeholder hidden and `[data-testid^="term-"]` visible; (2) a `test.use({ viewport: { width: 375, height: 800 } })` block that opens the Activity drawer and asserts `drawer.boundingBox().width === 375`. The spec may be authored complete (it runs only in the Plan 04 e2e gate over the real built UI); it is the scaffold the phase e2e leg fills.
  </action>
  <verify>
    <automated>cd ui && npm run typecheck && npm test -- css-rules 2>&1 | grep -Ev '^#' | grep -Eq 'w-drawer|focus-visible|scrollbar'</automated>
  </verify>
  <acceptance_criteria>
    - `ui/tests/css-rules.test.ts` exists, reads `src/index.css` as a string, and asserts the V2 token + 375px media override, the V3 `:focus-visible` rule with `var(--accent-line)` + `outline-offset: 2px`, and the V4 `::-webkit-scrollbar-thumb` `var(--border-mid)` + Firefox `scrollbar-color: var(--border-mid) transparent`.
    - `ui/src/components/ActivityDrawer.test.tsx` has a test asserting the dialog's inline `.style.width === "var(--w-drawer)"`.
    - `ui/tests/e2e/stop-start.spec.ts` exists with a create→stop→start journey (POST `/stop` and `/start` asserted via `waitForResponse`) and a 375px-viewport drawer-width test (`boundingBox().width === 375`).
    - `cd ui && npm run typecheck` exits 0; css-rules + the ActivityDrawer width test are RED pending Plan 03 (expected Wave 0 state).
  </acceptance_criteria>
  <done>The CSS-source test, the ActivityDrawer width-token test, and the e2e stop-start spec all exist and the vitest ones fail only because Plan 03's CSS/style swap is not yet in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → MSW | the new stop/start handlers fabricate same-origin `/api/v1` responses; no real network |
| e2e spec → Fake provider + stub ttyd | Playwright drives the built SPA over `BURROW_COMPUTE=fake`; no real Proxmox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing (of true state) | MSW stop/start handlers | mitigate | handlers return the server-truth status (`stopped`/`running`) in the envelope; tests assert the mutation fired + pending only, never an optimistic Zustand flip (Pitfall 1) |
| T-05-02 | Tampering | new test files (no new dep) | mitigate | no `npm install`; the toolchain is already present (RESEARCH "Installation: none"); the `googleapis\|gstatic\|jsdelivr` source assert is unaffected |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package install occurs in this plan (Package Legitimacy Audit n/a per 05-RESEARCH); no `[ASSUMED]`/`[SUS]` package to gate |

**Block-on-HIGH:** no HIGH threats — this is a LAN-only, no-auth v1 frontend test-infra plan by design. No auth/session/crypto control is added (out of scope, CLAUDE.md).
</threat_model>

<verification>
- `cd ui && npm run typecheck` exits 0 (the new tests compile against the props/handlers they reference).
- `cd ui && npm run lint` (biome ci) passes on the new/changed files (SPDX header present, format clean).
- `cd ui && npm test -- WorkspaceLayout useTerminal css-rules` runs the new tests; the feature/CSS assertions are RED (failing-first) and the MSW handlers resolve (no `onUnhandledRequest` error). RED here is the expected Wave 0 outcome, not a failure of this plan.
</verification>

<success_criteria>
- Two MSW handlers (`/stop`, `/start`) return the envelope and the shared 404 shape.
- Failing-first tests exist for: Stop/Start gating, pending + no-double-fire, the stopped placeholder, the `useTerminal` stopped gate + teardown + reconnect, the V2/V3/V4 CSS source rules, and the ActivityDrawer width token.
- The `stop-start.spec.ts` e2e scaffold exists and is wired into the Playwright config (no harness change needed).
- `npm run typecheck` + `npm run lint` green; SPDX header on the two new files.
</success_criteria>

<output>
Create `.planning/phases/05-stop-start-controls-drawer-polish/05-01-SUMMARY.md` when done
</output>
