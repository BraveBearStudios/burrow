---
phase: 05-stop-start-controls-drawer-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - ui/src/index.css
  - ui/src/components/ActivityDrawer.tsx
autonomous: true
requirements: [UI-09, UI-10, UI-11]

must_haves:
  truths:
    - "On a phone-width viewport (<=375px) the activity drawer renders full-width; above it, the 360px panel"
    - "Keyboard-focusing any interactive control shows the --accent-line focus ring across all four themes"
    - "Scrollable surfaces (drawer list, terminal body, sidebar list) use the custom Burrow scrollbar, not the native one"
  artifacts:
    - path: "ui/src/index.css"
      provides: "--w-drawer token + 375px media override, global :focus-visible ring, global custom scrollbar"
      contains: "--w-drawer"
    - path: "ui/src/components/ActivityDrawer.tsx"
      provides: "Drawer width reads var(--w-drawer) instead of the inline literal"
      contains: "var(--w-drawer)"
  key_links:
    - from: "ui/src/components/ActivityDrawer.tsx"
      to: "ui/src/index.css"
      via: "width: var(--w-drawer) consumes the @theme token"
      pattern: "var\\(--w-drawer\\)"
    - from: "ui/src/index.css"
      to: "per-theme token blocks"
      via: ":focus-visible + scrollbar read --accent-line / --border-mid (defined per theme)"
      pattern: "var\\(--accent-line\\)|var\\(--border-mid\\)"
---

<objective>
Restore the three activity-drawer polish details the v1.0 UI review flagged (04-UI-REVIEW 22/24): the V2 phone full-width responsive sheet (UI-09), the V3 `--accent-line` focus ring (UI-10), and the V4 custom Burrow scrollbar (UI-11) — turning the Plan-01 CSS-source + drawer-width tests GREEN.

Purpose: these are pure global-CSS additions plus one inline-style swap; pseudo-class / media / pseudo-element rules cannot live inline, so they go in `index.css`, tokens only, rendering correctly across all four themes. No JS, no `matchMedia`, no new dependency.
Output: the `--w-drawer` token + 375px override, the global `:focus-visible` rule, the global custom scrollbar, and the `ActivityDrawer` width-token swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-CONTEXT.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-UI-SPEC.md
@.planning/phases/05-stop-start-controls-drawer-polish/05-PATTERNS.md

<interfaces>
<!-- Anchors to build against. Extracted from source. -->

From ui/src/index.css (the file being modified):
- the fixed-chrome dimensions block lives in `@theme` at lines 47-56 (`--w-sidebar: 228px`, `--w-modal: 400px`, etc.) — add `--w-drawer: min(360px, 100vw);` here (a dimension, not a color → NOT re-declared per theme)
- `--accent-line` and `--border-mid` are defined in EVERY theme block (`:root,[data-theme="dark"]` at ~97, plus dark-soft/medium/light) → one global rule renders correctly across all four
- `--radius-full` exists (~63) for the scrollbar thumb radius
- the existing `@media (prefers-reduced-motion: reduce)` block (~264-272) is the model for adding the `@media (max-width: 375px)` override and the placement of the new global rules (after it)
- NO `:focus-visible`, `::-webkit-scrollbar`, or drawer media query exist yet — all net-new

From ui/src/components/ActivityDrawer.tsx (the file being modified):
- `const DRAWER_WIDTH = "min(360px, 100vw)";` at line 40 (with a now-stale comment at 39)
- `drawerStyle` consumes `width: DRAWER_WIDTH` at line 83; sets `outline: "none"` on the `<aside>` at line 92
- the `<aside>` is `role="dialog"`, `tabIndex={-1}` (programmatic focus, not :focus-visible)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: --w-drawer token + 375px media override; swap ActivityDrawer to the token (UI-09 / V2)</name>
  <files>ui/src/index.css, ui/src/components/ActivityDrawer.tsx</files>
  <read_first>
    - ui/src/index.css (the file being modified — `@theme` fixed-chrome block 47-56, the `prefers-reduced-motion` media block ~264-272 as the media-query model)
    - ui/src/components/ActivityDrawer.tsx (the file being modified — `DRAWER_WIDTH` 39-40, `drawerStyle.width` 83)
    - 05-PATTERNS.md "index.css — --w-drawer token + media query" lines 316-345 and "ActivityDrawer.tsx — width token swap" lines 285-312
    - 05-UI-SPEC.md "§4 Responsive drawer width" + 05-RESEARCH.md Pitfall 6 (do NOT nest `@theme` under `@media`; override `:root` in plain CSS)
    - ui/tests/css-rules.test.ts + ui/src/components/ActivityDrawer.test.tsx (the Plan-01 RED tests this turns GREEN)
  </read_first>
  <action>
    In `ui/src/index.css`, add `--w-drawer: min(360px, 100vw);` to the fixed-chrome dimensions group inside the `@theme` block (alongside `--w-sidebar`/`--w-modal`). Add a plain-CSS media override (NOT nested under `@theme` — Tailwind v4 `@theme` is not nestable in `@media`): `@media (max-width: 375px) { :root { --w-drawer: 100vw; } }`, placed near the existing `prefers-reduced-motion` block. 375px is the locked phone breakpoint.
    In `ui/src/components/ActivityDrawer.tsx`, replace `width: DRAWER_WIDTH` (line 83) with `width: "var(--w-drawer)"`, remove the now-unused `DRAWER_WIDTH` const (or repurpose it), and correct/drop the stale width comment the 04-UI-REVIEW flagged. The component stays inline-style; the responsiveness lives in the token.
  </action>
  <verify>
    <automated>cd ui && npm run typecheck && npm test -- css-rules ActivityDrawer 2>&1 | grep -Ev '^#' | grep -Eq 'w-drawer|width'</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/index.css` contains `--w-drawer: min(360px, 100vw)` in `@theme` and a `@media (max-width: 375px) { :root { --w-drawer: 100vw; } }` override (not nested in `@theme`).
    - `ui/src/components/ActivityDrawer.tsx` sets `width: "var(--w-drawer)"` (no remaining `min(360px, 100vw)` literal in `drawerStyle`).
    - `cd ui && npm test -- css-rules ActivityDrawer` exits 0 for the V2 source assert + the dialog inline `.style.width === "var(--w-drawer)"` assert.
  </acceptance_criteria>
  <done>The drawer width is token-driven and responsive; the Plan-01 V2 source test + the ActivityDrawer width-token test are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: global :focus-visible ring + global custom scrollbar (UI-10 / V3, UI-11 / V4)</name>
  <files>ui/src/index.css, ui/src/components/ActivityDrawer.tsx</files>
  <read_first>
    - ui/src/index.css (the file being modified — per-theme token blocks defining `--accent-line`/`--border-mid` in all four themes ~97-184, `--radius-full` ~63, the `.burrow-mosaic` token-driven global-rule analog ~194-231)
    - ui/src/components/ActivityDrawer.tsx (the file being modified — `outline: "none"` on the `<aside>` at 92; verify it does not suppress child control rings)
    - 05-PATTERNS.md "index.css — global :focus-visible ring + custom scrollbar" (verbatim rule bodies) lines 349-399
    - 05-UI-SPEC.md "§5 focus ring" + "§6 scrollbar" (exact rules: 2px solid var(--accent-line)/offset 2px; thumb var(--border-mid) on transparent track; Firefox scrollbar-width thin / scrollbar-color)
    - 05-RESEARCH.md Pitfall 5 (do not broaden the `<aside>` outline:none to `*`); ui/tests/css-rules.test.ts (the Plan-01 RED V3/V4 assertions)
  </read_first>
  <action>
    In `ui/src/index.css`, after the `prefers-reduced-motion` block, add the global V3 rule: `:focus-visible { outline: 2px solid var(--accent-line); outline-offset: 2px; }` (use `:focus-visible`, NOT `:focus`, so a mouse click does not paint the ring). Add the global V4 custom scrollbar, tokens only: `::-webkit-scrollbar { width: 8px; height: 8px; }`, `::-webkit-scrollbar-track { background: transparent; }`, `::-webkit-scrollbar-thumb { background: var(--border-mid); border-radius: var(--radius-full); border: 2px solid transparent; background-clip: padding-box; }`, `::-webkit-scrollbar-thumb:hover { background: var(--text-muted); }`, and the Firefox `* { scrollbar-width: thin; scrollbar-color: var(--border-mid) transparent; }`. Thumb is `--border-mid` (a neutral) — never accent, never gold.
    In `ui/src/components/ActivityDrawer.tsx`, confirm the `<aside>` `outline: "none"` (line 92) does not suppress the new global `:focus-visible` ring on the controls inside the drawer (`:focus-visible` targets the focused element specifically; the bare `tabIndex={-1}` `<aside>` keeping `outline:none` is fine). If the `outline:none` is broader than the bare container, scope it so child control rings are preserved. Do NOT introduce a `* { outline: none }`.
  </action>
  <verify>
    <automated>cd ui && npm run typecheck && npm test -- css-rules 2>&1 | grep -Ev '^#' | grep -Eq 'focus-visible|scrollbar|accent-line'</automated>
  </verify>
  <acceptance_criteria>
    - `ui/src/index.css` contains a global `:focus-visible { outline: 2px solid var(--accent-line); outline-offset: 2px }` rule (using `:focus-visible`, not `:focus`).
    - `ui/src/index.css` contains `::-webkit-scrollbar-thumb` with `background: var(--border-mid)` + `::-webkit-scrollbar-track { background: transparent }` + Firefox `scrollbar-width: thin` / `scrollbar-color: var(--border-mid) transparent`; zero hex on the new rules.
    - `cd ui && npm test -- css-rules` exits 0 for the V3 + V4 source assertions; the `<aside>` `outline:none` is not broadened to suppress child rings.
    - `cd ui && npm test && npm run lint && npm run typecheck && npm run build` all green (wave merge).
  </acceptance_criteria>
  <done>The global focus ring and custom scrollbar ship in `index.css`, tokens only, across all four themes; the Plan-01 V3/V4 source tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static CSS → browser render | the new rules are global stylesheet additions; no JS path, no network, no input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | new global CSS (font/icon CDN) | mitigate | all rules are tokens-only inline CSS in `index.css` — no `@import`, no `url()`, no font/icon CDN; the `googleapis\|gstatic\|jsdelivr` source assert stays green |
| T-05-08 | (a11y consistency) | the `<aside>` outline:none vs the global ring | mitigate | keep `:focus-visible` un-scoped so it covers every control; the bare `tabIndex={-1}` `<aside>` outline:none does not suppress child rings; never introduce `* { outline: none }` (Pitfall 5) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package install (pure CSS + one inline-style swap; no new runtime dependency); legitimacy gate n/a |

**Block-on-HIGH:** no HIGH threats — LAN-only, no-auth v1 frontend CSS polish. No auth/session/crypto control added (out of scope, CLAUDE.md). The change touches no network surface and no secret.
</threat_model>

<verification>
- `cd ui && npm test && npm run lint && npm run typecheck && npm run build` all green: the Plan-01 V2/V3/V4 CSS-source tests and the ActivityDrawer width-token test are GREEN.
- `ui/src/index.css` adds zero hardcoded hex (the new rules read `var(--accent-line)`, `var(--border-mid)`, `var(--text-muted)`, `var(--radius-full)` only).
- No new `@import`/CDN in `index.css`; the global no-CDN assert in `tokens.test.ts` stays green.
- The live render (drawer width at 375px, the ring painting on Tab, the scrollbar rendering) is proven by the Plan 04 Playwright leg — these are NOT asserted in jsdom (boundary honored).
</verification>

<success_criteria>
- `--w-drawer` is `min(360px, 100vw)` by default and `100vw` under `@media (max-width: 375px)`; `ActivityDrawer` reads `width: var(--w-drawer)`.
- A single global `:focus-visible { outline: 2px solid var(--accent-line); outline-offset: 2px }` exists; one global custom scrollbar (thumb `--border-mid`, transparent track; Firefox `scrollbar-width: thin` / `scrollbar-color`) exists.
- Tokens only across all four themes; no hex, no gold, no CDN. All UI-09/UI-10/UI-11 source tests GREEN; SPDX header intact.
</success_criteria>

<output>
Create `.planning/phases/05-stop-start-controls-drawer-polish/05-03-SUMMARY.md` when done
</output>
