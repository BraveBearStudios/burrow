<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->
---
phase: 02-terminal-proxy-react-ui
plan: 06
type: execute
wave: 5
depends_on: ["02-03", "02-04", "02-05", "02-01"]
files_modified:
  - ui/tests/e2e/terminal.spec.ts
  - ui/tests/integration/restore.test.tsx
  - api/tests/integration/conftest.py
  - api/tests/e2e/stub_ttyd_server.py
  - ui/playwright.config.ts
  - docker-compose.e2e.yml
autonomous: true
requirements: [UI-02, UI-05]
must_haves:
  truths:
    - "Playwright drives the full journey over the FakeComputeProvider + stub ttyd: create → terminal echoes → split/drag → detach→reconnect → terminate"
    - "Terminate asks the confirm copy before removing the panel; detach is non-destructive (session preserved)"
    - "After a simulated refresh, a still-running workspace's panel re-mounts and its terminal reconnects to the live session (no scrollback)"
    - "The persisted Mosaic tree reconciles against the live workspace list on reload (gone leaves dropped)"
  artifacts:
    - path: "ui/tests/e2e/terminal.spec.ts"
      provides: "the full create→terminal→split→detach→reconnect→terminate Playwright journey"
      contains: "test("
    - path: "ui/tests/integration/restore.test.tsx"
      provides: "UI-05 restore-after-refresh integration test (reconnect live session, reconcile tree)"
      contains: "reconcile"
    - path: "api/tests/e2e/stub_ttyd_server.py"
      provides: "standalone protocol-accurate stub ttyd process for the Playwright/Fake e2e stack"
      contains: "tty"
  key_links:
    - from: "ui/tests/e2e/terminal.spec.ts"
      to: "the running UI over BURROW_COMPUTE=fake + stub ttyd"
      via: "Playwright webServer / docker-compose.e2e"
      pattern: "(webServer|baseURL)"
    - from: "ui/tests/integration/restore.test.tsx"
      to: "ui/src/store/layoutStore.ts reconcile + useTerminal reconnect"
      via: "persisted tree + fresh mount"
      pattern: "reconcile"
---

## Phase Goal

**As an** operator, **I want** the whole workflow — create, tile, detach/reconnect, and
terminate terminals, and have a running session survive a refresh — to work end to end, **so
that** Burrow is trustworthy for day-to-day multi-session supervision.

<objective>
The phase e2e gate (MVP "last task: e2e"): a Playwright journey over the FakeComputeProvider +
a protocol-accurate stub ttyd proving the full path create → terminal echoes → split/drag →
detach→reconnect → terminate, plus the UI-05 restore-after-refresh integration test
(reconnect a still-running workspace's live session + reconcile the persisted tree). This is
the cross-cutting acceptance layer that exercises every prior plan together.

Purpose: prove the slice end-to-end in CI without real infra, and lock the detach-vs-terminate
and restore-after-refresh behaviors that are easy to regress. Real ttyd `tty` handshake + a
live `claude` TUI remain the dev-homelab smoke gate (deferred, human_needed) — NOT this plan.
Output: `terminal.spec.ts` (Playwright), `restore.test.tsx` (vitest+MSW), a standalone stub
ttyd server for the e2e stack, and the finalized `playwright.config.ts` + `docker-compose.e2e.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-terminal-proxy-react-ui/02-RESEARCH.md
@.planning/phases/02-terminal-proxy-react-ui/02-UI-SPEC.md
@.planning/phases/02-terminal-proxy-react-ui/02-VALIDATION.md
@CLAUDE.md
@docs/ci-cd-and-testing.md

@.planning/phases/02-terminal-proxy-react-ui/02-03-SUMMARY.md
@.planning/phases/02-terminal-proxy-react-ui/02-04-SUMMARY.md
@.planning/phases/02-terminal-proxy-react-ui/02-05-SUMMARY.md

<interfaces>
<!-- The assembled app + the stub-ttyd pattern this plan drives. -->
The full App shell (Plan 05): Navbar (+ New workspace) / WorkspaceList sidebar / WorkspaceLayout
  (Mosaic) / StatusBar / NewWorkspaceModal. TerminalPanel header has split/detach/terminate buttons.
Detach (plug) = non-destructive, session survives (shows reconnecting overlay). Terminate (×) =
  WS-08 destroy, requires the confirm `Destroy {name}? …` before closePanel (UI-SPEC Copywriting).
Backend e2e stack: `BURROW_COMPUTE=fake` (FakeComputeProvider, hermetic) + a protocol-accurate
  stub ttyd (the bridge dials `ws://{lxc_ip}:7681/ws`; the Fake IP is `10.99.0.<vmid%256>`).
The Plan-01 `stub_ttyd_ws` fixture is in-process for pytest; the Playwright stack needs a
  STANDALONE stub-ttyd process the bridge can reach at the Fake's worker IP:7681.
UI-05: reconnect attaches to the LIVE PTY (no scrollback in v1 — honest blank-then-live). The
  persisted tree reconciles against the live list on load (drop gone leaves).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone stub ttyd for the e2e stack + restore-after-refresh integration test (UI-05)</name>
  <files>api/tests/e2e/stub_ttyd_server.py, api/tests/integration/conftest.py, ui/tests/integration/restore.test.tsx, docker-compose.e2e.yml</files>
  <read_first>
    02-RESEARCH.md "Protocol-accurate stub ttyd" + Pitfall 1/7 (no scrollback); 02-VALIDATION.md
    Test Infrastructure (Playwright over BURROW_COMPUTE=fake + stub ttyd) + Manual-Only table
    (real ttyd deferred); docs/ci-cd-and-testing.md §4.3-4.4 (FakeComputeProvider + stub ttyd, MSW);
    the Plan-01 in-process `stub_ttyd_ws` fixture (reuse its handler logic).
  </read_first>
  <behavior>
    - the standalone stub ttyd serves the SAME protocol-accurate handler as the Plan-01 fixture
      (negotiates `tty`, requires the JSON init, echoes `'0'`-prefixed) on a reachable host:port so
      the running bridge can dial it for the Fake's worker IP.
    - restore.test.tsx: given a persisted Mosaic tree (localStorage) + a live workspace list where
      one id is gone and one is still `running`, on app mount the gone leaf is dropped (reconcile)
      and the running panel re-mounts and opens a fresh WS (reconnect to the live session) — no scrollback restored.
  </behavior>
  <action>
    Factor the protocol-accurate stub-ttyd handler (from the Plan-01 `stub_ttyd_ws` fixture) into a
    standalone runnable `api/tests/e2e/stub_ttyd_server.py` (`websockets.serve` on a configurable
    host:port, `subprotocols=["tty"]`, JSON-init required, `'0'`-prefixed echo) the Playwright/Fake
    stack can launch as a process and the bridge can reach at the Fake worker IP:7681. Keep the
    in-process pytest fixture importing the shared handler (no duplication). Add `docker-compose.e2e.yml`
    wiring three services for the e2e run: the api (`BURROW_COMPUTE=fake`), the stub ttyd, and the
    built ui (or a vite preview), networked so the bridge reaches the stub at the Fake IP — at
    Claude's discretion how to map the Fake's `10.99.0.<vmid>` to the stub (e.g. route all
    `:7681` to the stub container). Write `restore.test.tsx` (vitest + MSW + the mock WebSocket from
    Plan 03): seed localStorage with a persisted tree referencing two ids (one absent from the MSW
    workspace list, one `running`), mount the App, assert the absent leaf is reconciled out and the
    running panel re-mounts + opens a fresh WS (UI-05; assert no scrollback replay buffer is used).
    SPDX headers on all new files.
  </action>
  <acceptance_criteria>
    - `vitest run tests/integration/restore.test.tsx` passes: gone leaf dropped, running panel
      reconnects to a fresh WS (UI-05).
    - `python api/tests/e2e/stub_ttyd_server.py --help` (or import) runs; it shares the Plan-01 handler logic.
    - `docker-compose.e2e.yml` is valid (`docker compose -f docker-compose.e2e.yml config` parses).
    - `tsc --noEmit` + `biome ci` green on the new ui test.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npx vitest run tests/integration/restore.test.tsx && npx tsc --noEmit && docker compose -f ../docker-compose.e2e.yml config -q</automated>
  </verify>
  <done>A standalone protocol-accurate stub ttyd exists for the e2e stack (shared handler, no scrollback); the UI-05 restore-after-refresh integration test passes (reconcile + live reconnect); docker-compose.e2e wiring parses.</done>
</task>

<task type="auto">
  <name>Task 2: Full Playwright journey — create→terminal→split/drag→detach→reconnect→terminate</name>
  <files>ui/tests/e2e/terminal.spec.ts, ui/playwright.config.ts</files>
  <read_first>
    02-VALIDATION.md Per-Req map (the `all` e2e row) + Sampling Rate (phase gate = full suite incl.
    Playwright); 02-UI-SPEC.md acceptance criteria 9/10/11/12/13 (reconnecting overlay, mosaic
    affordances, modal saga, terminate-vs-detach, fit/reflow) + Copywriting (terminate confirm copy);
    02-RESEARCH.md Pitfall 7 (reconnect = live PTY, no scrollback).
  </read_first>
  <behavior>
    - create: open `+ New workspace`, fill name/repo/branch/node, `Create` → boot-progress → a panel
      opens for the new workspace.
    - terminal echoes: typing into the terminal shows the echoed output (via the stub ttyd `'0'`-echo).
    - tiling: split the panel (H or V), drag-reorder, resize the gutter — the tree updates and the
      terminal reflows (not stuck 80x24).
    - detach: the plug (detach) closes the WS non-destructively → the reconnecting overlay appears
      (`reconnecting… attempt n / 5`) and the session reconnects; the workspace stays `running`.
    - terminate: the × shows `Destroy {name}? …` confirm; `Destroy` removes the panel and the
      workspace leaves the list; `Cancel` keeps it.
  </behavior>
  <action>
    Finalize `playwright.config.ts` (testDir `tests/e2e`, baseURL the e2e UI, `webServer` or the
    docker-compose.e2e stack from Task 1; `npx playwright install` runs in CI per Environment
    Availability). Write `terminal.spec.ts` driving the full journey over `BURROW_COMPUTE=fake` +
    the standalone stub ttyd: create a workspace via the modal and assert a panel opens; type and
    assert the echoed output appears; split/drag/resize and assert the tree changes + the terminal
    reflows; trigger detach and assert the reconnecting overlay + that the workspace stays `running`
    (non-destructive); trigger terminate and assert the `Destroy {name}? …` confirm gate then panel
    removal + list drop. Use stable selectors (roles/aria/test-ids) matching the UI-SPEC a11y
    contract. Keep the spec resilient (await network/poll settle; do not assert scrollback — none in
    v1, Pitfall 7). SPDX header.
  </action>
  <acceptance_criteria>
    - `cd ui && npx playwright test tests/e2e/terminal.spec.ts` passes the full journey over the
      Fake + stub-ttyd stack.
    - Detach is proven non-destructive (workspace stays running, overlay shows); terminate is gated
      by the confirm copy and removes the panel + drops the workspace (UI-SPEC criteria 9/12).
    - Split/drag/resize change the tree and the terminal reflows (criteria 10/13).
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npx playwright test tests/e2e/terminal.spec.ts</automated>
  </verify>
  <done>The full create→terminal→split/drag→detach→reconnect→terminate journey passes in Playwright over the FakeComputeProvider + protocol-accurate stub ttyd; detach-vs-terminate semantics + tiling affordances proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e stub ttyd ↔ bridge | the standalone stub stands in for a worker's ttyd; it speaks the real `tty` protocol so a `.encode()`/subprotocol regression fails the journey |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering (test fidelity) | stub_ttyd_server.py | mitigate | Standalone stub shares the Plan-01 protocol-accurate handler (negotiates `tty`, requires JSON init, `'0'`-echo) — a bare echo would hide SC-7; the e2e journey asserts real echo through the bridge. |
| T-02-08 | Info disclosure | e2e stack config | accept | `docker-compose.e2e.yml` is test-only (FakeComputeProvider, no real Proxmox creds, no secrets); LAN-only no-auth e2e by design (A6). |
</threat_model>

<verification>
- `cd ui && npx vitest run tests/integration/restore.test.tsx && npx playwright test tests/e2e/terminal.spec.ts` — green.
- Full phase suite (run before /gsd:verify-work): `cd api && uv run pytest` + `cd ui && npx vitest run && npx tsc --noEmit && npx biome ci . && npx playwright test`.
- Real ttyd `tty` handshake + live claude TUI = dev-homelab smoke (deferred, human_needed) — NOT gated here.
</verification>

<success_criteria>
- Playwright proves the full journey (create → terminal echoes → split/drag → detach→reconnect →
  terminate) over the FakeComputeProvider + protocol-accurate stub ttyd, with detach non-destructive
  and terminate confirm-gated.
- The UI-05 restore-after-refresh integration test passes: a still-running workspace's panel
  re-mounts and reconnects to the live session (no scrollback), and the persisted tree reconciles
  against the live list (UI-02 + UI-05).
- The standalone e2e stub ttyd shares the protocol-accurate handler so SC-7 cannot hide in the e2e tier.
</success_criteria>

<output>
Create `.planning/phases/02-terminal-proxy-react-ui/02-06-SUMMARY.md` when done.
</output>
