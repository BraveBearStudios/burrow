<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->
---
phase: 02-terminal-proxy-react-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/vite.config.ts
  - ui/vitest.config.ts
  - ui/playwright.config.ts
  - ui/index.html
  - ui/tsconfig.json
  - ui/src/main.tsx
  - ui/src/index.css
  - ui/src/api/client.ts
  - ui/src/types/workspace.ts
  - ui/src/lib/ttyd.ts
  - ui/tests/setup.ts
  - ui/tests/msw/handlers.ts
  - ui/tests/msw/server.ts
  - ui/public/fonts/
  - ui/src/api/client.test.ts
autonomous: true
requirements: []
# NOTE: pure foundation / Wave-0 tooling — no UI requirement is *delivered* here.
# This plan is the foundation for UI-01..05, which 02-03/02-04/02-05 actually deliver
# (UI-01 -> 02-03/02-05, UI-02 -> 02-04, UI-03 -> 02-05, UI-04 -> 02-05, UI-05 -> 02-04).
must_haves:
  truths:
    - "vite build + tsc --noEmit + biome ci + vitest all run green on the real UI project"
    - "client.ts unwraps the {data,meta,error} envelope and throws ApiError when error != null"
    - "The four design themes resolve from --tokens; switching data-theme swaps every color"
    - "Fonts load self-hosted (no fonts.googleapis.com / gstatic / jsdelivr request)"
    - "MSW handlers mock GET/POST /api/v1/workspaces, GET /api/v1/nodes for Tier-2 UI tests"
  artifacts:
    - path: "ui/src/api/client.ts"
      provides: "typed envelope-unwrapping fetch wrapper + ApiError"
      contains: "class ApiError"
    - path: "ui/src/types/workspace.ts"
      provides: "Workspace/WorkspaceStatus/WorkspaceCreate/ApiEnvelope/NodeCapacity/TerminalState types (camelCase)"
      contains: "WorkspaceStatus"
    - path: "ui/src/lib/ttyd.ts"
      provides: "verified ttyd frame builders (init JSON, '0'+input, '1'+resize) + opcode constants"
      contains: "inputFrame"
    - path: "ui/src/index.css"
      provides: "@import tailwindcss + @theme tokens for all four data-themes"
      contains: "@theme"
    - path: "ui/vite.config.ts"
      provides: "@tailwindcss/vite + @vitejs/plugin-react wiring (no tailwind.config.ts)"
      contains: "tailwindcss()"
  key_links:
    - from: "ui/src/api/client.ts"
      to: "/api/v1{path}"
      via: "fetch + envelope unwrap"
      pattern: "fetch\\(`/api/v1"
    - from: "ui/src/index.css"
      to: "data-theme dark|dark-soft|medium|light"
      via: "CSS custom properties per theme"
      pattern: "data-theme"
---

<objective>
Stand up the real Burrow UI project on the bare Phase-0 scaffold (`ui/` currently has
only `placeholder.ts` + biome/tsconfig). Install the pinned stack, wire Vite 8 +
Tailwind v4 (CSS-first, no JS config) + vitest 4 + Playwright, and build the
foundation modules every later UI plan imports: the typed envelope `client.ts`, the
camelCase `types/workspace.ts`, the verified ttyd frame builders `lib/ttyd.ts`, the
four-theme `@theme` token sheet, self-hosted fonts, and the MSW test harness.

Purpose: this is the contracts-and-tooling layer. Defining `client.ts` / `types` /
`ttyd.ts` first means every downstream plan (terminal, layout, sidebar/modal/statusbar)
receives the blueprints in the plan, not a scavenger hunt. No business components here —
those are Waves 2-4.
Output: a buildable, testable UI project with green vite/tsc/biome/vitest, the typed
client + types + ttyd frame lib, the binding token sheet, fonts, and MSW handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-terminal-proxy-react-ui/02-RESEARCH.md
@.planning/phases/02-terminal-proxy-react-ui/02-UI-SPEC.md
@.planning/phases/02-terminal-proxy-react-ui/02-VALIDATION.md
@CLAUDE.md

# The frozen envelope + workspace contract this UI must match:
@api/lib/envelope.py
@api/models/workspace.py

# The existing bare scaffold (extend, don't fight the pins):
@ui/package.json
@ui/tsconfig.json
@ui/biome.json

<interfaces>
<!-- Backend contract the UI types MUST mirror (camelCase via model_dump(by_alias=True)). -->

Envelope (api/lib/envelope.py): { data: T | null, meta: { requestId, timestamp }, error: { code, message } | null }

Workspace JSON (camelCase): id, name, status, vmid, node, lxcIp, projectRepo,
  projectBranch, pluginSet, createdAt, stoppedAt, destroyedAt, deletedAt.
  status ∈ "creating" | "running" | "stopped" | "error" | "destroyed".

WorkspaceCreate body: { name, projectRepo, projectBranch="main", pluginSet="default", node }.

GET /api/v1/nodes data (from Plan 01): [{ node, memoryUsedFraction, capacityThreshold, overThreshold }].

ttyd protocol constants (VERIFIED, 02-RESEARCH Pattern 1/2):
  subprotocol "tty"; client INPUT '0', RESIZE '1'; server OUTPUT '0', TITLE '1', PREFS '2';
  init = JSON.stringify({AuthToken:"", columns, rows}) sent as encoded bytes.

Pinned stack (02-RESEARCH Standard Stack — install EXACT, do not loosen mosaic):
  runtime: react@19.2.7 react-dom@19.2.7 @xterm/xterm@6.0.0 @xterm/addon-fit@0.11.0
    @xterm/addon-web-links@0.12.0 react-mosaic-component@6.2.0 (EXACT, not ^) react-dnd@16.0.1
    react-dnd-html5-backend@16.0.1 @tanstack/react-query@5.101.0 zustand@5.0.14
  dev: vite@8.0.16 @vitejs/plugin-react@6.0.2 tailwindcss@4.3.0 @tailwindcss/vite@4.3.0
    vitest@4.1.8 @testing-library/react@16.3.2 @testing-library/jest-dom@6.9.1 jsdom
    msw@2.14.6 @playwright/test@1.60.0
  (typescript@6.0.3 + @biomejs/biome@2.4.16 already in the scaffold.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the pinned stack + wire Vite/Tailwind/vitest/Playwright (Wave 0 framework)</name>
  <files>ui/package.json, ui/package-lock.json, ui/vite.config.ts, ui/vitest.config.ts, ui/playwright.config.ts, ui/index.html, ui/src/main.tsx, ui/tsconfig.json, ui/tests/setup.ts</files>
  <read_first>
    02-RESEARCH.md Standard Stack (exact versions + the `react-mosaic-component@6.2.0` EXACT
    pin warning — `^7` pulls the 7.0.0-beta), "Tailwind v4 wiring" example, Environment Availability
    (Playwright browsers not installed — `npx playwright install` runs in CI); 02-VALIDATION.md
    Wave 0 Requirements + Test Infrastructure; ui/package.json + ui/tsconfig.json + ui/biome.json
    (the scaffold pins to keep).
  </read_first>
  <action>
    Install the exact pinned runtime + dev deps from the interfaces block (use the 02-RESEARCH
    install commands; pin `react-mosaic-component@6.2.0` EXACT, never `^`). Create `vite.config.ts`
    with `react()` + `tailwindcss()` plugins (no `tailwind.config.ts` — Tailwind v4 is CSS-first,
    ADR-0008). Create `index.html` with `<div id="root">` and `data-theme="dark"` on `<html>`, and
    a strict same-origin CSP `<meta>` (no `font-src`/`style-src` to Google/jsdelivr — PLAT-05 /
    UI-SPEC Security Reconciliation). Create `src/main.tsx` (`createRoot`, `<QueryClientProvider>`,
    render `<App/>` — a tiny placeholder `App` is fine here; the real shell is Wave 2/4). Add a
    dev-proxy in vite.config so `/api/v1` and `/ws` proxy to `http://localhost:8000` (the control
    plane). Create `vitest.config.ts` (jsdom env, `globals`, `setupFiles: ['./tests/setup.ts']`) and
    `tests/setup.ts` (import `@testing-library/jest-dom`; start/stop the MSW server from Task 4).
    Create `playwright.config.ts` (testDir `tests/e2e`, baseURL the vite/preview server; webServer
    config commented for the Wave-5 e2e plan to finalize). Extend `package.json` scripts: `dev`,
    `build`, `preview`, `test` (`vitest run`), `test:watch`, `e2e` (`playwright test`), keep
    `typecheck`/`lint`. Ensure `tsconfig.json` includes `src/**` + `tests/**` and DOM libs.
    Add SPDX two-line headers to every new source/config file (TS comment syntax).
  </action>
  <acceptance_criteria>
    - `cd ui && npm ci` (or install) resolves with mosaic pinned to exactly 6.2.0 (no 7.x in the lock).
    - `cd ui && npm run build` succeeds; `npx tsc --noEmit` clean; `npx biome ci .` clean.
    - `vitest run` executes (zero tests is fine at this task; config loads).
    - No `tailwind.config.ts` exists; Tailwind is wired via `@tailwindcss/vite`.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npm run build && npx tsc --noEmit && npx biome ci .</automated>
  </verify>
  <done>Real UI project builds; Vite 8 + Tailwind v4 + vitest 4 + Playwright wired; mosaic pinned 6.2.0 exact; CSP set; SPDX headers present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typed envelope client + camelCase domain types + verified ttyd frame lib</name>
  <files>ui/src/api/client.ts, ui/src/api/client.test.ts, ui/src/types/workspace.ts, ui/src/lib/ttyd.ts</files>
  <read_first>
    02-RESEARCH.md "Pattern 4: Typed envelope client" + "Pattern 2 frame builders" (the verified
    ttyd opcode constants + init/input/resize encoders); api/lib/envelope.py + api/models/workspace.py
    (the exact camelCase field names — `lxcIp`, `projectRepo`, `projectBranch`, `pluginSet`).
  </read_first>
  <behavior>
    - test_unwraps_data: `api<T>(path)` returns `body.data` when `error` is null.
    - test_throws_apierror: when `error != null`, it throws `ApiError` carrying `error.code` + `error.message`.
    - test_input_frame: `inputFrame("a")` is `Uint8Array [0x30, 0x61]` (`'0'` prefix + data byte).
    - test_resize_frame: `resizeFrame(80,24)` is `'1'` + JSON `{columns:80,rows:24}`.
    - test_init_frame: `initFrame(80,24)` decodes to JSON with `AuthToken`,`columns`,`rows`.
  </behavior>
  <action>
    Create `src/api/client.ts`: `interface ApiEnvelope<T>` (data|null, meta{requestId,timestamp},
    error{code,message}|null); `class ApiError extends Error` (carries `code`); `async function
    api<T>(path, init?)` that `fetch`es `/api/v1${path}`, parses the envelope, throws `ApiError`
    on `error != null`, else returns `data as T`. Create `src/types/workspace.ts` with `Workspace`
    (camelCase, mirror the interfaces block EXACTLY — `lxcIp`, `projectRepo`, etc.),
    `WorkspaceStatus` union, `WorkspaceCreate`, `NodeCapacity` ({node, memoryUsedFraction,
    capacityThreshold, overThreshold}), and `TerminalState` (`"connecting"|"open"|"reconnecting"
    |"error"|"closed"`). Create `src/lib/ttyd.ts` centralizing the VERIFIED ttyd constants
    (INPUT `'0'`, RESIZE `'1'`, server OUTPUT `'0'`) and the three frame builders — `initFrame`,
    `inputFrame`, `resizeFrame` — exactly per Pattern 2 (TextEncoder; `inputFrame` = `0x30` byte +
    data; `resizeFrame` = `"1"` + JSON; `initFrame` = JSON `{AuthToken:"",columns,rows}` encoded).
    Write `client.test.ts` covering the five behaviors (mock `fetch` for the envelope cases).
    SPDX headers on all files.
  </action>
  <acceptance_criteria>
    - `vitest run src/api/client.test.ts` passes all five behaviors.
    - `Workspace` type field names match `model_dump(by_alias=True)` camelCase (compile-time check
      against an MSW fixture in a later plan; here verify the union + key names by the test importing them).
    - `tsc --noEmit` + `biome ci` green.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npx vitest run src/api/client.test.ts && npx tsc --noEmit && npx biome ci src/api src/types src/lib</automated>
  </verify>
  <done>client.ts unwraps the envelope + throws ApiError; types mirror the camelCase backend contract; ttyd.ts provides the verified frame builders — all imported downstream without re-derivation.</done>
</task>

<task type="auto">
  <name>Task 3: Four-theme token sheet + self-hosted fonts + MSW handlers</name>
  <files>ui/src/index.css, ui/public/fonts/, ui/tests/msw/handlers.ts, ui/tests/msw/server.ts</files>
  <read_first>
    02-UI-SPEC.md "Color Tokens" (all four themes: dark/dark-soft/medium/light — verbatim values),
    "Typography" (Space Grotesk 500 / Inter / JetBrains Mono; self-hosted woff2 only, font-display
    swap), "Security / Posture Reconciliation" (NO Google/jsdelivr CDN — CSP), "Spacing Scale" +
    "Radius/Motion"; 02-RESEARCH.md Tailwind v4 `@theme` example.
  </read_first>
  <action>
    Create `src/index.css`: `@import "tailwindcss";` then an `@theme` block seeding the default
    (`dark`) tokens, plus `[data-theme="dark"]`, `[data-theme="dark-soft"]`, `[data-theme="medium"]`,
    `[data-theme="light"]` selectors each defining the full `--bg/--bg-surf/--bg-panel/--bg-panel-alt
    /--bg-hover/--border/--border-mid/--text/--text-sub/--text-muted/--accent/--accent-bg/--accent-line
    /--gold/--gold-bg/--ok/--warn/--err` set VERBATIM from the UI-SPEC tables. Add `--font-display`
    (Space Grotesk), `--font-sans` (Inter), `--font-mono` (JetBrains Mono) and the spacing/radius
    tokens (4px base; chrome heights 52/32/228/36px; radii 12/10/8/6px) from the Spacing Scale.
    Add `@keyframes pulse` (creating-dot, 1.4s), spinner spin (0.8s linear), cursor blink (1.1s),
    and a `@media (prefers-reduced-motion: reduce)` block that stills them. Self-host the three faces:
    place woff2 files under `ui/public/fonts/` and declare `@font-face` (`font-display: swap`) for
    each — NO `fonts.googleapis.com`/`gstatic`/`jsdelivr` link anywhere; if a face cannot be vendored
    at build time, fall back to the `_ds` system stacks (`ui-sans-serif, system-ui…` /
    `ui-monospace, Menlo…`) — never a network font (UI-SPEC). Create the MSW test harness:
    `tests/msw/handlers.ts` mocking `GET /api/v1/workspaces` (list, status-filterable),
    `POST /api/v1/workspaces` (returns a `running` workspace — the saga is synchronous in v1),
    `GET /api/v1/workspaces/:id`, `GET /api/v1/nodes`, all wrapped in the `{data,meta,error}`
    envelope with camelCase fields; `tests/msw/server.ts` (`setupServer(...handlers)`). SPDX headers.
  </action>
  <acceptance_criteria>
    - `cd ui && npm run build` includes the CSS; no network-font request string
      (`grep -rIl "googleapis\\|gstatic\\|jsdelivr" ui/src ui/index.html ui/public` returns nothing).
    - All four `data-theme` blocks define the full token set (each token appears in each block).
    - MSW `server.ts` + `handlers.ts` import-clean and are wired into `tests/setup.ts` (Task 1).
    - `tsc --noEmit` + `biome ci` green.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npm run build && npx biome ci src/index.css tests/msw</automated>
  </verify>
  <done>Four design themes resolve from --tokens with no hardcoded hex elsewhere; fonts self-hosted (CSP-clean, no CDN); MSW handlers mock the /api/v1 surface for Tier-2 tests.</done>
</task>

</tasks>

<verification>
- `cd ui && npm run build && npx tsc --noEmit && npx biome ci . && npx vitest run` — all green.
- No CDN font/icon request: `grep -rI "googleapis\|gstatic\|jsdelivr" ui/src ui/index.html ui/public` empty.
- `react-mosaic-component` resolves to exactly 6.2.0 in `ui/package-lock.json` (no 7.x).
</verification>

<success_criteria>
- The UI project builds, type-checks, lints, and runs vitest on the real (no longer placeholder) tree.
- `client.ts` unwraps the envelope + throws `ApiError`; `types/workspace.ts` mirrors the camelCase
  backend contract; `lib/ttyd.ts` exports the verified frame builders.
- All four UI-SPEC themes resolve from `--tokens`; fonts are self-hosted (no CDN); MSW mocks /api/v1.
</success_criteria>

<output>
Create `.planning/phases/02-terminal-proxy-react-ui/02-02-SUMMARY.md` when done.
</output>
