---
phase: 01-control-plane-api
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - api/compute/proxmoxProvider.py
  - api/pyproject.toml
  - api/tests/integration/test_proxmox_provider.py
autonomous: true
requirements: [PLAT-07, CAP-01, CICD-02, CICD-03]
user_setup: []

must_haves:
  truths:
    - "Every mutating Proxmox call blocks on its UPID task and asserts exitstatus OK before returning (SC-1)"
    - "Every synchronous proxmoxer call runs in asyncio.to_thread — no proxmoxer call runs on the event loop"
    - "The Proxmox client is constructed with CA-pinned TLS (verify_ssl=<ca path>), never verify_ssl=False"
    - "cloneCt sets the static net0 IP from VMID and adds the new VMID to the burrow-workers pool"
    - "getNodeMemory returns the used-memory fraction for the capacity guard"
    - "The Proxmox provider is unit/integration-provable in CI via the responses library (requests-mock)"
  artifacts:
    - path: "api/compute/proxmoxProvider.py"
      provides: "Real proxmoxer-backed ComputeProvider (UPID waits, net0, pool-add, node mem)"
      contains: "asyncio.to_thread"
    - path: "api/tests/integration/test_proxmox_provider.py"
      provides: "UPID-block + net0 + node-memory proof via responses (requests-mock)"
      min_lines: 50
    - path: "api/pyproject.toml"
      provides: "responses + respx dev deps; pytest config carried forward"
      contains: "responses"
  key_links:
    - from: "api/compute/proxmoxProvider.py"
      to: "proxmoxer.tools.Tasks.blocking_status"
      via: "UPID block inside asyncio.to_thread, assert exitstatus OK"
      pattern: "blocking_status"
    - from: "api/compute/proxmoxProvider.py"
      to: "settings.proxmox_ca_cert_path"
      via: "ProxmoxAPI(verify_ssl=<ca path>)"
      pattern: "verify_ssl"
---

<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<objective>
Fill the frozen Phase-0 `ProxmoxComputeProvider` skeleton with real `proxmoxer` bodies behind the ABC, and prove them in CI with the `responses` library (requests-mock). Real-Proxmox acceptance is the deferred dev-homelab smoke gate; this plan delivers the CI-provable code.

Purpose: PLAT-07 ships the real Proxmox impl with no driver leak. The three landmines live here: (1) proxmoxer is SYNCHRONOUS — every call must be wrapped in `asyncio.to_thread` or it stalls the event loop; (2) `respx` CANNOT mock proxmoxer (it rides `requests`, not httpx) — tests use `responses`; (3) every mutation returns a UPID immediately and must be blocked on (`Tasks.blocking_status`, assert OK) before returning (SC-1). CAP-01's `getNodeMemory` data source lives here.
Output: a complete `ProxmoxComputeProvider` (clone/start/stop/destroy UPID-blocked, static net0 from VMID, pool-add, node-memory, getIp, getNextVmid/usedVmids, waitTask, healthcheck), `responses`+`respx` dev deps, and a `responses`-backed integration test.

This plan is logically independent of the service/routers and touches only `compute/proxmoxProvider.py`, `pyproject.toml`, and one test file. It is sequenced into Wave 2 (depends on Plan 01) so the `tests/integration/` package and lockfile updates land in a clean order with no same-wave file overlap — not because of a code dependency on Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-control-plane-api/01-CONTEXT.md
@.planning/phases/01-control-plane-api/01-RESEARCH.md
@.planning/phases/01-control-plane-api/01-01-SUMMARY.md
@CLAUDE.md
@docs/adr/ADR-0003-proxmox-acl-scoping.md
@docs/adr/ADR-0004-static-ip-from-vmid.md
@docs/adr/ADR-0005-clone-mode-full.md

<interfaces>
<!-- Frozen Phase-0 contracts. Fill this skeleton; do not change the ABC. -->

From api/compute/proxmoxProvider.py (skeleton — every method currently raises NotImplementedError; proxmoxer already imported at module top; __init__ reads settings.proxmox_ca_cert_path):
```python
class ProxmoxComputeProvider(ComputeProvider):
    def __init__(self, settings: Any) -> None: ...
    async def getNextVmid(self, pool_start, pool_end, used: set[int]) -> int
    async def usedVmids(self) -> set[int]
    async def cloneCt(self, template_vmid, new_vmid, name, node, full=True) -> ComputeTask
    async def injectBootConfig(self, vmid, config: BootConfig) -> None
    async def startCt(self, node, vmid) -> ComputeTask
    async def stopCt(self, node, vmid) -> ComputeTask
    async def destroyCt(self, node, vmid) -> ComputeTask
    async def getStatus(self, node, vmid) -> ComputeStatus
    async def getIp(self, node, vmid) -> str | None
    async def getNodeMemory(self, node) -> float
    async def waitTask(self, node, upid, timeout: float) -> ComputeTask
    async def healthcheck(self) -> bool
```

From api/compute/provider.py (typed error hierarchy to raise): ComputeError, NoFreeVmidError, CloneError, TaskFailedError, LxcNotReadyError.
From api/models/compute.py: ComputeTask(upid: str|None, status: "ok"|"failed", exitstatus: str|None); ComputeStatus(status,uptime,mem,maxmem,cpu); BootConfig(config_repo,config_branch,project_repo,project_branch).
From api/config.py Settings: proxmox_host, proxmox_user, proxmox_token_name, proxmox_token_value, proxmox_ca_cert_path, template_vmid(9000), worker_pool_start(200), worker_pool_end(299), default_node.

proxmoxer API (verified 2026-06-10): `ProxmoxAPI(host, user=, token_name=, token_value=, verify_ssl=<ca path str>)`; method chaining `self._api.nodes(node).lxc(vmid).clone.post(newid=, hostname=, full=1)` returns the UPID string; `.config.put(net0=...)`; `.status.start.post()` / `.status.stop.post()`; `nodes(node).lxc(vmid).delete()`; `nodes(node).status.get()` returns dict with `mem`/`maxmem` (bytes); `cluster.resources.get(type="vm")` for used VMIDs. `from proxmoxer.tools import Tasks; Tasks.blocking_status(prox, upid, timeout=...)` returns a dict with `exitstatus` ("OK" on success) or None on timeout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responses + respx dev deps and carry the pytest config forward</name>
  <read_first>
    - api/pyproject.toml (the [dependency-groups] dev list; [tool.pytest.ini_options] asyncio_mode + testpaths; the proxmoxer mypy override)
    - 01-RESEARCH.md Standard Stack (responses 0.26.1 mocks proxmoxer's requests leg; respx 0.23.1 mocks the httpx ttyd leg) and Package Legitimacy Audit (both pre-audited Phase-0, established maintained projects)
    - 01-VALIDATION.md Wave 0 Requirements ("add responses + respx dev deps")
  </read_first>
  <files>api/pyproject.toml</files>
  <action>
    Add `responses==0.26.1` and `respx==0.23.1` to the `[dependency-groups] dev` list (versions per RESEARCH; both audited in the Package Legitimacy table — Disposition Approved). `responses` mocks proxmoxer's underlying `requests` calls (Pitfall 5: respx cannot — proxmoxer is requests-based); `respx` is for the httpx ttyd-health leg used by Plan 04's saga integration tests. Keep the existing pytest `[tool.pytest.ini_options]` (`asyncio_mode = "auto"`, `testpaths = ["tests"]`) — it already covers `tests/integration/` since testpaths is the `tests` root. Run `uv lock` to update the lockfile, then `uv lock --check` to confirm it is consistent. Do NOT add any runtime dependency (Phase 1 adds none).
  </action>
  <verify>
    <automated>cd api && uv lock --check && uv run python -c "import responses, respx"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` dev group lists `responses==0.26.1` and `respx==0.23.1`; runtime `dependencies` unchanged.
    - `uv lock --check` passes (lockfile consistent with the manifest).
    - `import responses, respx` succeeds in the project venv.
  </acceptance_criteria>
  <done>responses + respx are dev deps, the lockfile is updated and consistent, no runtime dep added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the real ProxmoxComputeProvider bodies (UPID-blocked, to_thread, CA-pinned)</name>
  <read_first>
    - api/compute/proxmoxProvider.py (the skeleton + __init__ reading proxmox_ca_cert_path; proxmoxer imported at module top)
    - api/compute/provider.py (the typed error hierarchy to raise)
    - api/compute/fakeProvider.py (observable lifecycle to match: clone→stopped, start→running, destroy→removed; getIp derives from VMID)
    - 01-RESEARCH.md Pattern 1 (UPID-block + to_thread template, full cloneCt example with pool-add + net0), the getNodeMemory code example, Pitfalls 1/2 (sync-on-loop, treat-as-sync), and ADR-0004 net0 formula / ADR-0003 pool-add
    - docs/adr/ADR-0004 (the `pct set net0` CIDR `ip=`/bare `gw=` formula), ADR-0003 (clone must add the VMID to /pool/burrow-workers), ADR-0005 (full clone)
  </read_first>
  <files>api/compute/proxmoxProvider.py</files>
  <behavior>
    - cloneCt: posts a full clone, adds the new VMID to the burrow-workers pool, sets net0 static IP from VMID, blocks on the returned UPID, asserts exitstatus OK → ComputeTask(status="ok"). A non-OK/timeout status raises TaskFailedError; a proxmoxer request error raises CloneError.
    - startCt/stopCt/destroyCt: post the mutation, block on the UPID, assert OK; destroyCt of a nonexistent CT is tolerated as a no-op success (idempotent compensation, Pitfall 7).
    - waitTask: wraps Tasks.blocking_status in to_thread; None → TaskFailedError (timeout); exitstatus != "OK" → TaskFailedError.
    - getNodeMemory: returns mem/maxmem fraction (0.0..1.0) from nodes(node).status.get().
    - getIp: computes the IP from the VMID per ADR-0004 (no DHCP poll, SC-6).
    - getNextVmid: pure scan over [pool_start,pool_end] minus `used` (NOT a reservation — the DB index is the arbiter, per the ABC docstring); raise NoFreeVmidError when exhausted. usedVmids: query cluster resources, return the set of VMIDs in the pool range.
    - healthcheck: a cheap GET (e.g. version) wrapped in to_thread; True on success, False on any exception.
  </behavior>
  <action>
    Implement every method by wrapping each blocking proxmoxer call in `await asyncio.to_thread(...)` (Pitfall 2 — proxmoxer is synchronous; an un-wrapped call stalls the loop). Construct the client in `__init__` as `ProxmoxAPI(settings.proxmox_host, user=settings.proxmox_user, token_name=settings.proxmox_token_name, token_value=settings.proxmox_token_value, verify_ssl=settings.proxmox_ca_cert_path)` — CA-pinned TLS, NEVER `verify_ssl=False` (CLAUDE.md security posture; forbidden by the block_on=high gate). Add a private `_block(upid, timeout)` helper (RESEARCH Pattern 1) that runs `Tasks.blocking_status` in a thread, maps None→TaskFailedError (timeout) and non-OK→TaskFailedError, and returns a `ComputeTask`. Add `_net0_for(vmid)` computing the net0 string from the VMID per ADR-0004 (read the subnet/gateway/bridge/prefix from `Settings` — add the needed settings keys if absent, defaulting to clearly-marked LAN placeholders; do NOT hard-code a real LAN). cloneCt does pool-add (`self._api.pools("burrow-workers").put(vms=str(new_vmid))`, ADR-0003) and net0 set as part of the clone, then blocks on the clone UPID (RESEARCH Pattern 1 example). Map clone/start/stop/destroy timeouts from `Settings` (add `clone_timeout`, `task_timeout` with conservative defaults per Assumption A5). Keep ALL proxmoxer symbols inside this file (seam-leakage guard: `proxmoxer`/`ProxmoxAPI` allowed only here). Raise only the typed `ComputeError` subclasses across the seam — no proxmoxer exception type escapes. Do NOT implement the tech-spec §6.1 `verify_ssl=False` snippet (anti-pattern). `injectBootConfig` here is the real DB-write seam consumer — but since the provider has no DB handle, leave it as the documented pull-at-boot no-op at the provider level (the saga writes boot intent to the DB via DbProvider; the provider's injectBootConfig stays a no-op that the Fake also no-ops), matching the ABC docstring.
  </action>
  <verify>
    <automated>cd api && uv run mypy . --strict && uv run pytest tests/unit/test_seam_leakage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - No method raises `NotImplementedError`; every blocking proxmoxer call is inside `asyncio.to_thread`.
    - The client is constructed with `verify_ssl=settings.proxmox_ca_cert_path`; the string `verify_ssl=False` (or `verify_ssl = False`) appears nowhere (block_on=high gate).
    - cloneCt adds the VMID to the pool and sets net0 from the VMID before/at the clone, then blocks on the UPID and asserts OK.
    - getNodeMemory returns a float fraction; getIp computes from VMID (no interface/agent poll).
    - `mypy . --strict` clean; `test_seam_leakage.py` passes (proxmoxer confined to this file).
    - `grep -v '^[[:space:]]*#' api/compute/proxmoxProvider.py | grep -c 'verify_ssl=False'` returns 0.
  </acceptance_criteria>
  <done>ProxmoxComputeProvider is fully implemented: UPID-blocked, to_thread-wrapped, CA-pinned, net0+pool-add on clone, node-memory + computed IP — mypy strict + seam guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — UPID block, net0 set, node memory via responses (requests-mock)</name>
  <read_first>
    - api/compute/proxmoxProvider.py (the methods under test)
    - api/tests/integration/__init__.py (the integration test package, created by Plan 01 in Wave 1)
    - 01-RESEARCH.md Pitfall 5 (responses, NOT respx, mocks the proxmoxer requests leg) and the Code Examples (UPID task response shape, /nodes/{node}/status mem/maxmem)
    - 01-VALIDATION.md Per-Req Verification Map row "WS-02 (Proxmox unit)" → test_proxmox_provider.py
  </read_first>
  <behavior>
    - Test: cloneCt against a mocked Proxmox HTTP API (responses) — the clone POST returns a UPID; the task-status GET returns exitstatus "OK"; assert cloneCt returns ComputeTask(status="ok") AND that the pool-add PUT and net0 config PUT were issued.
    - Test: a task-status GET returning exitstatus != "OK" makes cloneCt raise TaskFailedError; a timeout (status None) raises TaskFailedError.
    - Test: getNodeMemory against a mocked /nodes/{node}/status returning mem/maxmem returns mem/maxmem.
    - Test: the client is configured with verify_ssl set to the CA path (assert no verify_ssl=False path is taken) — e.g. construct with a settings stub carrying a cert path and assert construction succeeds.
    - Test: getIp returns the VMID-derived address; getNextVmid raises NoFreeVmidError on an exhausted range.
  </behavior>
  <files>api/tests/integration/test_proxmox_provider.py</files>
  <action>
    Create `tests/integration/test_proxmox_provider.py` using `responses` (the requests-mock library — NOT respx; proxmoxer rides requests, Pitfall 5) to register realistic Proxmox HTTP responses: the clone POST returning a UPID string, the `GET .../tasks/{upid}/status` returning `{"data": {"status":"stopped","exitstatus":"OK"}}` for success and a non-OK variant for failure, the pool PUT, the net0 config PUT, and `GET /nodes/{node}/status` returning `{"data":{"mem":..., "maxmem":...}}`. Build a `Settings` stub (or a dataclass) carrying proxmox_host/user/token/ca_cert_path + pool range so the provider constructs without a real `.env`. Assert the recorded `responses.calls` include the pool-add and net0 PUTs (proving ADR-0003/ADR-0004 wiring). Assert TaskFailedError on the non-OK and timeout paths. The `tests/integration/__init__.py` package already exists (Plan 01). Tests are exempt from the seam guard, so importing `responses` here is fine. Mark any case that genuinely needs a live node as out of scope (dev-homelab smoke) — do not attempt real network.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/integration/test_proxmox_provider.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_proxmox_provider.py -x` passes with zero outbound network (responses intercepts the requests leg).
    - The success-clone test asserts the pool-add PUT and net0 PUT were issued (recorded in `responses.calls`).
    - A non-OK/timeout task-status makes cloneCt raise `TaskFailedError`.
    - getNodeMemory returns the mocked mem/maxmem fraction.
    - The test uses `responses`, not `respx` (Pitfall 5 honored).
  </acceptance_criteria>
  <done>The Proxmox provider is CI-proven via responses: UPID-block + pool-add + net0 + node-memory + failure paths verified hermetically; real-clone is the deferred homelab gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control plane → Proxmox HTTPS :8006 | A privileged channel; CA-pinned TLS and a least-privilege scoped token cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing/Info Disclosure | ProxmoxAPI TLS channel | mitigate | CA-pinned TLS via `verify_ssl=settings.proxmox_ca_cert_path`; `verify_ssl=False` forbidden (block_on=high gate; grep asserts absence) |
| T-01-05 | Elevation of Privilege | Proxmox token scope | mitigate | Token is the Phase-0 BurrowProvisioner least-priv role scoped to /pool/burrow-workers+storage+node (ADR-0003); token read from gitignored .env only, never logged or CLI-arged |
| T-01-06 | Information Disclosure | proxmox token / errors in logs | mitigate | No proxmoxer exception text containing the token is logged; only typed ComputeError messages cross the seam; token never appears in event data |
| T-01-SC | Tampering | package installs (responses, respx) | mitigate | Both pre-audited Phase-0 (RESEARCH Package Legitimacy Audit, Disposition Approved, established maintained repos); `uv lock --check` CI gate; no [ASSUMED]/[SUS]/[SLOP] packages → no blocking checkpoint required |
</threat_model>

<verification>
- `cd api && uv run pytest tests/integration/test_proxmox_provider.py -q` green.
- `cd api && uv run ruff check . && uv run ruff format --check . && uv run mypy . --strict` clean.
- `cd api && uv lock --check` consistent.
- `test_seam_leakage.py` green — proxmoxer confined to `proxmoxProvider.py`.
- block_on=high: `verify_ssl=False` absent from the provider (grep gate, comment-stripped).
</verification>

<success_criteria>
- ProxmoxComputeProvider fully implemented behind the ABC: UPID-blocked, to_thread-wrapped, CA-pinned, net0+pool-add on clone, node-memory + computed IP.
- responses + respx dev deps added; lockfile consistent; no runtime dep added.
- The provider is CI-proven via responses (UPID block + net0 + node memory + failure paths); real-clone deferred to the dev-homelab smoke gate.
- mypy strict + seam guard + lint green.
</success_criteria>

<output>
Create `.planning/phases/01-control-plane-api/01-02-SUMMARY.md` when done.
</output>
