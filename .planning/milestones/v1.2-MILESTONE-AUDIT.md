<!--
SPDX-FileCopyrightText: 2026 Brave Bear Studios
SPDX-License-Identifier: AGPL-3.0-or-later
-->
---
milestone: v1.2
audited: 2026-06-16
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 4/4
  flows: complete
gaps: {}
tech_debt:
  - phase: 09-auto-node-selection
    items:
      - "IN-01 (info, backlog): create route returns HTTP 200 while the MSW/UI test doubles were aligned to 200 in the fix pass; verify no other double asserts 201."
deferred:
  - "ACC-01: dev-homelab smoke (real worker boot + plugin load + real multi-node auto-select, Phase 3/9 real-infra)."
  - "ACC-02: first live release-please PR on a runner + the egress-policy block flip + discovered allowlist + GITHUB_TOKEN tag-retrigger confirmation + full actionlint (Phase 8)."
  - "ACC-03: real GHCR publish + cosign / attestation verify (Phase 4 release supply-chain)."
  - "WSX-02 / WSX-03: persistent/snapshotted workspaces + full scrollback restore (real-boot v2)."
---

# Milestone v1.2: Backlog Fixes + Release Automation (Audit)

**Status: PASSED.** 5/5 requirements satisfied, 3/3 phases verified, cross-phase
integration WIRED (4/4 connections traced end-to-end), 0 blockers.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------|--------------|---------|--------------|-------|
| UI-12 | 7 | passed | listed | `[x]` Complete | satisfied |
| CICD-09 | 7 | passed | listed | `[x]` Complete | satisfied |
| RELX-01 | 8 | passed | listed | `[x]` Complete | satisfied |
| RELX-02 | 8 | passed | listed | `[x]` Complete | satisfied |
| WSX-01 | 9 | passed | listed | `[x]` Complete | satisfied |

No unsatisfied requirements. No orphans among v1.2-mapped REQ-IDs. (ACC-01/02/03 and
WSX-02/03 appear in the REQUIREMENTS body but are intentionally NOT mapped to v1.2 phases;
they are tracked Deferred future requirements, not orphans of this milestone.)

## Phases

| Phase | Name | Plans | Verification | Score |
|-------|------|-------|--------------|-------|
| 7 | Backlog Fixes (Fast-Reconcile + E2E Hardening) | 1/1 | passed | 5/5 |
| 8 | Release Hardening (release-please + harden-runner) | 2/2 | passed | 5/5 |
| 9 | Auto Node Selection | 3/3 | passed | 5/5 (16/16 truths) |

## Cross-Phase Integration (gsd-integration-checker: WIRED, 0 blockers)

Suites grounded by the checker: api `uv run pytest` → 202 passed; ui `npx vitest run` → 117 passed.

| Connection | Path | Verdict |
|------------|------|---------|
| UI-12 fast-reconcile | `useTerminal` fires → `TerminalPanel` threads `onTerminalEvent` → `WorkspaceLayout` supplies `useInvalidateWorkspaces` → `WORKSPACES_KEY` invalidate | WIRED |
| WSX-01 saga | `WorkspaceCreate.node Optional` → `selectNode` inside `_create_lock` → `_fits` guard → reserve → running; manual path proven never-calls-selectNode | WIRED |
| WSX-01 modal contract | modal Auto-default sends `node: null` → backend auto-selects; manual still selectable | WIRED |
| RELX tag-chain | `release-please.yml` (push:main → release PR → `v*` tag, `include-v-in-tag:true`) → `release.yml on: push: tags:["v*"]` publish | WIRED |

**Phase-9 modal change vs Phase-7 surfaces:** disjoint files (`NewWorkspaceModal.tsx` vs
`WorkspaceLayout.tsx`/`TerminalPanel.tsx`): no collision. The e2e create, terminal, stop/start,
destroy journey still completes under Auto-default (the helper leaves the node as-is = Auto,
backend auto-selects `pve1` over the single-node Fake → running).

## Nyquist Compliance (discovery only)

| Phase | VALIDATION.md | Compliant | Note |
|-------|---------------|-----------|------|
| 7 | (none) | n/a | backlog-fix phase; covered by the existing test tiers |
| 8 | exists | false (PARTIAL) | CI/docs phase ships no behavioral test framework; validation is static YAML/JSON parse + `reuse lint` (by design) |
| 9 | exists | true (COMPLIANT) | full criterion-5 unit+integration matrix over the Fake |

## Tech Debt / Warnings

- **Resolved during audit:** the 3 e2e specs' stale "Node defaults to the first node" comments
  were updated to "Auto (least-loaded)" (functionally correct before; comment-only drift).
- **Deferred ACC-02 (documented, not a gap):** `actionlint` unavailable on the dev host →
  workflows validated by Python YAML parse + SHA-pin regex only; full action-schema lint + the
  live `GITHUB_TOKEN`-retrigger check are the first-on-runner acceptance.
- **IN-01 (info, backlog):** create returns HTTP 200; ensure no remaining test double asserts 201.

## Verdict

All v1.2 requirements satisfied, all phases verified, integration WIRED, no blockers.
Proceed to complete-milestone. The deferred ACC-01/02/03 + WSX-02/03 remain real-infra/v2
acceptances tracked outside v1.2.
