---
context: default
phase: v1.4 Ship & Harden — three PRs open awaiting review, then the operator ring
task: review-and-merge-open-prs
total_tasks: 4
status: paused
last_updated: 2026-08-12T06:00:00.000Z
---

# Resume: Burrow v1.4 — three PRs open, operator ring unchanged

v1.4 is **code-complete (12/13 phases)** and the signed + attested
`ghcr.io/bravebearstudios/burrow-{api,ui}:1.4.1` release is published (run `29355954285`,
ADR-0019 workflow_dispatch hatch). Phases 20 and 22 remain `human_needed`.

**What changed this session:** three PRs were opened and pushed. None are merged. One is
still mid-experiment. The published operator runbook artifact has every corrected command
for the human tracks.

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Never flip harden-runner `egress-policy: audit → block` with a guessed
  `allowed-endpoints` list.** A missing endpoint silently breaks cosign on the release path
  and can burn a release tag. Read the run's Step-Security insights per workflow, flip
  `ci.yml` first, prove it green, then `release.yml`. There are **six** call sites, not
  five: `ci.yml:32,153,200`, `codeql.yml:39`, `release.yml:84`, `release-please.yml:46`.
- [ ] CONSTRAINT: **Do not attempt the block-flip until a `ci.yml` run is green.** "Green
  under `block`" is unprovable against a red baseline, and B needs a **ci.yml** run's
  insights — the only insights URL on file is the release path, which is the wrong workflow.
- [ ] CONSTRAINT: **Do not run discuss/plan/execute on Phases 20 or 22.** Both are
  `human_needed` with context already gathered, so the autonomous workflow goes straight to
  plan → execute and writes plans for work no agent can perform.
- [ ] CONSTRAINT: **Do not run `/gsd-autonomous` until PR #26 merges.** Until then
  `gsd-tools` reads every phase as `missing` and discovery queues all 13 phases, including
  the archived v1.3 phases 10-14.

**Do not proceed past these until each box is checked.**

## Correction to the record: main is green, the dependabot PRs are red

Earlier handoffs said "CI red since 2026-07-27" without qualification. That was too broad.
`ci.yml` triggers on `pull_request` **and** `push: branches: [main]`. Filtering to push
events, `origin/main`'s last run is `29364341164` at `03ecb3b` — **success** — as are the
four before it.

The red runs are all `pull_request` runs on dependabot branches, and they are red for two
different reasons:

| Run | PR | Failing job | Cause |
|-----|----|-------------|-------|
| 30910698379 | #21 undici | Build + scan burrow-api | the Trivy HIGH CVEs |
| 31403711958 | #23 uv group | Tier-0 static gates | `ruff check`, unrelated |

The caveat that keeps the work necessary: main's green record is from 2026-07-14 and
**predates the CVE advisories**. Main is green only because nothing has been pushed to
re-scan it. Push anything today and the Trivy gate reds.

<current_state>
Branch `main`, clean, **3 commits ahead of `origin/main`** (two prior handoffs plus this
session's STATE update). Deliberately not pushed: a push to main triggers `ci.yml` on main,
which would red it on the Trivy gate before the fixes land.

Three PRs open, none merged. **All three fail ONLY `Build + scan burrow-api`**, for the same
shared reason inherited from main. #26 is documentation-only and touches no Python, which
proves the failure is not theirs. Do not wait for any one of them to go green alone.

Phase verification roll-call unchanged: 15, 16, 17, 18, 19, 21 `passed`; 20 and 22
`human_needed`.
</current_state>

<completed_work>
- **PR #24 `fix/trivy-high-cves`** — base-image `setuptools` + `msgpack`. Reduced to
  Dockerfile-only, because dependabot **#23 already carries the identical
  `cryptography==50.0.0` bump** in the same two files; duplicating it would only conflict.
  — **In progress, see blockers**
- **PR #25 `fix/pin-ruff-ruleset`** — pins `select = ["E4","E7","E9","F"]` in
  `api/pyproject.toml`. Root cause: ruff changed its DEFAULT rule set between 0.15.16 and
  0.16.2. Same tree, 0.15.16 reports "All checks passed", 0.16.2 reports 115 errors. The
  repo never declared a `select`, so the lint gate was implicitly version-coupled. Verified
  clean under BOTH versions; `ruff format --check` 90 files; mypy strict clean; 302 tests
  pass. **Unblocks #23.** — Done
- **PR #26 `fix/verification-frontmatter-order`** — moves the SPDX block below the
  frontmatter in 9 files (8 VERIFICATION.md + 22-HUMAN-UAT.md). `gsd-tools` requires `---`
  on line 1, so every phase parsed as `missing`. After the fix all 8 phases report their
  true status, matching STATE.md exactly. `reuse lint` compliant 502/502. `STATE.md` already
  used this ordering, so it aligns with an existing convention. — Done
- Operator runbook artifact updated with the `gh auth refresh` scope trap, Track A moved to
  lintool03, and the corrected Track D state. — Done
</completed_work>

<remaining_work>
- **Read the result of run `31568098155` first.** It is the provenance probe on #24 (see
  blockers). Everything else on #24 depends on what it printed.
- **Review + merge #25, then #26, then #24 + #23.** Green main needs all four.
- **A (operator):** `cosign verify` + `gh attestation verify` both `:1.4.1` digests from a
  homelab pull, **on lintool03**. Closes Phase 20 MH2 and Phase 22 UAT-5 together.
- **B (operator, blocked):** harden-runner `audit → block` from a green **ci.yml** run's
  insights, six call sites, `ci.yml` first as a reviewed PR.
- **C (operator):** `22-HUMAN-UAT.md` on den01 + lintool03, Step 0 deploy then UAT-1..5,
  **eight** `result: [pending]` markers to flip (not six — the Summary block miscounts).
</remaining_work>

<blockers>
- **#24 is mid-experiment and its answer is unread.** Run `31567559416` proved the pip
  upgrade works: `usr/local/.../setuptools-84.0.0` and `.../msgpack-1.2.1` both scan **0
  vulns**. But Trivy still reports `setuptools 70.3.0` and `msgpack 1.1.2` against a
  **pathless `Python` target** (2 vulns), so a second stale copy survives somewhere
  `pip install --upgrade` cannot reach. Commit `6c82c12` prunes
  `/usr/local/lib/python3.12/ensurepip/_bundled` and adds `ls`/`find` build-log probes to
  pin the provenance. Expected outcomes are enumerated in that commit message. If setuptools
  clears and msgpack does not, the probe output names msgpack's real path and the next
  commit targets it directly. **Do not allowlist** — RELX-05 forbids it for a fixable CVE.
- **A:** needs cosign + `gh` with `read:packages` on lintool03, and `docker login ghcr.io`.
  A bare `gh auth refresh -h github.com` without `-s read:packages` is a silent no-op that
  prints success — this already cost one round trip. Verify with
  `gh auth status | grep -i "token scopes"`.
- **B:** Step-Security insights are not gh-readable, and no green `ci.yml` run exists yet.
- **C:** needs the live Proxmox homelab. External.
</blockers>

## Required Reading (in order)
1. The published operator runbook artifact — every corrected command, the resolved
   placeholders, and the landmines. Ask the user for the URL; it is not in the repo.
2. The three open PRs' bodies — each carries its own root-cause writeup and measurements.
3. `.planning/phases/20-signed-ghcr-release-harden-runner-block/20-VERIFICATION.md` — MH1
   passed; MH2/MH3 human_needed and why.
4. `.planning/phases/22-live-homelab-acceptance-capstone/22-HUMAN-UAT.md` — the runbook.
   **Read it against the landmines; it has real defects.**
5. auto-memories `burrow-deploy` + `burrow-v14-milestone` — live topology, release status.

## Landmines in the committed runbook (corrected in the artifact, NOT yet in the repo)
- `WORKER_NODES=den01,den02` is an **import-time crash** and a restart loop. The field is
  `list[str]` with no comma parser and `Settings()` runs at module scope. Only
  `WORKER_NODES=["den01","den02"]` parses.
- Three `docker compose` lines omit `-f compose.prod.yml` (22-HUMAN-UAT.md:232, :274, :369)
  and die with `no configuration file provided: not found`.
- `:8081` is **not in git**. `compose.prod.yml` commits `"80:8080"`; the remap is an
  uncommitted edit on the lintool03 checkout. Lose it and health checks hit ntfy on `:80`.
- `burrow-api` publishes **no host port** (`expose: 8000` only). `curl 127.0.0.1:8000` is
  always refused. Host-side health is `:8081` or the NPM domain.
- Health fields are nested under `.data` — `jq .status` returns null.
- `<REL>` is **1.4.1**; 1.4.0 is burned with no published images. No `v1.4.x` git tag and no
  GitHub Release object exist.
- The release run published **two digests per image**; the attestation-manifest digests
  (`cc2fb8f9…` api, `6008451443…` ui) are decoys. The real ones are
  api `sha256:fc37afff…9338467d`, ui `sha256:6d5f9f1e…f159fbe0`.
- 22-HUMAN-UAT.md has **eight** `result: [pending]` markers against a Summary that counts six.

## Infrastructure State
- `main` clean, 3 commits unpushed by design. Three feature branches pushed with PRs.
- Release: `:1.4.1` signed + attested (run `29355954285`, `workflow_dispatch`, success).
  `v1.4.0` was BURNED (ADR-0019). **Release-please PR #11 must NOT be merged.**
- harden-runner still `audit` at six sites.
- Homelab (operator): lintool03 (10.0.1.38, control plane `:8081`, NPM domain), den01
  (10.0.0.6, Proxmox, golden template VMID 9000).

<next_action>
Read run `31568098155` (the #24 provenance probe). Its build log contains `ls` of
`ensurepip/_bundled` and `find` output for msgpack/setuptools, plus the resulting Trivy
count. That single result decides whether #24 is finished or needs one more targeted commit.

Then review and merge in order: #25 (unblocks #23), #26 (unblocks autonomous), then #24
and #23 together for green main.

Tracks A and C are operator-run and independent of all of the above.
</next_action>
