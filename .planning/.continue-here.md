---
context: default
phase: v1.4 Ship & Harden — milestone wrap (Phases 20 + 22 operator-gated, plus a NEW red-CI blocker)
task: track-D-awaiting-commit-approval
total_tasks: 4
status: paused
last_updated: 2026-08-05T00:05:18.898Z
---

# Resume: Burrow v1.4 — one code fix pending approval, then the operator ring

v1.4 is **code-complete (12/13 phases)** and the signed + attested
`ghcr.io/bravebearstudios/burrow-{api,ui}:1.4.1` release is published (run `29355954285`,
ADR-0019 workflow_dispatch hatch). Phases 20 and 22 remain `human_needed`.

**What changed this session:** CI is RED and has been since 2026-07-27. Three FIXABLE HIGH
CVEs fail the Trivy gate on `burrow-api`. That is not recorded in any planning doc; it was
found by querying live runs. It reorders the work, because the harden-runner block-flip
cannot be validated against a red baseline. The fix is written, locally verified, and
**uncommitted** on branch `fix/trivy-high-cves` — the approval gate was never answered.

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Never flip harden-runner `egress-policy: audit → block` with a guessed
  `allowed-endpoints` list.** A missing endpoint silently breaks cosign on the release path
  and can burn a release tag. Read the run's Step-Security insights per workflow, flip
  `ci.yml` first, prove it green, then `release.yml`.
- [ ] CONSTRAINT: **Do not attempt the block-flip while CI is red.** "Green under `block`" is
  unprovable against a baseline that is red under `audit`. Land Track D first.
- [ ] CONSTRAINT: **Do not run discuss/plan/execute on Phases 20 or 22.** Both are
  `human_needed` with context already gathered, so the autonomous workflow goes straight to
  plan → execute and writes plans and summaries for work no agent can perform.

**Do not proceed past these until each box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Guessed egress allowlist | Flipping egress to block without the run's Step-Security insights silently breaks cosign/OIDC/GHCR | blocking | Read insights per-workflow; flip ci.yml first, prove green, then release.yml as a reviewed PR |
| Block-flip on a red baseline | Attempting Track B while the Trivy gate fails makes a green result unattainable and unattributable | blocking | Confirm `gh run list --workflow ci.yml` shows a green run BEFORE editing any egress-policy line |
| Faking operator work | Running discuss/plan/execute on Phases 20/22 produces fake progress; the open items need real hardware and creds, not codegen | blocking | Verify status is `human_needed`; these close on operator evidence |
| Verify by tag | `cosign verify` / `gh attestation verify` against a tag rather than the digest is unsound (T-14-01 exit-0 trap) | advisory | Verify by `@sha256:<DIGEST>`; assert on printed OUTPUT, not exit code |
| Trusting "5 call sites" | Every planning doc says five harden-runner sites; there are six. `codeql.yml:39` is unlisted and has no seeded allowlist comment | advisory | Grep `egress-policy` across `.github/workflows/` and decide about codeql.yml explicitly |
| Allowlisting a fixable CVE | RELX-05 policy: a FIXABLE HIGH/CRITICAL is cleared by upgrade, never by a `.trivyignore` entry | advisory | Check the Status column in Trivy output; `fixed` means upgrade |

<current_state>
Branch `fix/trivy-high-cves` (off `main`), **nothing committed**. `main` is 1 commit ahead of
`origin/main` (`6b787c7`, the previous handoff).

Uncommitted:
- `api/pyproject.toml`, `api/uv.lock`, `Dockerfile.api` — Track D, ready, awaiting approval
- `.planning/STATE.md` — resume reconcile, deliberately held OUT of the D commit
- `.planning/.continue-here.md`, `.planning/HANDOFF.json` — this handoff

Phase verification roll-call: 15, 16, 17, 18, 19, 21 `passed`; 20 and 22 `human_needed`.
</current_state>

<completed_work>
- Task 1: Resumed and reconciled `STATE.md`. It was ~3 weeks stale: still instructed "RESUME
  15-03 (RELX-03)" although Phase 15 verification reads `passed`, and named branch
  `feat/v1.4-harden` when the actual branch is `main`. Both corrected. — Done
- Task 2: Verified every operator command against source (10 agents, 169 claims, 8 refuted).
  Resolved `<REL>` and both image digests and the insights URL from the release run. — Done
- Task 3: Published the operator runbook artifact — per-step "which box" badges, copy-ready
  commands, persistent checkboxes, and the 8 landmines below. — Done
- Task 4: Track D — the Trivy CVE fix. Code complete, locally verified. — **In progress
  (uncommitted, awaiting approval)**
</completed_work>

<remaining_work>
- **D (agent-executable, blocks B):** commit + push `fix/trivy-high-cves`, open the PR,
  confirm `ci.yml` goes green. If msgpack persists in the SARIF, locate it inside the image
  and strip or repin rather than allowlist.
- **A (operator):** `cosign verify` + `gh attestation verify` both `:1.4.1` digests from a
  homelab pull. Closes Phase 20 MH2 and Phase 22 UAT-5 together.
- **B (operator, blocked on D):** harden-runner `audit → block` from real insights, six call
  sites, `ci.yml` first as a reviewed PR.
- **C (operator):** `22-HUMAN-UAT.md` on den01 + lintool03, Step 0 deploy then UAT-1..5,
  eight `result: [pending]` markers to flip (not six — the Summary block miscounts).
</remaining_work>

<decisions_made>
- Halted `/gsd-autonomous` at the blocker gate rather than running discuss/plan/execute on
  Phases 20/22 — both would have produced fabricated progress.
- Track D is a prerequisite for Track B, not a side quest: a block-flip cannot be validated
  against a red baseline.
- Fixed the CVEs in two places because the SARIF shows two distinct targets. `cryptography`
  lives in `app/.venv` (lockfile-reachable); `setuptools` and `msgpack` live in the base
  image's system site-packages and are absent from `api/uv.lock`, so only the Dockerfile
  can reach them.
- `setuptools>=83.0.0` rather than the 78.1.1 gate minimum, to clear CVE-2026-59890 (MEDIUM)
  in the same move. Zero risk: the app runs from `/app/.venv`.
- Did NOT repin the `python:3.12-slim` base. Checked the registry: the latest index
  (`sha256:57cd7c3a...`) carries the same `PYTHON_VERSION 3.12.13` and declares no setuptools
  version, so a repin would not predictably clear the finding.
- Held `STATE.md` out of the D commit to keep the PR diff to the three files CI cares about.
</decisions_made>

<blockers>
- **D uncommitted:** the approval gate was never answered. Nothing is committed or pushed.
- **msgpack provenance unresolved:** the SARIF reports it against the bare `Python` target
  with no file path, and it is absent from `uv.lock`. The Dockerfile upgrade is the best
  available lever but is unproven. Docker is unavailable on this Windows host, so the
  image-level result is CI-verified only.
- **A:** session `gh` token lacks `read:packages` (confirmed scopes: `admin:org, gist, repo,
  workflow`), no local cosign, GHCR packages private. All human-action.
- **B:** Step-Security insights are not gh-readable. Human-action.
- **C:** needs the live Proxmox homelab. External.

Note: 12 boot-harness tests fail on this dev box because `bash` resolves to `wsl.exe` and no
WSL distro is installed. Linux/CI-only, unrelated to any change. Excluding them: 280 passed.
</blockers>

## Required Reading (in order)
1. The published operator runbook artifact — every corrected command, the resolved
   placeholders, and the 8 landmines. Ask the user for the URL; it is not in the repo.
2. `.planning/phases/20-signed-ghcr-release-harden-runner-block/20-VERIFICATION.md` — MH1
   passed; MH2/MH3 human_needed and why.
3. `.planning/phases/22-live-homelab-acceptance-capstone/22-HUMAN-UAT.md` — the runbook.
   **Read it against the landmines below; it has real defects.**
4. auto-memories `burrow-deploy` + `burrow-v14-milestone` — live topology, release status.

## Landmines in the committed runbook (corrected in the artifact, NOT yet in the repo)
- `WORKER_NODES=den01,den02` is an **import-time crash** and a restart loop. The field is
  `list[str]` with no comma parser and `Settings()` runs at module scope. Only
  `WORKER_NODES=["den01","den02"]` parses. Measured against the repo venv.
- Three `docker compose` lines omit `-f compose.prod.yml` (22-HUMAN-UAT.md:232, :274, :369)
  and die with `no configuration file provided: not found`.
- `:8081` is **not in git**. `compose.prod.yml` commits `"80:8080"`; the remap is an
  uncommitted edit on the lintool03 checkout. Lose it and health checks hit ntfy on `:80`.
- `burrow-api` publishes **no host port** (`expose: 8000` only). `curl 127.0.0.1:8000` is
  always refused. Host-side health is `:8081` or the NPM domain.
- Health fields are nested under `.data` — `jq .status` returns null.
- `<REL>` is **1.4.1**; the runbook still says it "may be 1.4.0", which is burned with no
  published images. No `v1.4.x` git tag and no GitHub Release object exist.
- The release run published **two digests per image**; the attestation-manifest digests
  (`cc2fb8f9…` api, `6008451443…` ui) are decoys.
- 22-HUMAN-UAT.md has **eight** `result: [pending]` markers against a Summary that counts six.

## Infrastructure State
- `main` clean, 1 commit unpushed. Branch `fix/trivy-high-cves` carries the uncommitted fix.
- Release: `:1.4.1` signed + attested (run `29355954285`, `workflow_dispatch`, success).
  `v1.4.0` was BURNED (ADR-0019). **Release-please PR #11 must NOT be merged** — still open,
  still titled `chore(main): release 1.4.0`.
- Image digests: api `sha256:fc37afffd3cf8d2f81dd122ec4e0c4c4e80ca3f387614fd5ae7ebe5b9338467d`,
  ui `sha256:6d5f9f1e0810f7cce53cf3a542496e64aa50b122c1399fc796c63fc4f159fbe0`.
- Egress insights: `https://app.stepsecurity.io/github/BraveBearStudios/burrow/actions/runs/29355954285`
  (release path). Track B needs a **ci.yml** run's insights, which requires a green CI first.
- harden-runner still `audit` at six sites: `ci.yml:32,153,200`, `codeql.yml:39`,
  `release.yml:84`, `release-please.yml:46` (planning docs cite `:45`, which is `with:`).
- Homelab (operator): lintool03 (10.0.1.38, control plane `:8081`, NPM domain), den01
  (10.0.0.6, Proxmox, golden template VMID 9000).

<context>
The milestone's remaining ring was believed to be purely human: verify signatures, flip
egress, exercise the homelab. That is still true for A and C, but B turned out to be gated on
something nobody had noticed — the CI baseline is red, and has been for over a week, on three
CVEs that are all marked FIXABLE. So there is one piece of ordinary code work left in v1.4
after all, and it sits on the critical path. It is written and it passes locally. It needs a
yes or a no, not more analysis.

The other half of the session went into making the operator's night survivable: checking
every command in the committed UAT runbook against the actual source rather than trusting it,
and finding that several would have failed silently at 1am.
</context>

<next_action>
Answer the pending approval on the Track D commit (three files on `fix/trivy-high-cves`;
the full message was presented in-session and is reproducible from the decisions above).

If yes: commit, push, open the PR, watch `ci.yml`. A green run unblocks Track B and yields
the insights URL it needs.

Tracks A and C are operator-run and independent of all of the above — the published runbook
artifact has every command, corrected.
</next_action>
