---
context: default
phase: ACC-01 real-infra-acceptance (deploy)
task: 6
total_tasks: 9
status: in_progress
last_updated: 2026-07-03T21:16:56.110Z
---

# Resume: Burrow real-infra deploy acceptance (ACC-01)

Paused one step before the payoff: **create a real workspace and confirm the worker boots
green.** Base deploy + ADR-0015 credential store are proven; the git PAT (the worker-boot
unblock) is stored. Next click creates the workspace and reads the den01 worker journal.

Deep, current state lives in the auto-memories (updated this session):
`burrow-deploy` and `burrow-secret-mgmt-feature`. Read those first.

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong port on lintool03 | Curling `:80` hits **ntfy** (a different homelab service) and returns a misleading `{"code":40401,...}` JSON 404. | advisory | Burrow is on **:8081**. Always use `http://127.0.0.1:8081` / `http://10.0.1.38:8081`. |
| Stale nginx upstream after api recreate | `--force-recreate burrow-api` gives api a (possibly) new IP; `burrow-web` nginx resolved the old upstream IP once at startup -> `:8081` returns **502** until web re-resolves. | advisory | After any `burrow-api` recreate, run `docker compose -f compose.prod.yml up -d --force-recreate burrow-web` (or expect a transient 502 that clears when web restarts). |
| Testing the store with the wrong credential | Storing `proxmoxTokenValue` proved the ADR-0015 mechanism but NOT the deploy goal — the proxmox token already worked via `.env` (compute:ok). | advisory | For worker boot, the unblock is `{gitToken}`, not `{proxmoxTokenValue}`. Done this session (lg83). |

<current_state>
Deploy host **lintool03** on branch `feat/gui-managed-secrets` @ `9a9b197`. Control plane
UP at `:8081`: `burrow-api` healthy (feat image, real Proxmox `compute:ok`), `burrow-web`
recreated. `/data/burrow.db` holds three encrypted secrets: admin gate (argon2), proxmox
token (last4 **7640**, redundant), git PAT (last4 **lg83**, the worker-boot unblock).
`/opt/burrow/.env` has `TTYD_TIMEOUT=600` (failing workers linger ~10min for inspection).
No worker has been booted yet this run.
</current_state>

<completed_work>
- Task 1: Diagnosed the `:80` 404 false-alarm (ntfy owns :80; burrow is :8081) — Done
- Task 2: Switched host main -> feat/gui-managed-secrets (@9a9b197; +cryptography +argon2; 004 auto-migrates) — Done
- Task 3: Rebuilt burrow-api + force-recreated stale burrow-web — Done
- Task 4: Verified control plane db:ok / compute:ok (real Proxmox) at :8081 — Done
- Task 5: ADR-0015 store proven: admin-secret set + proxmox token stored (7640) — Done
- Task 6: Stored gitToken (lg83) + TTYD_TIMEOUT=600 + recreated api — Done
</completed_work>

<remaining_work>
- Task 7: ACC-01 CREATE — create workspace (thezoid/fable-test) in browser, watch den01 journal for green vs auth-fail
- Task 8: Finish H9 — terminal (live claude) -> stop -> start -> destroy on the real worker
- Task 9: Commit + deploy the H2 error-honesty fix (workspaceService.py TransportError + test_wait_ttyd.py)
</remaining_work>

<decisions_made>
- Deploy feat/gui-managed-secrets (not main) — only feat reads BURROW_SECRET_KEY / has the store
- Store gitToken not just proxmox — proxmox was redundant; the private-repo git-clone 401 is the real blocker
- Always force-recreate burrow-web after burrow-api recreate — nginx upstream IP staleness -> 502
</decisions_made>

<blockers>
- Worker boot green not yet confirmed (the ACC-01 gate itself). TTYD_TIMEOUT=600 keeps a failing worker alive for journal reads.
- PAT lg83 must clone BOTH BraveBearStudios/burrow AND thezoid/fable-test (classic repo scope; one global token reused for both). If auth-fails, the `cloning <config|project> repo` journal line names the repo it lacked.
</blockers>

## Required Reading (in order)
1. auto-memory `burrow-deploy` — live topology (den01 / lintool03), the worker-boot blocker + fix path, journal signatures
2. auto-memory `burrow-secret-mgmt-feature` — ADR-0015 store design + real-infra-proven status
3. `docs/test-it-out-checklist.md` §3 (H9 five-step gate) — the acceptance script

## Infrastructure State
- **lintool03** (10.0.1.38): compose stack up. `burrow-api` healthy (feat 9a9b197), `burrow-web` recreated, host `:8081`. `TTYD_TIMEOUT=600`. `/data/burrow.db` = admin secret + proxmox(7640) + git(lg83) encrypted. NPM fronts it at `https://burrow.local.bravebearstudios.com`.
- **den01** (10.0.0.6): Proxmox, golden template VMID 9000 (CONTROL_PLANE=http://10.0.1.38:8081 baked), worker VMID range 969-1022 -> IPs 10.0.0.201-.254.
- Uncommitted local work (E:\repos\burrow): H2 fix (`M api/services/workspaceService.py`, `?? api/tests/unit/test_wait_ttyd.py`) — NOT yet on the deployed 9a9b197 image.

<context>
Everything upstream of the worker boot is green. This session's arc was: kill the :80/:8081
confusion, move the host onto the feat branch, prove the credential store on real Proxmox,
and store the git PAT that unblocks the private-repo clone at worker boot. The only unknown
left is whether the lg83 PAT actually reaches both repos — the journal will say immediately.
</context>

<next_action>
Browser -> http://10.0.1.38:8081/ -> New Workspace -> thezoid/fable-test + a branch -> Create.
Do NOT watch the spinner (synchronous, waits up to 600s). Immediately on den01:
  W=$(pct list | awk 'NR>1 && $1>=969 && $1<=1022 {print $1; exit}'); echo "worker=$W"
  pct exec "$W" -- journalctl -u burrow-worker -b -f
GREEN = `cloning config repo` -> `cloning project repo` -> `Listening on port 7681`
        (then `ss -tlnp | grep 7681`). AUTH-FAIL = `Authentication failed / ERROR (exit 128)`;
        the `cloning ... repo` line names which repo the lg83 PAT lacked -> fix PAT, re-store, re-create.
</next_action>
