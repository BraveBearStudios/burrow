---
context: default
phase: v1.4 Ship & Harden — milestone wrap (Phases 20 + 22 operator-gated)
task: operator-handoff
total_tasks: 3
status: paused
last_updated: 2026-07-14T22:03:54.529Z
---

# Resume: Burrow v1.4 milestone wrap — operator follow-ups only

v1.4 is **code-complete (12/13 phases, 92%)**. All code phases merged to `main`; the
signed + attested `ghcr.io/bravebearstudios/burrow-{api,ui}:1.4.1` release is published
(run `29355954285`, ADR-0019 workflow_dispatch hatch). The `/gsd-autonomous` run this
session found **nothing left that autonomous can safely execute** — both open phases are
`human_needed` and blocked on operator hardware / registry creds / CI insights. This is a
hand-off pause, not a mid-code pause. No uncommitted code.

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered/recorded through the Phase 20 release cut. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Never flip harden-runner `egress-policy: audit → block` with a
  guessed `allowed-endpoints` list.** A missing endpoint silently breaks cosign on the
  release path (and can burn a release tag). Structural mitigation: read run
  `29355954285`'s Step-Security egress insights first (per-workflow — CI, CodeQL, release
  each differ), fill the real allowlist, and flip `ci.yml` first to prove green under
  `block` before touching `release.yml`.

**Do not proceed to the harden-runner flip until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Guessed egress allowlist | Flipping egress→block without the run's Step-Security insights silently breaks cosign/OIDC/GHCR path | blocking | Read insights per-workflow; flip ci.yml first, prove green, then release.yml as reviewed PR |
| Faking operator work | Running discuss/plan/execute on Phases 20/22 produces fake progress — the open items need real hardware/creds, not codegen | advisory | Verify status is `human_needed`; these close on operator evidence, not agent execution |
| Verify by tag | `cosign verify` / `gh attestation verify` against a tag not the digest is unsound (T-14-01 exit-0 trap) | advisory | Verify by `@sha256:<DIGEST>`; assert on printed OUTPUT, not exit code |

<current_state>
`main` @ clean, 0 unpushed. Signed release `:1.4.1` live. Two open phases, both
`status: human_needed`:
- **Phase 20** (Signed GHCR Release & Harden-Runner Block) — MH1 PASSED (signed release
  exists). MH2 (independent verify) + MH3 (harden-runner block-flip) are operator-only.
- **Phase 22** (Live Homelab Acceptance Capstone) — pure HUMAN-UAT; 5 criteria on the
  real Proxmox homelab (den01 + lintool03). Runbook committed at
  `.planning/phases/22-live-homelab-acceptance-capstone/22-HUMAN-UAT.md`.
This session produced an operator command sheet (chat only — no files changed).
</current_state>

<completed_work>
- Assessed the autonomous run: discovered both remaining phases are operator-gated — Done
- Pulled exact verify + harden-runner commands from phase artifacts (not invented) — Done
- Delivered operator handoff sheet: cosign/attestation verify, harden-runner flip
  procedure (5 call sites), Phase 22 UAT pointer — Done
</completed_work>

<remaining_work>
All three are operator-run (no agent-executable code):
- **A. Verify** (Phase 20 MH2 / Phase 22 UAT-5): `cosign verify` + `gh attestation verify`
  a homelab-pulled `@sha256:` digest. Needs cosign + jq + `gh` w/ `read:packages` +
  `docker login ghcr.io` (GHCR pkgs private by default).
- **B. Harden-runner** (Phase 20 MH3): read run `29355954285` Step-Security insights →
  fill `allowed-endpoints` + set `egress-policy: block` at the 5 call sites → reviewed PR
  (ci.yml first). See BLOCKING CONSTRAINT.
- **C. Phase 22 UAT**: run `22-HUMAN-UAT.md` on den01 + lintool03. Step 0 deploy `:1.4.1`
  to lintool03 (`BURROW_SECRET_KEY` unchanged, health ok/ok/ok on :8081), then 5 criteria:
  UAT-1 reaper+idle+capacity, UAT-2 least-loaded node, UAT-3 persistence stop→start,
  UAT-4 GUI credential store (migration 004, last4 stable no-restart + survives-restart),
  UAT-5 live re-verify (= A). Flip each `[ ]`→`[x]` with recorded evidence.
</remaining_work>

<decisions_made>
- Stopped `/gsd-autonomous` — no phase has safe autonomous work; forcing it would fake
  progress on human-gated acceptance.
- Did NOT draft the harden-runner block with a guessed allowlist (verification forbids it;
  would break future builds).
- Milestone stays OPEN until operator records UAT evidence — do not audit/complete/cleanup
  v1.4 prematurely.
</decisions_made>

<blockers>
- Verify (A): session token lacks `read:packages`, no local `cosign`, GHCR pkgs private — all human-action.
- Harden-runner (B): needs the run's Step-Security egress insights (not gh-readable) — human-action.
- Phase 22 (C): needs the live Proxmox homelab; not CI-provable — human-action / external.
</blockers>

## Required Reading (in order)
1. `.planning/phases/20-signed-ghcr-release-harden-runner-block/20-VERIFICATION.md` — MH1 passed, MH2/MH3 human_needed + why
2. `.planning/phases/20-signed-ghcr-release-harden-runner-block/20-SUMMARY.md` — release-trigger gap + ADR-0019 recovery
3. `.planning/phases/22-live-homelab-acceptance-capstone/22-HUMAN-UAT.md` — the 5-criterion UAT runbook (Step 0 + UAT-1..5)
4. auto-memories `burrow-deploy` + `burrow-v14-milestone` — live topology, release status, don't-merge PR #11

## Infrastructure State
- `main` clean, 0 unpushed. No uncommitted code this session.
- Release: `:1.4.1` signed + attested on GHCR (run `29355954285`). `v1.4.0` was BURNED (trigger-gap + immutable-releases; ADR-0019). Release-please PR #11 must NOT be merged.
- Homelab (operator): lintool03 (10.0.1.38, control plane :8081, NPM domain), den01 (10.0.0.6, Proxmox, golden template VMID 9000, worker range 969-1022).
- harden-runner call sites still `egress-policy: audit`: `ci.yml:32,153,200`, `codeql.yml:39`, `release.yml:84`, `release-please.yml:45`.

<context>
The whole v1.4 code effort is landed and green; the signed release is real. What remains
is the human acceptance ring: prove the signatures independently, lock down CI egress from
real insights, and exercise the whole thing on live hardware. None of it is codegen — it's
operator hands on creds and the homelab. The right move was to stop autonomous cleanly and
hand over exact commands rather than fabricate phase execution.
</context>

<next_action>
Operator runs A + B + C (above / handoff sheet in chat). Once verify passes, the
harden-runner PR is green under `block`, and the 22-HUMAN-UAT.md checklist is all `[x]`
with evidence, resume with:
  /gsd-autonomous --from 20
That re-reads verification, sees `passed`, and runs the milestone lifecycle
(audit → complete → cleanup) to close v1.4.
</next_action>
